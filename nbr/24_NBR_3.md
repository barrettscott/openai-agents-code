# Review Report: Human-in-the-Loop
Generated: 2026-05-10 | Notebook: /Users/scott/Dropbox/Notebooks/openai-agents/Week_4_Memory_Safety_Observability/24_Human_In_The_Loop.ipynb
Models: Anthropic (claude-opus-4-7 (CLI/Max)) · OpenAI (gpt-5.4) · Gemini (gemini-2.5-pro) · Grok (grok-4.20-0309-reasoning) · DeepSeek (deepseek-reasoner) · Dedup (claude-opus-4-7 CLI/Max)

---

# Grouped Findings: Human-in-the-Loop

# Third-Pass Synthesis — Notebook 24 (Human-in-the-Loop)

Five reviewers, twelve distinct findings after dedup. Comprehension issues cluster heavily in two places: the Part 1 conceptual overview (which skips key SDK names a student needs) and the Part 4 threshold helper (which bundles too many novel mechanics into one demo). The findings below are ordered by severity and by how many reviewers independently flagged them.

---

## Finding 1 — `state` / `result.to_state()` introduced as if already known

**Severity:** `[confidence gap]` / `[unexplained term]`
**Location:** Part 1 ("How HITL Works"), step 3 of the four-step flow
**Reviewers:** 4 of 5 — Anthropic, OpenAI, Gemini, Grok
**Quote:** *"3. You inspect what the agent wants to do — call `state.approve(interruption)` or `state.reject(interruption)`."* Then Part 2 code opens with `state = result.to_state()` with no warning.
**What the student experiences:** Part 1 makes `state` sound like an ambient object. When `result.to_state()` appears in code with no introduction, the student wonders where `state` comes from and whether `to_state()` is always required. This is the exact line they'd need to copy into their own project.
**Smallest fix:** Rewrite step 3: *"You convert the run result with `state = result.to_state()`, then call `state.approve(interruption)` or `state.reject(interruption)`."*
**Blocks transfer:** Yes
**Outline-backed:** Yes (approval workflow pattern)

---

## Finding 2 — `arguments` is a JSON string, never explained

**Severity:** `[confidence gap]`
**Location:** Part 2 ("Step 1: Run Until Paused") inspection prints; Part 4 `handle_refund_request` uses `json.loads()` with no setup
**Reviewers:** 3 of 5 — Anthropic, Gemini, DeepSeek
**Quote:** Part 2: `print(f"    Args: {interruption.raw_item.arguments}")` (prints what looks like a dict). Part 4: `args = json.loads(interruption.raw_item.arguments)` (suddenly needs parsing).
**What the student experiences:** `arguments` looks like a dict when printed, so the student writes `interruption.raw_item.arguments["amount"]` in their own code and gets `TypeError: string indices must be integers`. A frustrating roadblock right at the moment they try to extend the pattern.
**Smallest fix:** One inline comment above the `json.loads` line in Part 4: *"# arguments comes back as a JSON string — parse it to read individual fields"*. Optionally add the same note in Part 2 after the prints.
**Blocks transfer:** Yes
**Outline-backed:** No

---

## Finding 3 — `raw_item` unexplained

**Severity:** `[unexplained term]`
**Location:** Part 2, "Step 1: Run Until Paused"
**Reviewers:** 3 of 5 — Anthropic, Grok, DeepSeek
**Quote:** `print(f"🔔 Tool: {interruption.raw_item.name}")` and `print(f"    Args: {interruption.raw_item.arguments}")`
**What the student experiences:** "Why `raw_item` and not just `interruption.name`? Is there a non-raw item too?" They can copy the line, but they don't have a model of what `interruption` contains, and they can't autocomplete in a notebook.
**Smallest fix:** One sentence after the inspection block: *"Each interruption wraps a `raw_item` — that's the pending tool call, with `.name` and `.arguments` (as a JSON string) available for inspection."*
**Blocks transfer:** No (demo works), but reduces confidence about extending it.
**Outline-backed:** No

---

## Finding 4 — `while result.interruptions:` loop reasoning unclear

**Severity:** `[lost why]`
**Location:** Part 4, `handle_refund_request`
**Reviewers:** 2 of 5 — Anthropic, Gemini
**Quote:** `# Loop handles cases where resuming triggers additional approvals` followed by `while result.interruptions:`
**What the student experiences:** "Why `while` here when earlier examples used `if`? What 'additional approvals' — the demo only shows one." Without a concrete example, they don't know whether to use `if` or `while` in their own code.
**Smallest fix:** Expand the comment: *"# Use a while loop because the agent may call another approval-required tool after resuming — e.g., agent looks up the order, then asks to refund, then asks to send a confirmation email."*
**Blocks transfer:** No (the code works either way)
**Outline-backed:** No

---

## Finding 5 — Reusable pattern never named

**Severity:** `[confidence gap]`
**Location:** Part 2 "Why This Works" cell
**Reviewers:** 1 of 5 — OpenAI (Grok raises a related concern in Finding 9)
**Quote:** Notebook shows the run → `to_state()` → approve → resume sequence but never labels it as the reusable pattern.
**What the student experiences:** They followed the demo but don't have a clean mental template for "this is what I copy into my own project."
**Smallest fix:** Add one sentence to the "Why This Works" cell: *"This is the reusable pattern: run until interrupted, convert to `state`, approve or reject each interruption, then resume with that same state."*
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Finding 6 — Why SDK-level approval beats instruction-level

**Severity:** `[lost why]`
**Location:** Part 2 ("Basic Approval Flow"), opening sentence
**Reviewers:** 1 of 5 — OpenAI
**Quote:** *"We'll build an email agent where sending always requires human approval."*
**What the student experiences:** Coming from the previous notebook's instruction-based confirmation pattern, the student sees this as "another way to do the same thing" rather than a meaningful upgrade.
**Smallest fix:** Add one sentence: *"This matters because approval now happens in the SDK — the tool cannot run just because the agent ignored its instructions."*
**Blocks transfer:** No
**Outline-backed:** Yes

---

## Finding 7 — Hardcoded `decision = "yes"`: what to replace in production

**Severity:** `[confidence gap]`
**Location:** Part 4 threshold approval logic
**Reviewers:** 1 of 5 — OpenAI
**Quote:** `# In production, prompt a human via UI or messaging system` (with hardcoded `decision = "yes"` below)
**What the student experiences:** They see the demo simplification but don't know which line to replace in a real app.
**Smallest fix:** One sentence near the demo: *"In your own app, this is the line you'd replace with a real approval source — a button click in a UI, a Slack reply, or a CLI `input()` prompt."*
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Finding 8 — "Threshold" / "escalate" as workflow design terms

**Severity:** `[unexplained term]`
**Location:** Part 4 intro
**Reviewers:** 1 of 5 — OpenAI
**Quote:** *"A common pattern: auto-approve actions below a risk threshold, escalate to a human for high-risk ones, based on the tool's parameters."*
**What the student experiences:** Beginners may wonder if the threshold is something the SDK provides, the model decides, or they must define themselves.
**Smallest fix:** *"The SDK does not choose this threshold for you — you write the rule in Python, such as 'auto-approve refunds under $50.'"*
**Blocks transfer:** No
**Outline-backed:** Yes

---

## Finding 9 — Part 4 helper bundles too many novel mechanics into one cell

**Severity:** `[pacing break]`
**Location:** Part 4 single cell with tools + agent + 40-line `handle_refund_request`
**Reviewers:** 1 of 5 — Grok (related to OpenAI's Finding 5 about naming the pattern)
**Quote:** Part 4 header followed immediately by tool definitions, agent creation, and the long helper function in one cell.
**What the student experiences:** Risk-threshold concept + JSON parsing + `while` loop + state-resume pattern arrive simultaneously. The student loses the focal point.
**Smallest fix:** Split the cell — keep the two `@function_tool` definitions + agent in one cell, then put `handle_refund_request` in its own cell with a one-sentence prose lead-in.
**Blocks transfer:** No (slows learning)
**Outline-backed:** Yes

---

## Finding 10 — Relationship between `result.interruptions` and `state.get_interruptions()`

**Severity:** `[confidence gap]`
**Location:** Part 2 "Step 2: Approve and Resume" and Part 4 helper
**Reviewers:** 1 of 5 — DeepSeek
**Quote:** `if result.interruptions:` followed by `state = result.to_state()` then `for interruption in state.get_interruptions():`
**What the student experiences:** Two ways to access pending approvals appear without their relationship being explained. A student might iterate over `result.interruptions` after creating state, or skip `state.get_interruptions()` entirely.
**Smallest fix:** Replace `# Capture the resumable state` with: *"# Capture the resumable state — use state.get_interruptions() to access pending approvals (same list as result.interruptions)"*
**Blocks transfer:** Yes
**Outline-backed:** No

**Reviewer split note:** Only DeepSeek flagged this. Other reviewers either didn't notice the dual API or didn't see it as a transfer blocker. Reasonable case for treating as low-priority — once a student has the pattern from Finding 1, they may just follow it mechanically.

---

## Finding 11 — Why rejection matters in real projects

**Severity:** `[lost why]`
**Location:** End of Part 3 (Rejection demo), before Part 4 header
**Reviewers:** 1 of 5 — Grok
**Quote:** No "Why This Works" cell after the rejection demo.
**What the student experiences:** They saw the agent gracefully switch to drafting after rejection but don't know why this matters on a real project.
**Smallest fix:** One markdown sentence after the rejection demo: *"Rejection is useful because the agent receives context it can use to adapt instead of failing or retrying the same forbidden action."*
**Blocks transfer:** No
**Outline-backed:** Yes

---

## Recommended action order

1. **Ship Findings 1, 2, 3** — flagged by 3+ reviewers, all touch the lines a student would copy. Highest leverage.
2. **Ship Findings 5, 7** — single-reviewer but explicit confidence gaps that name a transferable pattern or replacement point.
3. **Ship Findings 4, 6, 8, 11** — one-sentence "lost why" / unexplained-term fixes, cheap to add.
4. **Consider Finding 9** — splitting the Part 4 cell is a bit more invasive but combines well with Finding 5's "minimal pattern" sentence.
5. **Defer Finding 10** — single reviewer, partly mitigated once Finding 1 lands.

---

# Individual Reviews

The grouped findings above are your primary working surface. The full individual reviews below are for verifying nuance.

---

## Anthropic Review

# Third-Pass Review — Notebook 24: Human-in-the-Loop

## Findings

---

**Severity:** `[confidence gap]`

**Location:** Part 1 ("How HITL Works") → Part 2 ("Step 2: Approve and Resume")

**Quote:** Part 1 says: *"You inspect what the agent wants to do — call `state.approve(interruption)` or `state.reject(interruption)`."* Part 2 code then opens with: *"state = result.to_state()"*

**What the student experiences:** Part 1's four-step flow makes `state` sound like something that just exists. Then in Part 2 a brand-new method `result.to_state()` appears with no warning. The student wonders: "Wait, where does `state` come from? Is `to_state()` always required, or only sometimes?" This is the exact line they'd need to copy into their own project — and the conceptual overview skipped it.

**Smallest fix:** Add `result.to_state()` into step 3 of the Part 1 flow: *"You inspect what the agent wants to do — call `state = result.to_state()` to get the resumable state, then `state.approve(interruption)` or `state.reject(interruption)`."*

**Blocks transfer:** Yes

**Outline-backed:** Yes (outline calls for "approval workflow pattern")

---

**Severity:** `[confidence gap]`

**Location:** Part 4, inside `handle_refund_request`

**Quote:** *"args = json.loads(interruption.raw_item.arguments)"*

**What the student experiences:** In Part 2, `interruption.raw_item.arguments` was printed directly as if it were already-readable text. Now in Part 4 it suddenly needs `json.loads()` to access fields. The student thinks: "Is `arguments` a string or a dict? When do I need `json.loads`?" If they wrote their own threshold logic, they'd reasonably try `interruption.raw_item.arguments["amount"]` and get a confusing error.

**Smallest fix:** One-sentence comment above the line: *"# arguments comes back as a JSON string — parse it to read individual fields"*

**Blocks transfer:** Yes

**Outline-backed:** Yes (outline calls for "confidence-based escalation")

---

**Severity:** `[lost why]`

**Location:** Part 4, `handle_refund_request` function

**Quote:** *"# Loop handles cases where resuming triggers additional approvals"*

**What the student experiences:** The student sees `while result.interruptions:` and a comment hinting at "additional approvals" — but no example shows when this happens. They wonder: "When would resuming trigger another approval? Is this defensive code I can ignore, or something I'll actually hit?" Without an answer, they don't know whether to use `while` or `if` in their own code.

**Smallest fix:** Expand the comment to one concrete example: *"# Use a loop because the agent may call another approval-required tool after resuming — e.g., agent looks up the order, then asks to refund, then asks to send a confirmation email."*

**Blocks transfer:** No (the code works either way), but it leaves an open question.

**Outline-backed:** No (outline doesn't call out this specific pattern)

---

**Severity:** `[unexplained term]`

**Location:** Part 2, "Step 1: Run Until Paused"

**Quote:** *"print(f\"🔔 Tool: {interruption.raw_item.name}\")"*  and  *"print(f\"    Args: {interruption.raw_item.arguments}\")"*

**What the student experiences:** `raw_item` appears with no explanation. The student wonders: "Why `raw_item` and not just `interruption.name`? Is there a non-raw item too? What else lives on `interruption`?" They can copy the line, but they don't have a mental model of what `interruption` actually contains.

**Smallest fix:** One sentence after the inspection block: *"Each interruption wraps a `raw_item` — that's the pending tool call, with the tool's `.name` and `.arguments` (as a JSON string) available for inspection."*

**Blocks transfer:** No (the demo code works), but reduces confidence about extending it.

**Outline-backed:** No

---

## Summary

Four findings. The first two are the load-bearing ones — both touch the exact lines a student would copy into their own project. Findings 3 and 4 are smaller comprehension smoothers; ship if cheap, defer if not.

---

## OpenAI Review

**Severity:** [unexplained term]  
**Location:** Part 1: How HITL Works  
**Quote:** “3. You inspect what the agent wants to do — call `state.approve(interruption)` or `state.reject(interruption)`.”  
**What the student experiences:** The student has not seen `state` yet, so this reads like a magic object that already exists somewhere. They can follow the broad flow, but not confidently enough to predict where `state` comes from when they try this on their own.  
**Smallest fix:** Add 4–6 words the first time `state` appears: “call `result.to_state()` to get a resumable `state`, then use `state.approve(interruption)` or `state.reject(interruption)`.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** Part 2: Basic Approval Flow  
**Quote:** “We'll build an email agent where sending always requires human approval.”  
**What the student experiences:** They understand the demo mechanics, but not the real-project reason for using SDK-level approval instead of the instruction-level confirmation pattern from the previous notebook. A student may think this is just another way to do the same thing.  
**Smallest fix:** Add one sentence after this line: “This matters because approval now happens in the SDK, so the tool cannot run just because the agent ignored its instructions.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Part 2: Approve and Resume  
**Quote:** “    # Capture the resumable state”  
**What the student experiences:** The demo works, but the student still may not know the reusable pattern to copy into their own app. They see `result.to_state()` and `Runner.run(email_agent, state)`, but the notebook never explicitly names this as the standard pause-and-resume pattern they should reuse for any approval workflow.  
**Smallest fix:** In the “Why This Works” cell, add one sentence: “This is the reusable pattern for your own projects: run until interrupted, convert to `state`, approve or reject each interruption, then resume with that same state.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [unexplained term]  
**Location:** Part 4: Threshold-Based Escalation  
**Quote:** “A common pattern: auto-approve actions below a risk threshold, escalate to a human for high-risk ones, based on the tool's parameters.”  
**What the student experiences:** “Threshold” and “escalate” are understandable English words, but here they function like workflow design terms. A beginner to agent systems may not quite know whether the threshold is something the SDK provides, the model decides, or they must define in their own code.  
**Smallest fix:** Add one clarifying sentence: “The SDK does not choose this threshold for you — you write the rule in Python, such as ‘auto-approve refunds under $50.’”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Part 4: Threshold-Based Approval Logic  
**Quote:** “# In production, prompt a human via UI or messaging system”  
**What the student experiences:** The student sees the demo’s hardcoded `decision = "yes"` and understands it is simplified, but may not know what they would actually replace in a real app. Without one explicit mapping, this can feel like a demo-only trick rather than a pattern they can implement.  
**Smallest fix:** Add a short sentence in the markdown immediately above or below this demo: “In your own app, this is the one line you’d replace with a real approval source — for example, a button click in a UI, a Slack reply, or a CLI `input()` prompt.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

This notebook is otherwise in strong shape for a first-time agent student. The core flow is clear, the previous notebook sets up the motivation well, and the main comprehension gaps are small “name the pattern / explain the real-world reason” issues rather than larger teaching failures.

---

## Gemini Review

This notebook passed third-pass review with three minor findings. The core concepts are taught clearly with well-chosen examples that build from a simple approval flow to a more realistic threshold-based escalation. The connection to the previous lesson on tool safety is strong, and the practice exercises directly reinforce the new patterns. The findings below address small gaps where a student might briefly lose confidence or misunderstand an implementation detail.

---

### Finding 1

**Severity:** `[unexplained term]`
**Location:** Part 1: How HITL Works
**Quote:**
```
3. You inspect what the agent wants to do — call `state.approve(interruption)` or `state.reject(interruption)`.
```
**What the student experiences:** "What is `state`? Where does that variable come from? The explanation is telling me to use it, but I haven't seen how to create it yet."
**Smallest fix:** Change step 3 to explicitly show where the `state` object comes from:
"3. You convert the run result to a resumable state object with `result.to_state()`, then inspect what the agent wants to do — calling `state.approve(interruption)` or `state.reject(interruption)`."
**Blocks transfer:** Yes. A student reading the conceptual overview would not know the critical `result.to_state()` step required to implement the pattern.
**Outline-backed:** Yes. The outline requires teaching the full pause/approve/resume flow, and `result.to_state()` is a necessary part of that flow.

### Finding 2

**Severity:** `[confidence gap]`
**Location:** Part 4: Threshold-Based Escalation, `handle_refund_request` code cell
**Quote:**
```python
args = json.loads(interruption.raw_item.arguments)
```
**What the student experiences:** "Wait, why do I need `json.loads`? In Part 2, the arguments were printed and looked like a Python dictionary. I might have tried to access `arguments['amount']` directly and gotten an error. I'm not sure what data type `.arguments` is."
**Smallest fix:** Add a short inline comment explaining the data type.
```python
# The .arguments attribute is a JSON string, so we need to parse it
args = json.loads(interruption.raw_item.arguments)
```
**Blocks transfer:** Yes. A student attempting to access arguments directly without parsing would hit a `TypeError`, which could be a frustrating and confusing roadblock.
**Outline-backed:** No.

### Finding 3

**Severity:** `[lost why]`
**Location:** Part 4: Threshold-Based Escalation, `handle_refund_request` code cell
**Quote:**
```python
# Loop handles cases where resuming triggers additional approvals
while result.interruptions:
```
**What the student experiences:** "Why is this a `while` loop instead of an `if` statement like in the previous examples? The comment mentions 'additional approvals', but the demo only shows a single interruption. I don't understand when I would need a loop here."
**Smallest fix:** Expand the comment to give a concrete example of when the loop would be necessary.
```python
# Use a while loop because resuming the run could trigger another tool call
# that also needs approval. This ensures we handle all interruptions, not just the first.
while result.interruptions:
```
**Blocks transfer:** No. The student could copy the pattern and it would work, but they wouldn't understand the reasoning and might misapply it later by using an `if` in a situation that truly required a `while`.
**Outline-backed:** No.

---

## Grok Review

**Severity:** `[unexplained term]`

**Location:** Part 1 ("How HITL Works") + first code cell in Part 2

**Quote:** "2. SDK pauses — tool does NOT execute yet. `result.interruptions` contains the pending approvals. 3. You inspect what the agent wants to do — call `state.approve(interruption)` or `state.reject(interruption)`." (followed by code that does `state = result.to_state()`, `state.get_interruptions()`, `interruption.raw_item.name`, and `json.loads(interruption.raw_item.arguments)`)

**What the student experiences:** I understand the four-step mental model, but when I reach the actual code I suddenly see `to_state()`, `get_interruptions()`, `raw_item`, and the fact that `arguments` is a JSON string. None of these exact names or the shape of the `Interruption` object were introduced, so I feel like I'm missing a page of documentation even though I just read the explanation.

**Smallest fix:** After the numbered list in Part 1, add one sentence: "In practice you do `state = result.to_state()` then loop over `state.get_interruptions()`; each interruption has a `raw_item` with `.name` and `.arguments` (a JSON string)."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`

**Location:** Part 4 ("Threshold-Based Escalation") — the `handle_refund_request` helper

**Quote:** The entire 40-line `async def handle_refund_request(customer_message, auto_approve_threshold=50.0):` (especially the `while result.interruptions:` loop, `json.loads(interruption.raw_item.arguments)`, and hardcoded `decision = "yes"`)

**What the student experiences:** The demo runs nicely and the threshold idea makes sense, but the helper does so many things at once that I can't easily see the *minimal* approval pattern I would copy into my own project. After the two test cells I know it works for refunds, but I'm not confident I could adapt it for a different tool without basically copying most of this function.

**Smallest fix:** Immediately before the `handle_refund_request` definition, add a short markdown cell titled "### Minimal approval skeleton for your own projects" containing a 10–12 line commented code block that shows only the `while` + `to_state` + `approve`/`reject` pattern with a placeholder for the business logic.

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[pacing break]`

**Location:** Part 4 section header and the cell that defines both tools + agent + the long helper function

**Quote:** "## 🎚️ Part 4: Threshold-Based Escalation" followed immediately by tool definitions, agent creation, then the full `handle_refund_request` implementation in one cell.

**What the student experiences:** After the straightforward approve/reject examples, I suddenly have to absorb (1) a new risk-threshold concept, (2) JSON argument parsing, (3) a `while` loop that handles multiple interruptions, and (4) the state-resume pattern all at once before I even reach the test calls. I lose which piece I'm supposed to be focusing on.

**Smallest fix:** Split the current helper cell: keep the two `@function_tool` definitions + agent in the first cell, then put the `handle_refund_request` in its own cell with a one-sentence prose lead-in that says "We wrap the approval logic in a helper so we can demonstrate the threshold decision."

**Blocks transfer:** No (but it slows learning)

**Outline-backed:** Yes

---

**Severity:** `[lost why]`

**Location:** End of Part 3 (Rejection demo) right before the Part 4 header

**Quote:** (There is no "Why This Works" or equivalent cell after the rejection demo; the next prose is the Part 4 header.)

**What the student experiences:** I saw the agent gracefully switch to drafting after the all-company email was rejected, which was cool, but the notebook never explicitly tells me *why this matters* on a real project (e.g., that rejection feedback prevents the agent from getting stuck in a loop or repeatedly trying forbidden actions).

**Smallest fix:** After the rejection demo's final `print("=" * 60)`, add one sentence in a new markdown cell: "Rejection is useful because the agent receives context it can use to adapt instead of failing or retrying the same forbidden action."

**Blocks transfer:** No

**Outline-backed:** Yes (the outline lists the full approval workflow including rejection)

---

This notebook is otherwise very strong. The progression from simple approval to rejection to threshold logic matches the course outline well, the security note is appropriately placed, and the practice exercises reinforce exactly what a student at this point in the course needs. With the four small clarifications above a first-time reader would feel confident transferring the pattern to their own agents.

---

## DeepSeek Review

**Calibration note:** This notebook is well-structured and the core concepts are clearly demonstrated. The student will understand the approval flow, rejection, and threshold escalation. The findings below are specific transfer gaps that could cause a motivated student to stumble when writing their own HITL code.

---

**Severity:** `[confidence gap]`

**Location:** `#### Step 1: Run Until Paused` code cell — the two print statements

```python
print(f"🔍 Tool: {interruption.raw_item.name}")
print(f"    Args: {interruption.raw_item.arguments}")
```

**What the student experiences:** The student sees `arguments` printed as a string that looks like a Python dict. Later, in the threshold handler, `json.loads(interruption.raw_item.arguments)` is used without ever explaining *why* it needs parsing. If they try to write their own inspection code and write `args = interruption.raw_item.arguments["amount"]`, they'll get a `TypeError: string indices must be integers` — a confusing failure for a student who didn't know `arguments` is a JSON string.

**Smallest fix:** In the `#### Step 1: Run Until Paused` cell, add one sentence after the print statements:

```
# Note: arguments is a JSON string — use json.loads() to inspect specific fields
```

Or, if the print remains unchanged, add a brief prose sentence below the code cell: "`arguments` is a JSON string — you'll use `json.loads()` to parse it when you need to condition on individual parameters."

**Blocks transfer:** Yes — the student will fail when trying to implement their own parameter-based approval logic.

**Outline-backed:** No (not a specific teaching beat in the outline, but a necessary implementation detail the notebook teaches)

---

**Severity:** `[confidence gap]`

**Location:** `#### Step 2: Approve and Resume` code cell and `Threshold-Based Approval Logic` code cell

```python
if result.interruptions:
    state = result.to_state()

    for interruption in state.get_interruptions():
```

and later in the threshold handler:

```python
while result.interruptions:
    state = result.to_state()

    for interruption in state.get_interruptions():
```

**What the student experiences:** The student sees two ways to access pending approvals: `result.interruptions` (used in the `if` check / `while` condition) and `state.get_interruptions()` (used inside the loop). The notebook never explains the relationship between them. A student writing their own HITL code might try to iterate over `result.interruptions` after creating state, or might not know to call `state.get_interruptions()` at all. The fact that `state.get_interruptions()` returns the same pending list tied to the resumable state is left implicit.

**Smallest fix:** In the `#### Step 2: Approve and Resume` cell, replace the plain `# Capture the resumable state` comment with:

```python
# Capture the resumable state — use state.get_interruptions()
# to access pending approvals (same list as result.interruptions)
state = result.to_state()

for interruption in state.get_interruptions():
```

This directly tells the student why `state.get_interruptions()` is the correct method to use once they have a state object.

**Blocks transfer:** Yes — the student will be uncertain which object to use for iterating interruptions.

**Outline-backed:** No

---

**Severity:** `[confidence gap]`

**Location:** Code cells in `#### Step 1: Run Until Paused` and `#### Step 2: Approve and Resume`

```python
print(f"🔍 Tool: {interruption.raw_item.name}")
...
state.approve(interruption)
```

**What the student experiences:** The student accesses tool information via `interruption.raw_item.name` and `interruption.raw_item.arguments`, but has no explanation of what `raw_item` is or why there's a wrapper. They might expect `interruption.name` to work. The `raw_item` attribute name is SDK-specific and not intuitive. A student trying to write similar code will either misspell `raw_item` (no autocomplete in a notebook) or wonder if there's a more direct accessor.

**Smallest fix:** In the `#### Step 1: Run Until Paused` cell, after the interruption print loop, add a prose note:

```
The `raw_item` contains the original tool call details — use `.name` for the tool name and `.arguments` for the parameters (as a JSON string).
```

Alternatively, add a one-line comment next to the print statements in the code cell.

**Blocks transfer:** Yes — the student won't confidently know how to access tool metadata.

**Outline-backed:** No

---

**Summary:** The notebook is otherwise clear and well-paced. The three findings all relate to missing implementation notes that would prevent a student from independently replicating the HITL pattern on their own project. Each fix is a sentence or a comment line — no structural changes needed.
