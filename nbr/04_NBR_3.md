# Review Report: Building Tools for Agents
Generated: 2026-05-10 | Notebook: /Users/scott/Dropbox/Notebooks/openai-agents/Week_1_Foundations/04_Building_Tools_For_Agents.ipynb
Models: Anthropic (claude-opus-4-7 (CLI/Max)) · OpenAI (gpt-5.4) · Gemini (gemini-2.5-pro) · Grok (grok-4.20-0309-reasoning) · DeepSeek (deepseek-reasoner) · Dedup (claude-opus-4-7 CLI/Max)

---

# Grouped Findings: Building Tools for Agents

# Third-Pass Synthesis — Notebook 04: Building Tools for Agents

The notebook passed third-pass review in good shape. Reviewers converged tightly on a small number of comprehension gaps, almost all clustered around two locations: the `@function_tool` explanation cell in Part 1, and the docstring-design guidance in Part 3. No reviewer flagged structural or pacing problems severe enough to require restructuring.

---

## Finding 1 — "Schema" used without definition

**Severity:** `[unexplained term]`

**Location:** Part 1, markdown cell after `get_employee` definition (also appears in Topics list and Key Takeaways)

**Reviewers:** 5/5 — Anthropic, OpenAI, Gemini, Grok, DeepSeek

**Quote:** "Type hints — define the expected inputs; the parameter types generate the schema automatically. No manual JSON required."

**What the student experiences:** "Schema" appears three times in the notebook without ever being defined. The student new to agent tooling can't picture what's being generated or why "no manual JSON" matters as the alternative — they infer the concept loosely but can't connect it to anything concrete.

**Smallest fix:** Add a parenthetical gloss in the bullet: "the parameter types generate the schema (the structured description the model reads to know what arguments the tool expects) automatically. No manual JSON required."

**Blocks transfer:** Split — Anthropic, OpenAI, Gemini, DeepSeek say No (students can write working tools without grasping the term); Grok says Yes (the conceptual piece is what makes `@function_tool` feel special). Lean No: code works either way, but the gloss is one sentence and worth adding.

**Outline-backed:** Yes — "Automatic schema generation — no manual JSON" is an outline topic.

---

## Finding 2 — No concrete guidance for writing good tool docstrings

**Severity:** `[confidence gap]`

**Location:** Part 3, "How the Agent Decides" cell and after the three test runs

**Reviewers:** 3/5 — Anthropic, Grok, OpenAI (OpenAI's version pivots to "give each tool a narrow job" rather than docstring contents specifically, but it's the same student gap)

**Quote:** "A clear, specific docstring helps the agent pick the right tool. A vague one leads to missed calls or wrong choices."

**What the student experiences:** The principle is named but the student is never told *what makes a docstring clear and specific*. They've seen good examples but have no checklist. The Topics list promises "Writing good tool descriptions — naming, inputs, outputs," and a student finishing the notebook would still hesitate when writing their first non-trivial tool.

**Smallest fix:** Add a 3-bullet sub-cell at the end of Part 3 listing what a good tool docstring states: (1) *what* the tool returns, (2) *when* to use it (the trigger condition), (3) *what each input means* if not obvious from the parameter name. Optionally append a one-line contrast: "'Get employee info' is too vague; the current docstring naming the returned fields works much better."

**Blocks transfer:** Yes (3/3 reviewers agree)

**Outline-backed:** Yes — outline lists "Writing good tool descriptions — naming, inputs, outputs" as a dedicated topic.

---

## Finding 3 — "Why this matters" for real projects is left implicit

**Severity:** `[lost why]`

**Location:** Part 1, after the `@function_tool` explanation / first "After" demo

**Reviewers:** 2/5 — OpenAI, Grok

**Quote:** "The `@function_tool` decorator turns any Python function into an agent tool…" / `print(result.final_output)` after the employee lookup demo

**What the student experiences:** The toy employee lookup runs, but the student doesn't see the bridge to real projects. They understand "tools add capabilities" abstractly without recognizing this is the pattern for connecting agents to internal databases, APIs, and business systems they already have.

**Smallest fix:** Insert one sentence after the bullet list (or below the demo): "This is the core reason tools matter in real projects — the same pattern lets an agent answer questions from your live business data, internal APIs, or services instead of guessing from model knowledge."

**Blocks transfer:** Split — OpenAI says No, Grok says Yes. Lean No: students can build the toy tool without this framing, but the sentence is cheap insurance against the lesson feeling like a toy exercise.

**Outline-backed:** Yes

---

## Finding 4 — No way to verify the tool was actually called

**Severity:** `[confidence gap]`

**Location:** Part 1, after the first "After" demo (and recurring in Part 3)

**Reviewers:** 1/5 — Anthropic

**Quote:** `result = await Runner.run(agent_with_tools, input=message)` / `print(result.final_output)`

**What the student experiences:** Output looks correct but the student has no direct evidence the agent *called* the tool versus just guessing from model knowledge. The mechanism (agent decides → tool fires → result returned) stays abstract, and they won't know how to debug their own tools when output looks wrong.

**Smallest fix:** Add one sentence after the first "After" demo pointing back to L03's tracing: "Open the traces dashboard to confirm the agent selected `get_employee`, called it with `'E003'`, and used the result."

**Blocks transfer:** Yes (per the one reviewer who flagged it)

**Outline-backed:** Yes — outline calls for "How the agent decides which tool to call."

---

## Findings considered and not included

- **Gemini Finding 2 (return type beyond `str`)** and **Gemini Finding 3 (conversational input test)** — both are useful curiosity-extensions but the fixes require *adding new content* (a "Why This Works" cell, a new test demo). Per the guidance, third-pass fixes must be minimal; these are nice-to-haves that don't represent comprehension blockers.
- **OpenAI Finding 6 (lost thread across notebook)** — only one reviewer flagged it, and the fix is a single orienting sentence at the start of Part 3. Worth considering as a low-cost addition but the other four reviewers found the through-line clear, suggesting the issue isn't broadly felt.
- **Grok Finding 4 (merge three test cells into a loop)** — flagged as `[pacing break]` by one reviewer, but four others found Part 3 paced well, and merging the cells would lose the narrative beats ("Test: Employee Question" / "Time Question" / "No Tool Needed") that make the decision logic visible. This is style preference, not a comprehension blocker.
- **OpenAI Finding 2 (what kinds of functions make good tools)** — useful but partially overlaps with Finding 3 above; the "real databases, APIs, internal services" sentence covers the same ground.

---

## Summary

Four actionable findings. All fixes are single-sentence or short-bullet additions. The dominant theme is one location — the `@function_tool` explanation cell in Part 1 — where two of the four findings (the "schema" gloss and the "why this matters" sentence) can be addressed in a single small edit. The Part 3 docstring-design bullets are the only finding that adds slightly more than a sentence, but it's a 3-bullet sub-cell, not a section.

---

# Individual Reviews

The grouped findings above are your primary working surface. The full individual reviews below are for verifying nuance.

---

## Anthropic Review

# Third-Pass Review — Notebook 04: Building Tools for Agents

## Finding 1

**Severity:** `[confidence gap]`

**Location:** Part 1 "After: Agent with an employee lookup tool" (and recurring through Part 3)

**Quote:** `result = await Runner.run(agent_with_tools, input=message)\n\nprint(result.final_output)`

**What the student experiences:** The output appears correct, but the student has no direct evidence the agent actually *called* the tool. They might wonder: "Did it really use my function, or did the model just guess? How would I check this on a real project?" Without that confirmation, the lesson's central mechanism — agent decides → tool fires → result returned — stays abstract.

**Smallest fix:** After the first "After" demo, add one sentence pointing back to L03's tracing: "Open the traces dashboard ([platform.openai.com/traces](https://platform.openai.com/traces)) to see the agent select `get_employee`, call it with `'E003'`, and use the result." A single line connecting today's demo to L03's tracing closes the loop.

**Blocks transfer:** Yes — without confirming tool invocation, students can't debug their own tools when something looks wrong.

**Outline-backed:** Yes — outline explicitly says "How the agent decides which tool to call."

---

## Finding 2

**Severity:** `[unexplained term]`

**Location:** Part 1 explanation cell, repeated in Key Takeaways and Topics

**Quote:** "Type hints — define the expected inputs; the parameter types generate the schema automatically"

**What the student experiences:** "Schema" is used three times in the notebook (Topics, Part 1 explanation, Key Takeaways) without ever being defined. A Python-comfortable student new to LLM tooling has no reason to know what schema this refers to or why "no manual JSON" is the alternative. They infer the concept loosely but can't picture what's happening under the hood.

**Smallest fix:** In the Part 1 explanation cell, expand one clause: "Type hints — the parameter types generate the JSON description the model needs to call your function correctly. No manual JSON required."

**Blocks transfer:** No — students can still write working tools without grasping the term.

**Outline-backed:** Yes — "Automatic schema generation — no manual JSON" is in the outline topics.

---

## Finding 3

**Severity:** `[confidence gap]`

**Location:** Part 3 "How the Agent Decides"

**Quote:** "A clear, specific docstring helps the agent pick the right tool. A vague one leads to missed calls or wrong choices."

**What the student experiences:** The principle is named, but the student is never told *what makes a docstring clear and specific*. They've seen good examples but have no checklist to apply to their own tools. The Topics list promises "Writing good tool descriptions — naming, inputs, outputs," and a student finishing the notebook would still hesitate when writing their first non-trivial tool: "Is my docstring good enough? What should it actually contain?"

**Smallest fix:** Add a 3-bullet sub-cell at the end of Part 3 listing what a good tool docstring states: (1) *what* the tool returns, (2) *when* to use it (the trigger condition), (3) *what each input means* if it's not obvious from the parameter name.

**Blocks transfer:** Yes — students will write their own tools immediately and need explicit guidance to write docstrings the agent can act on.

**Outline-backed:** Yes — outline lists "Writing good tool descriptions — naming, inputs, outputs" as a dedicated topic.

---

Three findings total. The notebook is in good shape — the Before/After framing in Part 1 lands cleanly, Part 2 is appropriately brief, and Part 3 sets up the right principle. The fixes above are all single-sentence or short-bullet additions, no structural changes needed.

---

## OpenAI Review

**Severity:** [lost why]  
**Location:** Part 1, after “#### Run Agent with Employee Lookup Tool”  
**Quote:** `print(result.final_output)`  
**What the student experiences:** The student sees that the agent can now answer a database-style question, but there’s no explicit bridge from this toy employee lookup to real projects. They may understand “tools add capabilities” in the abstract without recognizing that this is the pattern for connecting agents to internal business data or APIs they already have.  
**Smallest fix:** Add one sentence right below this demo: “This is the core reason tools matter in real projects: they let an agent answer questions from your live business data instead of guessing from model knowledge.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Part 1, explanation cell after the `get_employee` tool  
**Quote:** `The \`@function_tool\` decorator turns any Python function into an agent tool:`  
**What the student experiences:** The student learns what pieces the decorator uses, but not what to do when writing their own tool tomorrow. They can copy this exact example, but they still don’t have a simple rule for “what kinds of Python functions make good tools.”  
**Smallest fix:** Add one sentence after “No manual JSON required.”: “In your own projects, a good first tool is usually a small function you already trust — something like a database lookup, calculator, API wrapper, or file reader.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [unexplained term]  
**Location:** Part 1, explanation cell after the `get_employee` tool  
**Quote:** `Type hints — define the expected inputs; the parameter types generate the schema automatically`  
**What the student experiences:** “Type hints” is familiar Python, but “schema” lands as a new AI/tooling term that the notebook treats as self-evident. A student new to agents may not know whether “schema” means validation rules, JSON, argument definitions, or something else.  
**Smallest fix:** Add a short gloss in the same bullet: “Type hints — define the expected inputs; the parameter types generate the schema automatically, meaning the agent gets a structured description of what arguments the tool expects.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** Part 3 header and intro  
**Quote:** `When an agent has tools, it doesn't always use them. It decides based on the user's message and the tool's name and docstring — which means **your docstring is part of your prompt**.`  
**What the student experiences:** The student understands that docstrings affect tool choice, but not why this deserves real attention. Without a concrete “so what,” docstrings can still feel like a nice-to-have comment rather than something that directly changes agent behavior.  
**Smallest fix:** Add one sentence at the end of the paragraph: “In practice, this means a weak docstring can make a perfectly good Python function behave like a bad tool because the agent won’t know when to call it.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Part 3, after the three test runs  
**Quote:** `#### Test: No Tool Needed`  
**What the student experiences:** The student watches the agent choose between two tools and sometimes use none, but the lesson never names the practical rule they should carry forward. They may not know how to design their own toolset so the agent can make clean decisions instead of overlapping, confusing ones.  
**Smallest fix:** Add a brief markdown cell after the three tests: “A useful design rule: give each tool a narrow, obvious job. When tools overlap too much, the agent has a harder time choosing reliably.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [lost thread]  
**Location:** Entire notebook opening through Part 3  
**Quote:** `## 🧠 Part 3: How the Agent Decides`  
**What the student experiences:** The notebook teaches three separate ideas — tools add capabilities, tools can take different kinds of inputs, and docstrings influence tool selection — but it never reanchors the bigger lesson that students are building toward a small assistant they can extend. By the time they reach Part 3, the demos feel isolated rather than like pieces of one reusable pattern.  
**Smallest fix:** Add one sentence at the start of Part 3: “You’ve now seen the three building blocks of a useful assistant: define a function, decorate it as a tool, then give the agent enough tool metadata to choose it correctly.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

## Gemini Review

This notebook passed third-pass review with three findings. The overall structure is excellent, clearly demonstrating the core value of tools and the mechanics of the `@function_tool` decorator. The progression from a single tool to a multi-tool agent that makes decisions is very effective. The recommended fixes address small but important gaps that a new student would likely encounter when trying to apply these patterns to their own projects.

---

### Finding 1

**Severity:** `[lost why]`
**Location:** Topics list, and the markdown cell explaining `@function_tool` in Part 1.
**Quote:** "Automatic schema generation — no manual JSON"
**What the student experiences:** "I see that this decorator is easy to use, but I don't understand what a 'schema' is in this context or what 'manual JSON' I'm being saved from. I can't appreciate why this is an important feature."
**Smallest fix:** In the markdown cell after the `get_employee` tool definition, add a sentence explaining what the decorator abstracts away. For example: "The SDK uses these pieces to generate a detailed JSON description (a 'schema') of the tool for the model. Without the decorator, you would have to write this schema by hand."
**Blocks transfer:** No
**Outline-backed:** Yes

---

### Finding 2

**Severity:** `[confidence gap]`
**Location:** Part 1, after the successful run of the agent with the `get_employee` tool.
**Quote:** `def get_employee(employee_id: str) -> str:`
**What the student experiences:** "All the tools in this lesson are type-hinted to return a string. Can my tools return other Python types like a dictionary or a list, or does it have to be a string for the agent to understand the output?"
**Smallest fix:** Add a short `### 💡 Why This Works` cell after the demo run. "The tool returns a formatted string, which the agent can easily read and summarize in its final answer. Your tools can also return structured data like dictionaries or lists; the SDK will serialize them to JSON for the agent to use."
**Blocks transfer:** Yes
**Outline-backed:** No

---

### Finding 3

**Severity:** `[confidence gap]`
**Location:** Part 1, the `get_employee` tool demo.
**Quote:** `message = "What department does employee E003 work in?"`
**What the student experiences:** "The demo works because the input 'E003' is a perfect match for what the tool's `employee_id` parameter expects. What happens if I ask about 'employee number two' or just 'Priya'? Does the agent figure it out, or does my tool need to handle fuzzy inputs?"
**Smallest fix:** Add a new markdown cell `#### Test: Conversational Input` and a new code cell after the first successful tool demo. In the code cell, run the agent with a slightly more natural query like `await Runner.run(agent_with_tools, input="what's the deal with employee e002?")`. This demonstrates that the model handles parameter extraction from natural language.
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Grok Review

**Severity:** `[unexplained term]`

**Location:** Opening **Topics:** list and the markdown cell immediately after the `get_employee` definition

**Quote:** "Automatic schema generation — no manual JSON" and "Type hints define the expected inputs; the parameter types generate the schema automatically. No manual JSON required."

**What the student experiences:** I've just finished Lesson 03 and understand `Agent` + `Runner`, but "schema" appears with no definition. I don't know if it's something the model reads, something Python builds, or why skipping manual JSON is a big deal. The code works, but I feel like I'm missing the conceptual piece that makes `@function_tool` special.

**Smallest fix:** Change the second sentence to: "Type hints define the expected inputs; the parameter types generate the schema (the structured description the model reads) automatically. No manual JSON required."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`

**Location:** Part 3 markdown cell ("## 🧠 Part 3: How the Agent Decides")

**Quote:** "When an agent has tools, it doesn't always use them. It decides based on the user's message and the tool's name and docstring — which means **your docstring is part of your prompt**. A clear, specific docstring helps the agent pick the right tool. A vague one leads to missed calls or wrong choices."

**What the student experiences:** The three test runs show the agent choosing correctly, but I have no concrete sense of what "good" vs "bad" looks like in practice. After finishing the notebook I wouldn't know how to write descriptions for my own tools so the agent reliably picks the right one (or knows when to skip).

**Smallest fix:** Add this one sentence at the end: "Example: 'Get employee info' is too vague; the current docstring that names the returned fields and implies its use for HR lookups works much better."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[lost why]`

**Location:** End of Part 1 explanation cell (right before "#### Run Agent with Employee Lookup Tool")

**Quote:** "The `@function_tool` decorator turns any Python function into an agent tool: - **Name** — tells the agent what the tool is called - **Docstring** — tells the agent what the tool does and when to use it - **Type hints** — define the expected inputs; the parameter types generate the schema automatically No manual JSON required."

**What the student experiences:** I see how the decorator works on this toy employee lookup, but I don't yet understand why this pattern would matter on a real project (e.g. connecting to an actual database, API, or internal system). The "so what" feels abstract even though the code runs.

**Smallest fix:** Insert one sentence after the bullet list: "This is powerful because the same pattern lets an agent safely call your real databases, APIs, or internal services."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[pacing break]`

**Location:** Part 3 ("## 🧠 Part 3: How the Agent Decides") — the sequence of three nearly identical test cells

**Quote:** "#### Test: Employee Question" / "#### Test: Time Question" / "#### Test: No Tool Needed"

**What the student experiences:** By the third test I'm holding the main idea ("the agent decides based on message + docstring") while also tracking three almost-identical `Runner.run()` blocks. The core teaching point gets diluted; I can't clearly say which concept I was supposed to focus on.

**Smallest fix:** Merge the three tests into one cell with three short `input=` strings and a single `for` loop that prints each result, then keep the existing explanatory markdown.

**Blocks transfer:** No

**Outline-backed:** No

---

This notebook is otherwise very clear for a first-time agent learner. The progression from "no tools" → single tool → parameterless tool → multi-tool decision making is easy to follow, the exercises reinforce the exact skills just taught, and the "why tools matter" framing in Part 1 lands well. With the four small additions above a student would finish feeling they could confidently add their own tools to a project.

---

## DeepSeek Review

Notebook 04 (Building Tools for Agents) is well-structured and focused. The student will find the before/after contrast in Part 1 effective, the examples are clear, and the pacing is appropriate for a first look at tools.

I have one finding. The notebook is otherwise clean: no unexplained terms from the previous notebook are missing, no lost thread, no pacing breaks, and the exercises map directly to the demos.

---

**Severity:** `[unexplained term]`

**Location:** Part 1, markdown cell immediately after the `get_employee` tool definition — the paragraph explaining `@function_tool`.

**Quote:**
> Type hints — define the expected inputs; the parameter types generate the schema automatically — no manual JSON required.

**What the student experiences:**
The student has just learned about `@function_tool` and sees the word "schema". They are a novice to AI agent internals and have not been told what a "schema" is in this context. They may infer it means "some kind of structure" but the term is used without definition, creating a small moment of uncertainty: *"Schema of what? The tool itself? The parameters?"*

**Smallest fix:**
Replace "schema" with a more concrete term, or add a brief parenthetical. The current sentence is close; the minimal change is:

```
the parameter types generate the tool's parameter description automatically — no manual JSON required.
```

Alternatively, keep "schema" but add a clarification:

```
the parameter types generate the parameter schema (a structured description the model uses) automatically — no manual JSON required.
```

**Blocks transfer:** No — the student can use the pattern without understanding the term "schema".

**Outline-backed:** Yes — the Course Outline lists "Automatic schema generation — no manual JSON" as a topic. The notebook should teach what a schema is in this context.
