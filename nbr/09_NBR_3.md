# Review Report: Testing & Evaluating Agents
Generated: 2026-05-10 | Notebook: /Users/scott/Dropbox/Notebooks/openai-agents/Week_2_Reliability_And_Built_In_Tools/09_Testing_And_Evaluating_Agents.ipynb
Models: Anthropic (claude-opus-4-7 (CLI/Max)) · OpenAI (gpt-5.4) · Gemini (gemini-2.5-pro) · Grok (grok-4.20-0309-reasoning) · DeepSeek (deepseek-reasoner) · Dedup (claude-opus-4-7 CLI/Max)

---

# Grouped Findings: Testing & Evaluating Agents

# Third-Pass Synthesis — Notebook 09: Testing & Evaluating Agents

**Scope note:** Several reviewers flagged structural cell-corruption issues (missing `check_output()` definition, duplicated cells stored as markdown-in-code) that are second-pass concerns. Where these directly create comprehension blockers (e.g., a missing helper that prevents the student from seeing the core pattern), they're included below. Pure formatting issues (cell type wrong) are filtered out.

---

## Finding 1 — Missing `check_output()` helper hides the core pass/fail pattern

**Severity:** `[confidence gap]`
**Location:** Part 2 — "Run the Full Test Suite" cell (cascades into Part 3 rubric eval and Part 4 comparison)
**Reviewers:** 4 of 5 (OpenAI, Gemini, Grok, DeepSeek). Anthropic flagged the same issue but classified it as out-of-scope second-pass.
**Quote:** `passed, failures = check_output(output, test_case["must_contain"], test_case["must_not_contain"])`
**What the student experiences:** They hit `NameError: name 'check_output' is not defined`. Even if they patch it themselves, they never see the canonical implementation of the pass/fail pattern this notebook is meant to teach. They can't transfer the pattern to their own project without guessing.
**Smallest fix:** Insert a code cell before "Run the Full Test Suite" defining `check_output(output, must_contain, must_not_contain)` with simple substring logic and a one-line docstring. Gemini, Grok, and DeepSeek all proposed near-identical implementations.
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Finding 2 — Why the judge agent needs ground truth is invisible

**Severity:** `[confidence gap]` / `[lost why]`
**Location:** Part 3 — judge_input construction in the rubric evaluation loop
**Reviewers:** 4 of 5 (Anthropic, OpenAI, Grok, Gemini — each from a slightly different angle)
**Quote:** `judge_input = (f"Product catalog (ground truth):\n{catalog}\n\n" ...)`
**What the student experiences:** They see a complex catalog string assembled and pasted into the judge prompt with no stated principle. The transferable insight — *the judge has no built-in knowledge of your data; pass ground truth in so it can verify instead of guess* — is only visible in code. Students writing a judge for their own domain will skip this and wonder why the judge happily approves wrong answers. OpenAI separately notes "ground truth" is used as eval vocabulary without definition. Gemini adds a scale concern: students will assume this won't work past a tiny catalog.
**Smallest fix:** One sentence above the loop: "We give the judge the full catalog as ground truth so it can verify facts itself instead of trusting the agent — for larger datasets, pass only the relevant slice (e.g., the entry for the product being asked about)." Optionally gloss "ground truth" inline the first time.
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Finding 3 — Decision rule for pass rate vs. avg score is missing

**Severity:** `[lost why]`
**Location:** Part 4 — final summary / winner declaration
**Reviewers:** 3 of 5 (Anthropic, Gemini, with related framing from OpenAI)
**Quote:** `print(f"👑 Winner (by judge score): {winner}")`
**What the student experiences:** Two metrics are computed and printed, but `winner` is decided by judge score alone. The student silently asks: "Why did we calculate pass rate if it doesn't affect the decision? What if a version has a higher quality score but breaks more facts?" Without guidance they'll default to whichever number looks better.
**Smallest fix:** One line under the summary: "Pass rate is a gate — it catches factual breakage. Avg score catches quality drift. Only compare quality scores if pass rate is stable or improved; don't ship a version that loses on one metric to win on the other."
**Blocks transfer:** Anthropic says No; Gemini says Yes. Treat as Yes — without this, the lesson's main decision-making payoff is muddled.
**Outline-backed:** Yes

---

## Finding 4 — agent_v2's hypothesis isn't stated before the comparison

**Severity:** `[lost why]`
**Location:** Part 4 — agent_v2 definition
**Reviewers:** 1 of 5 (Anthropic) — but unique angle, not redundant with Finding 3
**Quote:** `# Version B — improved instructions`
**What the student experiences:** They see two instruction sets and a comparison harness but no statement of *what* v2 is trying to improve. The result reads as "v2 won" or "v1 won" without insight. The transferable lesson — state a hypothesis before measuring — is missing.
**Smallest fix:** One line above v2_instructions: "v2 trades verbosity for structure: lead with the answer, suggest alternatives proactively, cap at 3 sentences. The test set will tell us whether the tighter format costs us factual accuracy."
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Finding 5 — Test-set coverage strategy isn't named

**Severity:** `[confidence gap]`
**Location:** Part 1 — Building a Golden Test Set, intro cell
**Reviewers:** 1 of 5 (Anthropic)
**Quote:** "A golden test set is a collection of inputs where you know what the correct output should look like."
**What the student experiences:** They learn the *format* of a test case but not the *coverage strategy*. Looking at the 5 examples, the categories (price lookup, missing product, comparative, etc.) are visible but unnamed. A student starting their own test set will default to 10 happy-path cases and miss the failure-mode coverage that makes evaluation valuable.
**Smallest fix:** One sentence in the intro: "A useful test set covers three things: happy paths, edge cases (missing items, ambiguous inputs), and known failure modes you've already seen the agent get wrong."
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Finding 6 — Test set is described as "save and version-control" but no code shows how

**Severity:** `[confidence gap]`
**Location:** End of Part 1 — paragraph after the TEST_CASES cell
**Reviewers:** 1 of 5 (Grok)
**Quote:** "Test sets should be saved to a file and version-controlled alongside your agent code…"
**What the student experiences:** The notebook tells them this matters but never shows the code. After finishing they still wouldn't know what to type in their own project to make a persistent golden test set.
**Smallest fix:** A 4-line snippet right after that sentence: `import json` + `json.dump(TEST_CASES, open("test_cases.json", "w"), indent=2)` and a matching load line.
**Blocks transfer:** Yes
**Outline-backed:** Yes ("saving and versioning over time")

---

## Finding 7 — Part 4 cell does too many things at once

**Severity:** `[pacing break]`
**Location:** Part 4 — Head-to-Head Comparison cell
**Reviewers:** 1 of 5 (Grok)
**Quote:** `## 🔄 Part 4: Comparing Two Agent Versions`
**What the student experiences:** A single cell runs both agents, does pass/fail checks, rebuilds the catalog, builds judge prompts, calls the judge twice per case, tracks four lists, computes averages, and picks a winner. Too many ideas at once — the student can't tell which part is the core "compare two versions" pattern they should take away.
**Smallest fix:** Split into two cells: "Step 1: Get responses from both agents" (run + print outputs) and "Step 2: Judge and compare" (judging, scoring, winner). Keep all logic identical.
**Blocks transfer:** No (they can still run it), but weakens what the student remembers.
**Outline-backed:** Yes

---

## Finding 8 — Part 3 needs a one-line orienting frame

**Severity:** `[pacing break]`
**Location:** Part 3 — Rubric-Based Evaluation intro
**Reviewers:** 1 of 5 (OpenAI)
**Quote:** "Pass/fail checks can't measure quality — helpfulness, clarity, tone. For that, use a judge agent…"
**What the student experiences:** Several new ideas land at once — judge agent, scoring rubric, structured output, ground truth. The student can follow the code but can't tell which is the core takeaway vs. supporting machinery.
**Smallest fix:** One orienting sentence before the code: "The core pattern is simple: generate the agent's answer first, then send that answer plus the ground truth into a second agent whose only job is scoring."
**Blocks transfer:** No
**Outline-backed:** Yes

---

## Findings considered and dropped

- **Stray Part 2 markdown-in-code cell** (OpenAI, DeepSeek) — pure structural corruption, second-pass concern.
- **OpenAI's "real use case" framing for Part 4** — overlaps with Finding 4; the hypothesis fix already addresses the "when would I use this" gap more concretely.
- **OpenAI's bridge-from-must_contain-to-pass/fail** — collapses into Finding 1; once `check_output` is visible, the bridge is visible.

---

## Summary

Eight findings, all single-sentence or single-snippet fixes. Findings 1–4 are the load-bearing ones — they affect whether the student leaves with the four transferable patterns the notebook is built around (pass/fail mechanics, judge-needs-ground-truth, hypothesis-before-comparison, multi-metric decision rule). Findings 5–8 sharpen the lesson but don't gate transfer the same way. Once the structural issues (`check_output` missing, duplicated cells) are cleaned up in second-pass, applying these eight fixes lands the lesson cleanly.

---

# Individual Reviews

The grouped findings above are your primary working surface. The full individual reviews below are for verifying nuance.

---

## Anthropic Review

# Third-Pass Review — Notebook 09: Testing & Evaluating Agents

**Scope note:** Before the comprehension findings — there are serious structural problems that appear to be cell-corruption from a prior edit pass. They're second-pass concerns (out of scope for this review), but the notebook will not run as-is and the student-facing flow assumes they're fixed:

- `check_output()` is called repeatedly but never defined in any code cell
- Part 2's intro markdown is duplicated as a code cell with markdown content
- Part 3 has a duplicate "rubric evaluation" cell stored as markdown
- Part 4 has a duplicate "comparison" cell stored as markdown

Findings below assume the corrupted cells are removed and `check_output()` is restored.

---

## Findings

**Severity:** `[confidence gap]`
**Location:** Part 3 — Run Rubric Evaluation, judge_input construction
**Quote:** `judge_input = (f"Product catalog (ground truth):\n{catalog}\n\n"`
**What the student experiences:** They see a complex `catalog` string being assembled and pasted into the judge prompt with no explanation of why. The pattern — *the judge needs ground truth to evaluate factual accuracy* — is the most transferable principle in the lesson, and it's only visible in code. A student writing their own judge agent for a different domain might skip this and wonder why their judge happily approves wrong answers.
**Smallest fix:** Add one sentence to the Part 3 intro cell: "The judge agent has no built-in knowledge of your data — pass the ground truth into the prompt so it can verify factual accuracy instead of guessing."
**Blocks transfer:** Yes
**Outline-backed:** Yes (outline calls for "Rubric-based evaluation with a judge agent")

---

**Severity:** `[lost why]`
**Location:** Part 4 — agent_v2 definition
**Quote:** `# Version B — improved instructions`
**What the student experiences:** They see two sets of instructions and a comparison harness, but no statement of *what* v2 is trying to improve or *why* this comparison is interesting. The result reads as "v2 won" or "v1 won" without insight. The transferable lesson — that you state a hypothesis before measuring — is missing.
**Smallest fix:** Add one line above v2_instructions: "v2 trades verbosity for structure: lead with the answer, suggest alternatives proactively, cap at 3 sentences. The test set will tell us whether the tighter format costs us factual accuracy."
**Blocks transfer:** Yes
**Outline-backed:** Yes (outline calls for "Comparing two agent or prompt versions")

---

**Severity:** `[confidence gap]`
**Location:** Part 1 — Building a Golden Test Set, intro cell
**Quote:** `"A golden test set is a collection of inputs where you know what the correct output should look like. Each test case has a question and one or more criteria the answer must satisfy."`
**What the student experiences:** They learn the *format* of a test case but not the *coverage strategy*. Looking at the 5 examples, they can see "price lookup, stock, listing, missing product, comparative" — but no one names the categories. A student starting their own test set on their own agent would default to writing 10 happy-path tests and miss the failure-mode coverage that makes evaluation valuable.
**Smallest fix:** Add one sentence to the intro: "A useful test set covers three things: happy paths, edge cases (missing items, ambiguous inputs), and known failure modes you've already seen the agent get wrong."
**Blocks transfer:** Yes
**Outline-backed:** Yes (outline calls for "Building a golden test set — saving and versioning over time")

---

**Severity:** `[lost why]`
**Location:** Part 4 — final summary
**Quote:** `print(f"📊 Pass rate  — v1: {v1_pass_rate:.0%}  |  v2: {v2_pass_rate:.0%}")\nprint(f"📊 Avg score  — v1: {avg_v1:.1f}/5  |  v2: {avg_v2:.1f}/5")`
**What the student experiences:** Two metrics are reported and then `winner` is decided purely by judge score. The student silently wonders: "What if pass rate dropped but avg score went up — is that still a win?" Without guidance, they'll default to whichever number looks better.
**Smallest fix:** Add one line under the summary: "Pass rate catches factual breakage; avg score catches quality drift. A drop in either is a regression — don't ship a version that loses on one metric to win on the other."
**Blocks transfer:** No (they could still run comparisons), but it weakens the lesson's main decision-making payoff.
**Outline-backed:** Yes (outline calls for "regression checks after model swaps, prompt changes, or schema updates")

---

## Summary

Four findings, all fixable with a single sentence each. The notebook's structure is sound; what's missing are the named principles behind the patterns the code already demonstrates. Once the structural bugs (missing `check_output`, duplicated cells) are addressed in a second-pass cleanup, these four additions would make the lesson transfer cleanly to a student's own agent.

---

## OpenAI Review

**Severity:** [confidence gap]  
**Location:** Part 1 → before “#### Run the Full Test Suite”  
**Quote:** “A golden test set is a collection of inputs where you know what the correct output should look like. Each test case has a question and one or more criteria the answer must satisfy.”  
**What the student experiences:** The student understands what a golden test set is in theory, but still doesn’t know how the notebook turns `must_contain` / `must_not_contain` into an actual pass/fail result. When the test runner appears, it feels like a missing bridge between the data structure and the evaluation pattern they’re supposed to reuse.  
**Smallest fix:** Add one sentence right after this paragraph: “In this notebook, each test case uses simple string checks: the answer passes if it includes at least one required phrase and none of the forbidden ones.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Part 2: Pass/Fail Checks  
**Quote:** `passed, failures = check_output(`  
**What the student experiences:** The notebook starts using the core evaluation helper without ever showing or naming the pattern that makes pass/fail evaluation work. A Python-comfortable student can read the call, but they can’t reproduce the lesson independently because the key helper is invisible.  
**Smallest fix:** Insert a short code cell before “#### Run the Full Test Suite” defining `check_output(output, must_contain, must_not_contain)` with a one-line docstring and simple substring logic, plus a ready print.  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [pacing break]  
**Location:** Part 3: Rubric-Based Evaluation with a Judge Agent  
**Quote:** “Pass/fail checks can't measure quality — helpfulness, clarity, tone. For that, use a judge agent: an LLM that scores the output against a rubric.”  
**What the student experiences:** This section introduces several new ideas at once: a judge agent, a scoring rubric, structured output for eval results, and supplying ground truth to the judge. The student can probably follow the code, but may not know which of those ideas is the main takeaway versus supporting machinery.  
**Smallest fix:** Add one orienting sentence before the code cell: “The core pattern is simple: generate the agent’s answer first, then send that answer plus the ground truth into a second agent whose only job is scoring.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [unexplained term]  
**Location:** Part 3 → judge agent setup  
**Quote:** `f"Product catalog (ground truth):\n{catalog}\n\n"`  
**What the student experiences:** “Ground truth” is used like standard eval vocabulary, but a student new to agent evaluation may not know it means the trusted reference data used to judge correctness. That makes the judge-input construction feel more abstract than it needs to.  
**Smallest fix:** Add a brief parenthetical the first time it appears: `Product catalog (ground truth — the trusted correct data):`  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** Part 4: Comparing Two Agent Versions  
**Quote:** “When you change an agent's instructions, how do you know if it got better or worse? Run both versions on the same test set and compare scores.”  
**What the student experiences:** The student sees a neat side-by-side comparison, but the real project use case is still a little thin. They may not connect this to the everyday situations where they’ll actually need it: prompt edits, model swaps, tool changes, schema changes.  
**Smallest fix:** Add one sentence after this paragraph: “This is the pattern you’ll use anytime you tweak prompts, swap models, or change tool behavior and need evidence that the agent actually improved.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [lost thread]  
**Location:** Part 2: Pass/Fail Checks  
**Quote:** `## ✅ Part 2: Pass/Fail Checks\n\nThe simplest evaluation: check whether required facts appear in the output.\n\nFast, deterministic, and catches factual errors automatically.`  
**What the student experiences:** This appears as a code cell containing markdown text, and there’s no visible transition from “here is the test set” to “here is the function that evaluates it.” For a first-time learner, the lesson’s main pattern starts to feel jumpy instead of progressive.  
**Smallest fix:** Replace this stray cell with a real markdown explanation or, if it was meant as a placeholder, use that slot for the missing `check_output()` helper cell so the flow becomes: test cases → helper → full suite.  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

## Gemini Review

This notebook passed third-pass review with a few findings. The overall structure is excellent, progressing logically from simple checks to more nuanced, rubric-based evaluation. A student will walk away with a clear framework for testing agents. The suggested fixes address a missing helper function and two small confidence gaps that could prevent a student from applying these patterns to a real-world project.

---

### Finding 1

**Severity:** `[confidence gap]`
**Location:** Part 2: Pass/Fail Checks
**Quote:** `passed, failures = check_output(output, test_case["must_contain"], test_case["must_not_contain"])`
**What the student experiences:** "This code won't run because the `check_output` function is never defined. I'm completely blocked and can't continue with the demos in the rest of the notebook."
**Smallest fix:** Add a new code cell before "Run the Full Test Suite" that defines the `check_output` helper function.

```python
def check_output(output: str, must_contain: list[str], must_not_contain: list[str]) -> tuple[bool, list[str]]:
    """Check if output contains all required and none of the forbidden strings."""
    output_lower = output.lower()
    failures = []

    for s in must_contain:
        if s.lower() not in output_lower:
            failures.append(f"Missing required string: {s}")

    for s in must_not_contain:
        if s.lower() in output_lower:
            failures.append(f"Contains forbidden string: {s}")
            
    return not failures, failures

# --------------------------------------------------------------
print("✅ check_output() helper ready")
```

**Blocks transfer:** Yes
**Outline-backed:** Yes

### Finding 2

**Severity:** `[confidence gap]`
**Location:** Part 3: Rubric-Based Evaluation with a Judge Agent
**Quote:** `judge_input = (f"Product catalog (ground truth):\\n{catalog}\\n\\n" ...)`
**What the student experiences:** "This works for a tiny catalog of four items, but what if my project has thousands? I can't put my entire database into the judge's prompt every time. This pattern seems like it won't work for a real application."
**Smallest fix:** Add a one-sentence note in a new markdown cell after the "Run Rubric Evaluation" demo.

```markdown
💡 **For your own projects:** For a large dataset, you'd only provide the *relevant* ground truth to the judge — for example, just the database entry for the product being asked about, not the entire catalog.
```
**Blocks transfer:** Yes
**Outline-backed:** No

### Finding 3

**Severity:** `[lost why]`
**Location:** Part 4: Comparing Two Agent Versions
**Quote:** `print(f"👑 Winner (by judge score): {winner}")`
**What the student experiences:** "The code declares a winner based only on the judge score. Why did we calculate the pass/fail rate if it's not used in the decision? What if a new version has a higher quality score but gets more facts wrong?"
**Smallest fix:** Add a short explanatory sentence to the summary printout that clarifies the relationship between the two metrics.

Change this:
```python
print(f"👑 Winner (by judge score): {winner}")
```
To this:
```python
print(f"👑 Winner: {winner} (quality score improved without hurting factual correctness)")
print(f"💡 Factual correctness (pass rate) is a gate. Only compare quality scores if correctness is stable or has improved.")
```
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Grok Review

**Severity:** `[unexplained term]`

**Location:** Part 2 ("Run the Full Test Suite" cell)

**Quote:** `passed, failures = check_output( output, test_case["must_contain"], test_case["must_not_contain"] )`

**What the student experiences:** I'm reading the cell, I see `check_output` being called with the agent's output and the test criteria, but the function has never been defined anywhere in this notebook or the previous one on error handling. I have no idea what it's doing under the hood or how it decides pass/fail.

**Smallest fix:** Insert a helper function cell immediately before the "Run the Full Test Suite" cell that defines `check_output` (with a one-line docstring and clear variable names for the two returned values).

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[lost why]`

**Location:** Part 3 (inside the rubric evaluation loop)

**Quote:** `judge_input = ( f"Product catalog (ground truth):\\n{catalog}\\n\\n" f"Question: {test_case['input']}\\n\\n" f"Agent response: {agent_output}\\n\\n" f"Evaluate this response. Use the product catalog to verify factual accuracy." )`

**What the student experiences:** I see the full product catalog being rebuilt and fed to the judge on every test, but I don't understand why the judge needs the ground truth. It feels like extra code — is the point to make the judge independent, or is this just how judge agents always work?

**Smallest fix:** Add one sentence right before the `for test_case in eval_cases:` loop: "We give the judge the full product catalog as ground truth so it can verify facts itself instead of blindly trusting the agent's response."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`

**Location:** End of Part 1 (the paragraph after the TEST_CASES cell)

**Quote:** `Test sets should be saved to a file and version-controlled alongside your agent code — that way regressions can be traced to specific instruction or model changes.`

**What the student experiences:** The notebook tells me this is important for real projects, but it never shows the actual Python code to save or load the test set. After finishing the notebook I still wouldn't know what to type in my own project to make a persistent golden test set.

**Smallest fix:** Right after that sentence, add a 4-line code snippet showing `import json` + `json.dump(TEST_CASES, open("test_cases.json", "w"), indent=2)` and a matching load line.

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[pacing break]`

**Location:** Part 4 (the "Head-to-Head Comparison" cell)

**Quote:** `## 🔄 Part 4: Comparing Two Agent Versions`

**What the student experiences:** This single cell runs both agents, does pass/fail checks, rebuilds the catalog again, builds a judge prompt, calls the judge twice per test case, tracks four different lists, then computes averages and a winner. I'm holding too many new ideas at once and can't tell which part is the core "compare two versions" pattern I should take away.

**Smallest fix:** Split the cell into two: first cell runs both agents and prints raw outputs (with a clear header "Step 1: Get responses"), second cell does the judging/comparison (header "Step 2: Judge and compare"). Keep everything else identical.

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

This notebook has a few rough edges (especially the missing helper and some leftover editing cells), but the core teaching beats land. With the four small additions above a first-time student would finish feeling they could actually build and use a test suite on their own project. The rest of the notebook is clear for someone who has come through the course sequentially.

---

## DeepSeek Review

**Third-Pass Review: Notebook 09 — Testing & Evaluating Agents**

**Student persona:** Completed Notebook 08 (Error Handling & Recovery). Comfortable with Python. First exposure to agent evaluation.

---

**Finding 1: Missing `check_output` function — all pass/fail demos crash**

**Severity:** `[confidence gap]`

**Location:** Part 2, code cell "#### Run the Full Test Suite"

**Quote:**
```python
passed, failures = check_output(
    output,
    test_case["must_contain"],
    test_case["must_not_contain"]
)
```

**What the student experiences:** The very first evaluation demo errors with `NameError: name 'check_output' is not defined`. They can't run the pass/fail test suite. The same missing function also crashes the rubric evaluation duplicate cell and the version comparison cell. Every demo that uses pass/fail is broken. The student will assume the notebook is incomplete or they missed a definition.

**Smallest fix:** Define `check_output` in the Part 2 code cell (or immediately before the test suite run):
```python
def check_output(output, must_contain, must_not_contain):
    passed = True
    failures = []
    for term in must_contain:
        if term not in output:
            passed = False
            failures.append(f"Missing: '{term}'")
    for term in must_not_contain:
        if term in output:
            passed = False
            failures.append(f"Found forbidden: '{term}'")
    return passed, failures
```

**Blocks transfer:** Yes — the student cannot see any pass/fail evaluation work, and cannot use the pattern in their own project without guessing the implementation.

**Outline-backed:** Yes — pass/fail checks are a core topic.

---

**Finding 2: Duplicate rubric evaluation cells — pacing break and confusion**

**Severity:** `[lost thread]`, `[pacing break]`

**Location:** Immediately after the "#### Run Rubric Evaluation" markdown cell. Two nearly identical code cells appear back-to-back.

**Quote (first cell, correct one):**
```python
eval_cases = TEST_CASES[:3]
...
eval_results = []
for test_case in eval_cases:
```

**Quote (second cell, duplicate with extra logic):**
```python
eval_cases = TEST_CASES[:3]
...
eval_results = []
v1_passes = []
...
passed, _ = check_output(...)
```

**What the student experiences:** They run the first rubric evaluation cell and see output. Then they scroll down and see a second code cell that looks identical but adds `v1_passes` and calls `check_output` (which is undefined). They will either run it and hit an error, or skip it and later wonder where `v1_passes` came from in the version comparison section (Finding 3). The lesson's flow from pass/fail → rubric → version comparison is lost.

**Smallest fix:** Delete the duplicate cell entirely. Keep only the first rubric evaluation cell. The version comparison section should compute v1 scores independently rather than relying on variables from a prior cell.

**Blocks transfer:** No, but it severely disrupts the learning sequence and wastes student attention on debugging the notebook's internal state.

**Outline-backed:** Yes — rubric evaluation is a main topic.

---

**Finding 3: Version comparison references undefined variables (`v1_passes`, `v1_scores`)**

**Severity:** `[confidence gap]`

**Location:** Part 4, code cell "#### Head-to-Head Comparison"

**Quote:**
```python
v1_scores = []
v1_passes = []
...
print(f"   v1: {'✅' if v1_passes[-1] else '❌'} pass/fail  |  ...")
...
v1_pass_rate = sum(v1_passes) / len(v1_passes)
```

**What the student experiences:** If they ran the first rubric evaluation cell (the correct one), `v1_passes` and `v1_scores` are never defined. When they reach the comparison cell, they get `NameError: name 'v1_passes' is not defined`. They cannot complete the head-to-head demo. They don't know whether the improved agent actually helps.

**Smallest fix:** In the version comparison code cell, run the v1 evaluation fresh alongside v2 (as the cell already does for `v2_result` but does not for `v1_result` — wait, the cell *does* run `v1_result = await Runner.run(agent_v1, ...)` and computes scores. The problem is the cell uses `v1_passes` and `v1_scores` list variables that were *supposed* to be populated inside the loop, but the cell initializes them as empty lists and then uses `.append()` correctly — the actual issue is that the cell also references variables from outside scope (the duplicate cell's `v1_passes` if it ran, or nothing if it didn't). Actually re-reading the code: the cell does define `v1_scores = []` and `v1_passes = []` at the top. So the indexing `v1_passes[-1]` inside the loop will work because the lists are being appended to. Wait, the code shows:

```python
v1_scores = []
v1_passes = []

for i, test_case in enumerate(comparison_cases):
    v1_result = await Runner.run(agent_v1, ...)
    v1_output = v1_result.final_output
    passed, _ = check_output(...)
    v1_passes.append(passed)
    ...
    v1_scores.append(score)
```

That looks correct. The `v1_passes` and `v1_scores` lists are defined at the top of the cell. So why would it fail? Because `check_output` is undefined — same root cause as Finding 1. So the variable issue is secondary; the real blocker is the missing function. However, the duplicate cell defined `v1_passes` outside this cell, which could cause confusion. I'll adjust the finding to focus on the chain of missing definition.

Revised finding: The version comparison cell relies on `check_output` (undefined) and also on `catalog` (defined in the duplicate rubric evaluation cell but not in the comparison cell). The cell references `catalog` without defining it:

```python
catalog = "\n".join(
    f"{pid}: {p['name']} — ${p['price']:.2f} — ..."
    for pid, p in PRODUCTS.items()
)
```

Actually, the comparison cell *does* define `catalog` inside the loop. Let me re-read the JSON: the comparison code cell defines `catalog` inside the loop. So that's fine. So the only issue is `check_output` again.

**Revised Finding 3: `check_output` missing in version comparison**

**Severity:** `[confidence gap]`

**Location:** Part 4, code cell "#### Head-to-Head Comparison", line `passed, _ = check_output(...)`

**What the student experiences:** Same as Finding 1 — No `check_output` defined. The version comparison run crashes. They never see whether v2 improved.

**Blocks transfer:** Yes — the student cannot compare agent versions as taught.

**Outline-backed:** Yes.

---

**Finding 4: Part 2 header is a code cell instead of markdown**

**Severity:** `[lost thread]` (minor)

**Location:** The cell between the "---" divider after Part 1 and the "#### Run the Full Test Suite" markdown cell. `cell_type` is "code" but source is plain markdown text: `"## ✅ Part 2: Pass/Fail Checks\nThe simplest evaluation: check whether required facts appear in the output.\n\nFast, deterministic, and catches factual errors automatically."`

**What the student experiences:** If they accidentally run this cell, they get a Python `SyntaxError`. Even if they don't run it, seeing a code cell that contains no code breaks the visual rhythm of the notebook.

**Smallest fix:** Change `cell_type` to `"markdown"`.

**Blocks transfer:** No.

---

**Summary**

This notebook has a critical missing function that breaks three demos (pass/fail, rubric duplicate, version comparison). The duplicate rubric evaluation cell adds confusion. The student cannot execute the core concepts they are supposed to learn.

**Priority fixes:**
1. Define `check_output()` before the test suite run.
2. Remove the duplicate rubric evaluation code cell.
3. Ensure the version comparison cell defines all its own variables (it does, except for `check_output`).
