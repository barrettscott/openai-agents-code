# Review Report: Guardrails
Generated: 2026-05-10 | Notebook: /Users/scott/Dropbox/Notebooks/openai-agents/Week_4_Memory_Safety_Observability/22_Guardrails.ipynb
Models: Anthropic (claude-opus-4-7 (CLI/Max)) · OpenAI (gpt-5.4) · Gemini (gemini-2.5-pro) · Grok (grok-4.20-0309-reasoning) · DeepSeek (deepseek-reasoner) · Dedup (claude-opus-4-7 CLI/Max)

---

# Grouped Findings: Guardrails

# Third-Pass Synthesis — Notebook 22 (Guardrails)

Five reviewers surfaced overlapping concerns. Strong consensus on three confidence-gap issues around the SDK API surface (decorator-vs-class, ctx parameters, output_info), plus a broken Part 4 test cell that three reviewers independently flagged. Findings ordered by priority.

---

## 1. Decorator + class wrapper feels redundant

**Severity:** `[confidence gap]`
**Location:** Part 2, `cooking_agent` definition (pattern repeats in Parts 3, 4, 5)
**Reviewers:** 2 — Anthropic, Gemini
**Quote:** `input_guardrails=[InputGuardrail(guardrail_function=topic_guardrail)]`
**What the student experiences:** "I just decorated my function with `@input_guardrail` — why do I also have to wrap it in `InputGuardrail(...)`? Am I doing the same thing twice? Which form do I write in my own code?"
**Smallest fix:** One sentence near the first agent definition: "The `@input_guardrail` decorator marks the function as a guardrail; wrapping it in `InputGuardrail(...)` is what attaches it to the agent and lets you configure parameters like `run_in_parallel` (Part 5)."
**Blocks transfer:** Yes
**Outline-backed:** Yes (input guardrails as a usable pattern); the decorator/class explanation is not specifically scripted

---

## 2. Guardrail signature types are unexplained boilerplate

**Severity:** `[unexplained term]`
**Location:** Part 2, `topic_guardrail` function signature
**Reviewers:** 3 — Anthropic, Gemini, Grok
**Quote:** `async def topic_guardrail(ctx: RunContextWrapper, agent: Agent, input: str | list[TResponseInputItem]) -> GuardrailFunctionOutput:`
**What the student experiences:** "Three of these types (`RunContextWrapper`, `TResponseInputItem`, `GuardrailFunctionOutput`) are imported but never explained. The body doesn't even use `ctx` or `agent`. Is the signature mandatory? Can I simplify it? Which parts can I change for my own project?"
**Smallest fix:** One inline comment or short markdown note above the function: "The SDK calls every guardrail with the same signature: `ctx` is the run context (used in Part 3 for tracing), `agent` is the agent being guarded, and `input` is the user message. You can ignore parameters you don't need."
**Blocks transfer:** Yes
**Outline-backed:** Split — Anthropic and Grok say yes (guardrails as a usable pattern), Gemini says no (signature explanation isn't specifically scripted). Either way the fix is minimal.

---

## 3. `ctx.context` appears without explanation

**Severity:** `[unexplained term]`
**Location:** Part 3, LLM-based guardrail
**Reviewers:** 3 — Gemini, Grok, DeepSeek
**Quote:** `result = await Runner.run(topic_checker_agent, input, context=ctx.context)`
**What the student experiences:** "I've seen `Runner.run()` before but never with `context=`. What is `ctx.context`? Why pass it here? Is it required boilerplate or something I should understand?"
**Smallest fix:** One inline comment: `# Pass the run context through so the checker agent shares state and links into the same trace.` Alternatively, fold into the "Why This Works" cell.
**Blocks transfer:** Split — Gemini and Grok say yes (student won't know which parts to adapt); DeepSeek says no (student can copy without understanding). Treat as Yes — the fix is one comment.
**Outline-backed:** Split (Grok yes, Gemini/DeepSeek no)

---

## 4. Part 4 output-guardrail test cell is broken

**Severity:** `[lost why]`
**Location:** Part 4, the cell beginning `print("📤 OUTPUT GUARDRAIL TEST")`
**Reviewers:** 3 — Anthropic, Gemini, DeepSeek
**Quote:** Anthropic and Gemini report the cell has `cell_type: "markdown"` instead of `"code"`; DeepSeek reports a duplicated short-answer test block within the cell. Likely the same underlying corruption (probable Gemini-edit artifact).
**What the student experiences:** Either the long-response test never runs (so the student never sees the output guardrail actually fire — they only see the input guardrail blocking in Part 2), or the short test runs twice and they wonder if the notebook is broken. Lesson promise ("output guardrails validate the agent's response before it's delivered") is never demonstrated visually.
**Smallest fix:** Open the cell, convert to `cell_type: "code"`, remove any duplicated short-test block so it reads: short-test (passes) → long-test (triggers tripwire). The content already exists.
**Blocks transfer:** Yes (Anthropic) / No (Gemini, DeepSeek). Treat as Yes — without the long test firing, the central concept of Part 4 never lands.
**Outline-backed:** Yes (outline calls for "output guardrails — validate what goes out")

---

## 5. Output guardrail demo is too narrow — student leaves thinking it's about length

**Severity:** `[lost why]` / `[confidence gap]`
**Location:** Part 4, output guardrail intro and aftermath
**Reviewers:** 2 — OpenAI, Grok
**Quote:** "Output guardrails validate the agent's response before it's delivered. Useful for blocking responses that contain sensitive data, violate policy, or don't meet quality standards."
**What the student experiences:** The intro lists realistic uses (PII, policy, quality), but the demo is a 30-word limit. The student leaves thinking output guardrails are about length, not safety. They also don't get the reusable pattern: let the main agent generate, inspect output in a separate function, fire the tripwire if needed.
**Smallest fix:** One bridging sentence after the demo: "This word-count example is deliberately simple — the same pattern (let the agent answer normally, then inspect the final output before it reaches the user) is how you'd block PII leaks, policy violations, or low-quality outputs in a real app."
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## 6. Blocking mode — student doesn't feel when to choose it

**Severity:** `[lost why]`
**Location:** Part 5, intro paragraph
**Reviewers:** 2 — OpenAI, Grok
**Quote:** "By default, guardrails run in parallel with the agent — both start at the same time. With `run_in_parallel=False`, the guardrail runs first and blocks the agent from starting if the tripwire fires. This saves tokens when the guardrail check is cheap and the agent call is expensive."
**What the student experiences:** Mechanics are clear, but no concrete heuristic for choosing. "When would I actually pick blocking?"
**Smallest fix:** Reframe the opening sentence: "Use blocking mode (`run_in_parallel=False`) when your guardrail is cheap (keyword or simple rule) but the main agent is expensive — the agent never starts if the input is invalid, saving both latency and tokens. Stick with parallel mode when most requests are expected to pass and you want faster responses."
**Blocks transfer:** Split — Grok yes, OpenAI no. Treat as Yes (student can't make the design call independently).
**Outline-backed:** Yes

---

## 7. Part 5 pacing — student can't tell what the new idea is

**Severity:** `[pacing break]`
**Location:** Part 5 opening
**Reviewers:** 2 — OpenAI, Grok
**Quote:** "## ⚙️ Part 5: Blocking Mode"
**What the student experiences:** By Part 5 they've absorbed rule-based vs LLM-based, input vs output, tripwires, and the parallel default. Then Part 5 reuses the Part 2 guardrail with one new parameter. Easy to lose track of which mode is the default and what's actually new here.
**Smallest fix:** One orienting sentence at the top of Part 5: "This isn't a new kind of guardrail — it changes *when* an input guardrail runs relative to the main agent."
**Blocks transfer:** No
**Outline-backed:** Yes

---

## 8. `output_info` field is set but never visibly used

**Severity:** `[unexplained term]` / `[confidence gap]`
**Location:** Part 2 (string form) and Part 4 (Pydantic model form)
**Reviewers:** 2 — Anthropic, DeepSeek
**Quote:** Part 2: `output_info="Off-topic request detected" if is_off_topic else "OK"` ; Part 4: `output_info=LengthCheck(is_acceptable=not is_too_long, word_count=word_count)`
**What the student experiences:** The shape of `output_info` changes between Parts 2 and 4 (string → Pydantic model) without comment. Field is set, never visibly consumed. "Is it required? What does it control? When should I use a string vs a model?"
**Smallest fix:** One comment near Part 2's first use: `# output_info is surfaced in tracing/logs — useful for debugging why a guardrail fired. Can be a string or a Pydantic model (Part 4) when you want structured detail.`
**Blocks transfer:** Split — Anthropic no, DeepSeek yes. Treat as Yes for the structured-output design question.
**Outline-backed:** No

---

## 9. "Tripwire" used before it's defined

**Severity:** `[unexplained term]`
**Location:** Part 1, "How Guardrails Work"
**Reviewers:** 1 — OpenAI
**Quote:** "If a guardrail triggers its **tripwire**, execution halts and raises an exception."
**What the student experiences:** First appearance of "tripwire" in the course; inferable but not crisp. They'll see `tripwire_triggered=...` two cells later without knowing the term is the SDK's stop signal.
**Smallest fix:** One sentence: "A *tripwire* is the guardrail's stop signal — when it fires, the SDK raises an exception instead of returning the agent's response."
**Blocks transfer:** No
**Outline-backed:** Yes

---

## 10. Instructions vs guardrails — when to reach for which

**Severity:** `[confidence gap]`
**Location:** "🎯 The Problem" cell
**Reviewers:** 1 — DeepSeek
**Quote:** "Agents respond to whatever they're asked... Guardrails let you validate inputs and outputs so unsafe or off-topic content never reaches the user."
**What the student experiences:** "I already write topic limits into instructions. Why do I need guardrails?" The notebook doesn't make the distinction between guidance (instructions) and enforcement (guardrails).
**Smallest fix:** Add one sentence to the Problem cell: "Instructions encourage good behavior; guardrails enforce it — they can't be overridden by the model."
**Blocks transfer:** Yes
**Outline-backed:** No (not specifically scripted, but a natural teaching beat)

---

## 11. Rule-based vs LLM-based — no decision rule

**Severity:** `[confidence gap]`
**Location:** Part 2 intro
**Reviewers:** 1 — OpenAI
**Quote:** "The simplest guardrail — no LLM needed. Just a function that checks the input against rules and returns a pass/fail verdict."
**What the student experiences:** Student can copy each demo but doesn't have a heuristic for choosing between them in their own project.
**Smallest fix:** One sentence at end of Part 2 intro: "Use rule-based guardrails when the violation is easy to define in code — keywords, length limits, required formats, or simple allow/block lists. Reach for LLM-based (Part 3) when the check needs judgment."
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## 12. Part 1 introduces parallel/blocking caveat too early

**Severity:** `[pacing break]`
**Location:** Part 1, paragraph after the diagram
**Reviewers:** 1 — Anthropic
**Quote:** "By default, the input guardrail and agent run concurrently; the agent's result is discarded if the tripwire fires... Use blocking mode (Part 5) to guarantee the agent never starts if the guardrail fires."
**What the student experiences:** Four ideas (sequential → parallel, discarded results, "by default" implies alternatives, forward to Part 5) crammed in before they've seen one guardrail fire.
**Smallest fix:** Cut the parallel/blocking-mode preamble from Part 1. Keep just: "If a guardrail triggers its tripwire, execution halts and raises an exception — no unsafe response is delivered." The parallel-vs-blocking nuance is already taught in Part 5.
**Blocks transfer:** No
**Outline-backed:** No (outline puts parallel vs blocking as a Part 5 beat)

---

## Recommended fix order

1. **Fix the broken Part 4 test cell** (#4) — this is the only finding that breaks something the lesson promises to demonstrate.
2. **Add the decorator/class clarification** (#1) and **signature explanation** (#2) — both land in Part 2 and are the most-flagged confusions.
3. **Add `ctx.context` comment in Part 3** (#3) — one-line fix, three reviewers.
4. **Add the output-guardrail bridging sentence in Part 4** (#5) and **the blocking-mode heuristic in Part 5** (#6) — these are the two `[lost why]` issues that affect transfer.
5. The remaining six findings (#7–#12) are each one sentence; bundle them in a single editing pass.

Total addition if all are taken: roughly 10–12 sentences plus one cell-type fix. No new sections required.

---

# Individual Reviews

The grouped findings above are your primary working surface. The full individual reviews below are for verifying nuance.

---

## Anthropic Review

# Third-Pass Review — Notebook 22: Guardrails

**5 findings.**

---

**Severity:** `[confidence gap]`
**Location:** Part 2, agent definition cell (and Parts 3, 4, 5 repeat the pattern)
**Quote:** `input_guardrails=[InputGuardrail(guardrail_function=topic_guardrail)]`
**What the student experiences:** Just defined `topic_guardrail` with the `@input_guardrail` decorator — and now we're also wrapping it in `InputGuardrail(guardrail_function=...)`. The student wonders: do I need both? What does the decorator do if I have to wrap it again? They won't know which form to write in their own code.
**Smallest fix:** Add one sentence near the first agent: "The `@input_guardrail` decorator marks the function as a guardrail; wrapping it in `InputGuardrail(...)` is what lets you add parameters like `run_in_parallel` (Part 5)."
**Blocks transfer:** Yes
**Outline-backed:** Yes — outline calls for teaching input guardrails as a usable pattern

---

**Severity:** `[unexplained term]`
**Location:** Part 2, the `topic_guardrail` function signature
**Quote:** `async def topic_guardrail(ctx: RunContextWrapper, agent: Agent, input: str | list[TResponseInputItem]) -> GuardrailFunctionOutput:`
**What the student experiences:** Three of those types (`RunContextWrapper`, `TResponseInputItem`, `GuardrailFunctionOutput`) are imported at the top but never explained. The function body doesn't even use `ctx` or `agent` here. The student is uncertain whether the signature is mandatory, what those types do, or what would happen if they simplified it.
**Smallest fix:** One inline comment above the signature: "The SDK passes the run context, the agent being guarded, and the input — your function only has to read the input."
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

**Severity:** `[lost why]`
**Location:** Part 4, the second test cell (intended "Long response — should trigger" demo)
**Quote:** The cell whose source begins `print("📤 OUTPUT GUARDRAIL TEST")` and continues `# Long response — should trigger the output guardrail` has `cell_type: "markdown"` instead of `"code"`.
**What the student experiences:** The student reads the lesson promise ("Output guardrails validate the agent's response before it's delivered") and then never sees the output guardrail actually fire — only the short-answer pass case runs. They get the concept of input tripwires (Part 2 shows blocking) but no concrete proof that output guardrails block too.
**Smallest fix:** Convert the cell to `cell_type: "code"`. The content already exists, just miscategorized — likely a Gemini edit artifact. (Flagged here because it directly breaks the student's ability to see the concept land, not as a style issue.)
**Blocks transfer:** Yes
**Outline-backed:** Yes — outline lists "output guardrails — validate what goes out"

---

**Severity:** `[pacing break]`
**Location:** Part 1, the cell after the diagram
**Quote:** *"The diagram above shows the logical flow... By default, the input guardrail and agent run concurrently; the agent's result is discarded if the tripwire fires.* … *By default, input guardrails run in parallel with the agent. Use blocking mode (Part 5) to guarantee the agent never starts if the guardrail fires."*
**What the student experiences:** They just absorbed a clean sequential mental model from the diagram, then within two short paragraphs are told (a) it's actually parallel, (b) the agent's result is discarded, (c) "by default" implies a non-default exists, (d) Part 5 will cover that. Four ideas crammed in before they've even seen one guardrail fire. The "parallel" caveat could wait until Part 5 introduces it.
**Smallest fix:** Cut the parallel/blocking-mode preamble here. Keep just: "If a guardrail triggers its tripwire, execution halts and raises an exception — no unsafe response is delivered." Move the parallel-vs-blocking nuance into Part 5 where it belongs.
**Blocks transfer:** No
**Outline-backed:** No — outline lists parallel vs blocking as a distinct teaching beat (Part 5), not a Part 1 framing item

---

**Severity:** `[unexplained term]`
**Location:** Part 2, return statement of `topic_guardrail`
**Quote:** `return GuardrailFunctionOutput(tripwire_triggered=is_off_topic, output_info="Off-topic request detected" if is_off_topic else "OK")`
**What the student experiences:** They understand `tripwire_triggered` — but `output_info` is set and then never visibly consumed in the notebook. The student wonders whether it's required, what it controls, and whether they need to set it carefully in their own guardrails.
**Smallest fix:** Brief comment after the field: `# output_info is surfaced in tracing/logs — useful for debugging why a guardrail fired`
**Blocks transfer:** No
**Outline-backed:** No

---

## OpenAI Review

**Severity:** [unexplained term]  
**Location:** Part 1: How Guardrails Work  
**Quote:** “If a guardrail triggers its **tripwire**, execution halts and raises an exception — no unsafe response is delivered.”  
**What the student experiences:** The notebook uses “tripwire” as if it’s already intuitive, but this is the first time the concept appears in the course. A student can infer it means “block the run,” but not quite what the SDK is treating as the decisive signal.  
**Smallest fix:** Add one short sentence right before or after this line: “A *tripwire* is the guardrail’s stop signal — when it fires, the SDK raises an exception instead of returning the agent’s response.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Part 2: Rule-Based Input Guardrail  
**Quote:** “The simplest guardrail — no LLM needed. Just a function that checks the input against rules and returns a pass/fail verdict.”  
**What the student experiences:** The student sees the pattern run, but doesn’t get a concrete rule for when to choose rule-based vs LLM-based guardrails in their own projects. They can copy the demo, but may not know how to decide between the two approaches.  
**Smallest fix:** Add one sentence at the end of this intro cell: “Use rule-based guardrails when the violation is easy to define in code — keywords, length limits, required formats, or simple allow/block lists.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** Part 4: Output Guardrail  
**Quote:** “Output guardrails validate the agent's response before it's delivered. Useful for blocking responses that contain sensitive data, violate policy, or don't meet quality standards.”  
**What the student experiences:** The section opens with realistic reasons to use output guardrails, but the demo itself is just a 30-word limit. A student understands *what* the code does, but may leave thinking output guardrails are mainly about response length instead of safety or production checks.  
**Smallest fix:** Add one bridging sentence before the code: “This word-count example is deliberately simple — the same pattern is how you’d block PII leaks, policy violations, or low-quality outputs in a real app.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Part 4: Output Guardrail  
**Quote:** “Output guardrails validate the agent's response before it's delivered.”  
**What the student experiences:** The student sees an output guardrail function, but not the principle for designing one independently. They may not realize the reusable pattern is “let the main agent generate, then inspect `output` in a separate guardrail function and either pass or fire the tripwire.”  
**Smallest fix:** Add one sentence after the demo or in a “Why This Works” cell: “In your own projects, the pattern is: let the main agent answer normally, then inspect that final output in a separate guardrail function before anything reaches the user.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [pacing break]  
**Location:** Part 5: Blocking Mode  
**Quote:** “## ⚙️ Part 5: Blocking Mode”  
**What the student experiences:** This section lands right after the student has just learned input guardrails, LLM-based guardrails, output guardrails, and tripwires. “Blocking mode” is important, but it introduces a new execution model late enough that the student may struggle to tell whether the main takeaway is *what guardrails are* or *how they’re scheduled*.  
**Smallest fix:** Add one orienting sentence to the Part 5 intro: “This does not introduce a new kind of guardrail — it only changes *when* an input guardrail runs relative to the main agent.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** Part 5: Blocking Mode  
**Quote:** “By default, guardrails run in parallel with the agent — both start at the same time. With `run_in_parallel=False`, the guardrail runs first and blocks the agent from starting if the tripwire fires.”  
**What the student experiences:** The mechanics are clear, but the student may still wonder when latency is worth more than token savings, or what real situations make blocking mode the better choice. The section explains behavior, but only lightly answers “why would I pick one mode over the other?”  
**Smallest fix:** Add one sentence: “In practice, use parallel mode when most requests are expected to pass and you want faster responses; use blocking mode when many requests may be rejected or the main agent is expensive.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

## Gemini Review

This notebook passed third-pass review with four findings. The core concepts are taught well, with clear "why this matters" explanations for each new pattern. The issues are concentrated in the API surface, where several new classes and parameters are introduced without enough context for a student to feel confident using them independently.

---

**Severity:** `[unexplained term]`
**Location:** Part 2: Rule-Based Input Guardrail
**Quote:** `async def topic_guardrail(\n    ctx: RunContextWrapper,\n    agent: Agent,\n    input: str | list[TResponseInputItem]\n)`
**What the student experiences:** "I've never seen `RunContextWrapper` or `TResponseInputItem` before. Why is the signature for this function so complicated? I also don't see `ctx` or `agent` being used in this first example, which makes me wonder if I'm supposed to do something with them."
**Smallest fix:** Add a short markdown cell before the code block explaining the signature. For example: "The SDK provides a standard signature for all guardrail functions. `ctx` gives access to the run's context (which we'll use in Part 3 for tracing), `agent` is the agent being guarded, and `input` is the user's message. You can ignore parameters you don't need."
**Blocks transfer:** Yes
**Outline-backed:** No

---

**Severity:** `[unexplained term]`
**Location:** Part 3: LLM-Based Input Guardrail
**Quote:** `result = await Runner.run(topic_checker_agent, input, context=ctx.context)`
**What the student experiences:** "I see `ctx.context` is being used here, but what is it? Why am I passing it to the `Runner`? The notebook hasn't explained this."
**Smallest fix:** Add an inline comment explaining the purpose. For example: `# Pass the context to link this sub-run in the trace`. Alternatively, add to the "Why This Works" cell: "Passing `context=ctx.context` links the guardrail's execution to the main agent's run, making it easier to debug in the tracing dashboard."
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`
**Location:** Part 2: Rule-Based Input Guardrail, in the `cooking_agent` definition
**Quote:** `input_guardrails=[InputGuardrail(guardrail_function=topic_guardrail)]`
**What the student experiences:** "I already used the `@input_guardrail` decorator on my function. Why do I also have to wrap it in an `InputGuardrail` class? It feels like I'm doing the same thing twice, and I don't know why both are necessary."
**Smallest fix:** Add a sentence to the markdown just before the agent definition. For example: "The `@input_guardrail` decorator prepares the function to be used as a guardrail, and the `InputGuardrail` class attaches it to the agent. This class is also where we configure settings like `run_in_parallel` (Part 5)."
**Blocks transfer:** Yes
**Outline-backed:** No

---

**Severity:** `[pacing break]`
**Location:** Part 4: Output Guardrail
**Quote:** The entire markdown cell that begins `print("...OUTPUT GUARDRAIL TEST")`
**What the student experiences:** "This looks like code, but it's in a markdown cell so I can't run it. It also repeats the test from the cell I just ran. Is the notebook broken?"
**Smallest fix:** Change the problematic cell's type from Markdown to Code. Then, merge it with the preceding code cell. This creates a single, coherent test cell for Part 4 that demonstrates both a passing response and a blocked response.
**Blocks transfer:** No
**Outline-backed:** No

---

## Grok Review

**Severity:** `[unexplained term]`

**Location:** Part 2 (first `@input_guardrail`) and Part 3 (`llm_topic_guardrail` + `Runner.run` call)

**Quote:** `@input_guardrail async def topic_guardrail(ctx: RunContextWrapper, agent: Agent, input: str | list[TResponseInputItem]) -> GuardrailFunctionOutput:`

(and later) `result = await Runner.run(topic_checker_agent, input, context=ctx.context)`

**What the student experiences:** I just finished vector memory last notebook, so I know about sessions and context in a general sense, but I have no idea what `RunContextWrapper`, `TResponseInputItem`, or `ctx.context` actually are or why they're in every guardrail signature. It feels like magic boilerplate I have to copy-paste, and I don't know which parts I can change for my own project.

**Smallest fix:** After the first guardrail function, add one sentence: "The `RunContextWrapper` provides access to the current run's context (including any memory from Lesson 21); we pass `context=ctx.context` to the checker agent so it sees the same state as the main agent."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`

**Location:** Part 4 (after the length guardrail definition and its test cells)

**Quote:** "Output guardrails validate the agent's response before it's delivered. Useful for blocking responses that contain sensitive data, violate policy, or don't meet quality standards. Like input guardrails, output guardrails can be rule-based (as shown here) or LLM-based — Exercise 2 walks through the LLM-based pattern."

**What the student experiences:** I can see the length check working in the toy example, but the 30-word limit feels arbitrary and the test input ("Write at least 80 words...") isn't guaranteed to trigger with the agent's instructions. After the demo I still don't have a clear picture of when or how I'd actually use an output guardrail in my own project (e.g. for PII, brand voice, or downstream parsing safety).

**Smallest fix:** After the second test, add: "In a real project this pattern prevents token waste, policy violations, or malformed output that would break downstream code — `output_info` can be logged even on passes."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[lost why]`

**Location:** Part 5: Blocking Mode (intro paragraph and test)

**Quote:** "By default, guardrails run in parallel with the agent — both start at the same time. With `run_in_parallel=False`, the guardrail runs first and blocks the agent from starting if the tripwire fires. This saves tokens when the guardrail check is cheap and the agent call is expensive."

**What the student experiences:** The paragraph tells me it saves tokens, but the demo re-uses the cheap keyword guardrail from Part 2 and the test is just "it blocked early." I don't feel the stakes or see a realistic scenario where I'd choose blocking mode versus the default, so the concept doesn't stick as something I'd reach for on my own project.

**Smallest fix:** Change the first sentence of Part 5 to: "Use blocking mode (`run_in_parallel=False`) when your guardrail is cheap (keyword or simple rule) but the main agent is expensive — the agent never starts if the input is invalid, saving both latency and tokens."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[pacing break]`

**Location:** Part 4 → Part 5 transition (the output guardrail test cells through the blocking-mode section)

**Quote:** "## ⚙️ Part 5: Blocking Mode"

**What the student experiences:** By the time I reach blocking mode I've just absorbed rule-based vs LLM-based, input vs output, tripwires, and the parallel default from Part 1. Part 5 then re-uses the Part 2 guardrail with one new parameter. I’m juggling five new concepts in quick succession and no longer remember which mode is the default or why I’d pick one over the other.

**Smallest fix:** At the very start of the Part 5 markdown, add one orienting sentence: "We have seen parallel mode (default) and rule-based checks; now we make the guardrail run first."

**Blocks transfer:** No

**Outline-backed:** Yes

---

This notebook is in very good shape for a third-pass review. The core teaching beats are clear, the demos are short, and the Key Takeaways do a nice job of consolidating the material. The four findings above are the only spots where I (as a student who just finished vector memory) would lose confidence or momentum. Each can be fixed with one targeted sentence.

---

## DeepSeek Review

**Third-Pass Review: Notebook 22 — Guardrails**  
**Reviewer role:** Student who completed Notebook 21 and is reading this for the first time.

---

**Severity:** `[pacing break]`  
**Location:** Part 4 — output guardrail test cell  

**Quote:**  
```python
print("📤 OUTPUT GUARDRAIL TEST")
print("=" * 60)

# Short answer — should pass
try:
    result = await Runner.run(concise_agent, input="What is 2 + 2?")
    print(f"✅ Short response passed ({len(result.final_output.split())} words)")
    print(f"   {result.final_output}")
except OutputGuardrailTripwireTriggered as e:
    print(f"🔴 Blocked: {e}")

print("=" * 60)

# [then the same short-answer test is repeated identically before the long test]
```

**What the student experiences:**  
The exact same short-answer test runs twice. The cell prints the header, runs the short test, prints a divider, then runs the *same short test again* and then the long test. I’d stop and wonder if the notebook is broken or if I missed why the test is duplicated. It breaks the rhythm and makes me doubt whether the long test ever ran.

**Smallest fix:**  
Remove the duplicate block (everything between `print("=" * 60)` and `print()` before the long test). Keep only one short test and then the long test.

**Blocks transfer:** No  
**Outline-backed:** No (code hygiene issue)

---

**Severity:** `[confidence gap]`  
**Location:** `## 🎯 The Problem` cell  

**Quote:**  
```
Agents respond to whatever they're asked — including off-topic requests, harmful prompts,
or questions outside their scope.

Guardrails let you validate inputs and outputs so unsafe or off-topic content never reaches
the user.
```

**What the student experiences:**  
I already wrote instructions telling my agent what topics to handle. Why do I need guardrails? The notebook never explains the difference — instructions are guiding hints, guardrails are programmatic enforcement. Without that distinction, I wouldn’t know when to reach for guardrails vs when to improve instructions. I can follow the demos, but I’m missing the “so what” that tells me where guardrails fit in my agent design.

**Smallest fix:**  
Add one sentence to the Problem cell:  
```
Instructions encourage good behavior; guardrails enforce it — they can't be overridden by the model.
```

**Blocks transfer:** Yes (student won’t independently choose guardrails over instructions)  
**Outline-backed:** No (outline doesn’t script this distinction, but it’s a natural teaching beat)

---

**Severity:** `[unexplained term]`  
**Location:** Part 3 — LLM guardrail code cell  

**Quote:**  
```python
result = await Runner.run(topic_checker_agent, input, context=ctx.context)
```

**What the student experiences:**  
I’ve seen `Runner.run()` before, but never with a `context=` argument. What is `ctx.context`? Where does `ctx` come from? I can guess it’s the agent run’s context, but I don’t know what data it carries or why I should pass it here. The parameter is used without explanation, and since the guardrail doesn’t actually use `ctx` for anything else, I’m left wondering if this is required boilerplate or something I should understand.

**Smallest fix:**  
Either remove the irrelevant `context=` parameter (the guardrail doesn’t use it) or add a brief comment:  
```python
# ctx provides access to the agent's run context; ctx.context is passed through to the guardrail agent
```

**Blocks transfer:** No (student can copy the pattern, but won’t understand why it’s there)  
**Outline-backed:** No

---

**Severity:** `[confidence gap]`  
**Location:** Part 4 — output guardrail code cell  

**Quote:**  
```python
return GuardrailFunctionOutput(
    tripwire_triggered=is_too_long,
    output_info=LengthCheck(
        is_acceptable=not is_too_long,
        word_count=word_count
    )
)
```

**What the student experiences:**  
Earlier (Part 2) the guardrail’s `output_info` was a plain string. Here it’s a Pydantic model (`LengthCheck`). Why? What does `output_info` do with that structured data? The notebook never uses it — it just gets returned. I’m left wondering if `output_info` is just metadata for debugging or if the tripwire exception makes use of it. There’s no explanation of the two allowed types for `output_info` or when to use each.

**Smallest fix:**  
Add one sentence below the code cell:  
```output_info can be a string or a Pydantic model — use a model when you want structured details available to error handling or logging.
```

**Blocks transfer:** Yes (student won’t know how to design output_info for their own guardrails)  
**Outline-backed:** No

---

**Summary:**  
- 1 serious code error (duplicate test block) that breaks pacing  
- 2 confidence gaps (instructions vs guardrails; output_info purpose)  
- 1 unexplained term (context parameter)  

The notebook teaches the core guardrail patterns effectively. With these four small fixes, a student would finish the lesson confident they understand *why* guardrails exist, *how* to write them, and *where* the pieces fit in a real project.
