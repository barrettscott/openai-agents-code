# Review Report: Error Handling & Recovery
Generated: 2026-05-10 | Notebook: /Users/scott/Dropbox/Notebooks/openai-agents/Week_2_Reliability_And_Built_In_Tools/08_Error_Handling_And_Recovery.ipynb
Models: Anthropic (claude-opus-4-7 (CLI/Max)) · OpenAI (gpt-5.4) · Gemini (gemini-2.5-pro) · Grok (grok-4.20-0309-reasoning) · DeepSeek (deepseek-reasoner) · Dedup (claude-opus-4-7 CLI/Max)

---

# Grouped Findings: Error Handling & Recovery

# Third-Pass Synthesis — Notebook 08: Error Handling & Recovery

Five reviewers found broad agreement on a small set of comprehension gaps. The notebook's progression is sound; findings cluster around four locations where one missing sentence leaves a student unable to transfer the pattern. Two unanimous findings (Part 1 SDK parameter, Part 4 fallback mechanism), two strong-majority findings (Part 3 unexplained terms, Part 5 invisible failure path), and two singleton findings included for completeness.

---

## Finding 1 — Part 1 / Part 2 framing: `failure_error_function=None` lands cold and reverses the motivation

**Severity:** `[unexplained term]` + `[lost why]` (combined — same root gap)

**Location:** Part 1 intro markdown + first code cell (`@function_tool(failure_error_function=None)`); reverberates into Part 2 intro.

**Reviewers:** 4 of 5 — Anthropic (two findings), OpenAI, Grok, DeepSeek. Gemini did not flag.

**Quote (Part 1):** *"We disable the SDK's default error handler with `failure_error_function=None` so tool exceptions crash the run — this makes the failure mode visible."*
**Quote (Part 2):** *"By default the SDK already converts tool exceptions into a generic error message for the model."*

**What the student experiences:** A new SDK parameter appears with no setup — they don't know what the "default error handler" does, whether they'll ever set this in real code, or whether it's demo-only. They form the mental model "tools crash without try/except." Part 2 then reveals the default already catches exceptions, flipping the motivation. The student is left unsure whether unhandled exceptions actually break their real agents (they don't).

**Smallest fix:** Add one sentence to the Part 1 intro *before* the code cell, plus one inline comment on the decorator:

> *"By default, the SDK catches tool exceptions and returns a generic error message to the model. For this demo only, we pass `failure_error_function=None` to surface the raw exception so the failure mode is visible — Part 2 will write proper handling that gives the agent something more useful than the generic default."*

```python
# Demo-only: surfaces the raw exception. Don't set this on real tools.
@function_tool(failure_error_function=None)
```

**Blocks transfer:** Yes — without this, a student copying Part 1 into their own project propagates a demo flag and disables real error handling.

**Outline-backed:** Partial — the outline calls for "what happens when a tool raises an exception" as the leading beat, but does not call for teaching this specific parameter.

---

## Finding 2 — Part 4: Fallback mechanism is not made explicit

**Severity:** `[confidence gap]` (majority) / `[lost why]` (one reviewer)

**Location:** Part 4 intro markdown and the cell after the fallback demo.

**Reviewers:** 5 of 5 — Anthropic, OpenAI, Gemini, Grok, DeepSeek. Unanimous, with three distinct angles worth preserving:

- **Mechanism gap (Anthropic, Grok, DeepSeek):** the agent reads the tool's returned string and acts on it per the instructions — this is never stated. Students may think fallback is automatic at the SDK level.
- **Convention gap (Anthropic):** Part 2 taught "return error string instead of raising." Part 4 silently relies on that convention. Students writing their own fallbacks may try exception routing.
- **"Why this approach" gap (Gemini):** why drive fallback through agent instructions instead of a Python try/except around two tool calls? The unique benefit (recovery strategy lives in the prompt, not code) is never named.

**Quote:** *"The agent chooses the fallback through its instructions, but the tools still need to return errors in a form the agent can recognize."*

**What the student experiences:** The demo works smoothly and they can copy the pattern, but they don't have a mental model for *how* the agent decides to switch tools, *what* error format works, or *why* this is better than wrapping tool calls in Python.

**Smallest fix:** Add a `### 💡 Why This Works` cell (or expand the existing intro) after the demo with:

> *"The convention from Part 2 holds here: a failing tool returns a clear error string instead of raising. The agent reads that string and follows the instructions to call the fallback — there is no automatic SDK mechanism. Putting recovery logic in the instructions (rather than a Python try/except around the tool calls) means you can change the strategy by editing the prompt as you add or swap tools."*

**Blocks transfer:** Yes — students cannot write effective fallback instructions or format error messages for the agent without understanding the mechanism.

**Outline-backed:** Yes — outline calls for "Fallback strategies — alternative tools and graceful degradation."

---

## Finding 3 — Part 3: "exponential backoff with jitter" named without definition

**Severity:** `[confidence gap]`

**Location:** Part 3, "Why This Works" cell.

**Reviewers:** 3 of 5 — OpenAI, Gemini, DeepSeek. (Anthropic and Grok did not flag.)

**Quote:** *"In production, use exponential backoff with jitter instead of fixed waits."*

**What the student experiences:** They are told the demo's fixed-wait pattern isn't production-ready, but the production pattern is named without explanation. They now know what *not* to ship, but cannot implement the alternative.

**Smallest fix:** Add a parenthetical:

> *"In production, use exponential backoff (longer wait after each failure) with jitter (a small random offset, so many clients don't all retry at once) instead of fixed waits."*

**Blocks transfer:** Yes — students cannot apply the production pattern from a name alone.

**Outline-backed:** Mixed — OpenAI called this outline-backed; Gemini called it not. The retry pattern is outline-backed; these specific terms are not.

---

## Finding 4 — Part 3: "idempotent" used in security note without definition

**Severity:** `[unexplained term]`

**Location:** Security note cell after the retry "Why This Works."

**Reviewers:** 3 of 5 — Gemini, Grok, DeepSeek. (Anthropic and OpenAI did not flag.)

**Quote:** *"Only retry read-only or idempotent actions. Tools that create orders, send messages, or write records may have partially succeeded — retrying risks duplication."*

**What the student experiences:** A student unfamiliar with the term may skip the warning entirely and miss a safety-critical principle, or feel uncertain whether their own tools qualify.

**Smallest fix:** Add a parenthetical:

> *"Only retry read-only or idempotent actions (operations that produce the same result whether run once or multiple times)."*

**Blocks transfer:** Yes (Gemini, Grok) / No (DeepSeek — the examples following clarify the concept). Resolution: even if the examples scaffold the meaning, the term still gates the safety advice on first read.

**Outline-backed:** No — the safety principle is outline-backed; the term itself is not specified.

---

## Finding 5 — Part 5: `run_safely()` demo never exercises the failure path

**Severity:** `[confidence gap]` / `[lost why]`

**Location:** Part 5 demo cell + surrounding prose.

**Reviewers:** 3 of 5 — Anthropic, OpenAI, Grok. (Gemini and DeepSeek did not flag.)

**Quote:** `response = await run_safely(safe_agent, "What is the capital of France?")` — followed by the success output.

**What the student experiences:** They see the wrapper and a successful run, but the `except` block never triggers. They have the pattern but no concrete sense of when it would activate, what it catches that tool-level handling doesn't, or where it sits in a real app.

**Smallest fix:** Add one sentence after the printed response:

> *"This wrapper catches errors that happen outside any tool — an invalid API key, a rate-limit response, or a network timeout while the agent is calling the model. In a real app, it usually sits at your app boundary, where every user request passes through. If the API were unavailable, you'd see the friendly fallback message instead of the success above."*

**Blocks transfer:** Mixed — Anthropic said no (pattern is copyable), OpenAI and Grok said yes (placement and trigger conditions are unclear). The fix is small enough to apply regardless.

**Outline-backed:** Yes — outline calls for "API errors, rate limits, and network timeouts" and "wrapping `Runner.run()` for API-level failures."

---

## Singleton findings — included for completeness, lower priority

**Finding 6 (OpenAI only):** Part 2 intro could state *why* returned error strings matter — agents can reason over returned messages but cannot recover from a crashed run. Smallest fix: one sentence ("The agent can reason over a returned error message, but it can't recover from a run that already crashed."). Borderline — Finding 1's fix already implies this; may resolve together.

**Finding 7 (DeepSeek only):** Part 3 retry tool uses `print()` statements that students might copy into production code. Smallest fix: one line in the "Why This Works" cell noting prints are demo-only. Borderline style/comprehension — including only because it pairs naturally with Finding 3's fix in the same cell.

---

## Summary

Five real findings, all fixable with single-sentence additions. The two unanimous findings (1 and 2) are highest leverage — they fix gaps the entire reviewer panel independently identified. Findings 3 and 4 cluster in the same Part 3 cells and could be addressed together. Finding 5 has the most reviewer disagreement on whether it blocks transfer, but the fix is cheap. The notebook's structure, demos, and progression are not in question; this is a polish pass on five specific sentences.

---

# Individual Reviews

The grouped findings above are your primary working surface. The full individual reviews below are for verifying nuance.

---

## Anthropic Review

# Third-Pass Review — Notebook 08: Error Handling & Recovery

---

**Finding 1**

**Severity:** `[lost why]`

**Location:** Part 1 intro vs Part 2 intro — the framing whiplash between them

**Quote (Part 1):** *"We disable the SDK's default error handler with `failure_error_function=None` so tool exceptions crash the run — this makes the failure mode visible."*

**Quote (Part 2):** *"By default the SDK already converts tool exceptions into a generic error message for the model. Adding try/except inside the tool gives you control over that message…"*

**What the student experiences:** Part 1 sets up the lesson as "tools without error handling crash the agent." The student writes a mental note: *I need try/except or my agent crashes.* Then Part 2 says "actually the SDK already catches exceptions by default" and the motivation flips. The student is now unsure whether try/except is necessary at all or just nice-to-have, and whether their own real tools would crash without it (no — they wouldn't).

**Smallest fix:** Add one sentence to Part 1's intro stating what the default does, so Part 2 doesn't feel like a reversal:
> *"By default, the SDK catches tool exceptions and returns a generic error message to the model. We disable that default here with `failure_error_function=None` to show the raw exception — then in Part 2 we'll write proper handling that gives the agent something more useful than the generic default."*

**Blocks transfer:** Yes — the student walks away unsure whether unhandled exceptions actually break their real agents.

**Outline-backed:** Yes — outline calls for "What happens when a tool raises an exception" as the leading beat.

---

**Finding 2**

**Severity:** `[unexplained term]`

**Location:** Part 1, code cell — first appearance of `failure_error_function=None`

**Quote:** `@function_tool(failure_error_function=None)`

**What the student experiences:** A new `@function_tool` parameter appears that the student has never seen (Lesson 04 introduced the decorator with no parameters). With only the prose hint about "disabling a default error handler," the student doesn't know if this is something they'll set in real code, what other values it accepts, or whether it's a demo-only trick. They might copy it into their own tools thinking it's standard.

**Smallest fix:** One inline comment on the decorator line, e.g.:
```python
# Disables the SDK's default error handler — for this demo only;
# you would not set this on a real tool.
@function_tool(failure_error_function=None)
```

**Blocks transfer:** Yes — without this, a student copying the Part 1 pattern into their own project would propagate the demo flag and break their real error handling.

**Outline-backed:** No (outline doesn't call out this parameter), but the term lands without context.

---

**Finding 3**

**Severity:** `[confidence gap]`

**Location:** Part 5: Runner-Level Recovery — the demo cell

**Quote:** `response = await run_safely(safe_agent, "What is the capital of France?")`

**What the student experiences:** The wrapper is shown, but the demo input succeeds normally — the student never sees `run_safely()` actually catch a failure. They have the pattern but no proof of when it would activate. On their own project, they'd wonder: *what kind of error would land here vs. in the tool's try/except?* They can copy the wrapper but won't have intuition for when it's catching them something.

**Smallest fix:** One short sentence in the existing comment trail or the cell below the demo, naming a concrete trigger:
> *"This wrapper catches errors that happen outside any tool — for example, an invalid API key, a rate-limit response, or a network timeout while the agent is calling the model."*

**Blocks transfer:** No (the pattern is copyable as-is), but it weakens the student's confidence in *when* to rely on it.

**Outline-backed:** Yes — outline lists "API errors, rate limits, and network timeouts" as a teaching beat.

---

**Finding 4**

**Severity:** `[confidence gap]`

**Location:** Part 4: Fallback Strategy — `get_live_weather` tool

**Quote:** `return "Error: Weather API is currently unavailable"`

**What the student experiences:** The primary tool *returns* an error string rather than *raising* an exception, and the agent reads that string to decide to fall back. But Part 2 just taught that try/except inside a tool returns the error string, and Part 4 skips the try/except and returns the error string directly. The student doesn't get the connecting principle: *whether you raise or catch, the tool's job is to give the agent a clear, recognizable error message.* Without that principle named, the student writing their own fallback flow may try clever exception-routing or wonder why this tool has no try/except at all.

**Smallest fix:** One sentence right after the existing Part 4 intro, before the code cell:
> *"The convention from Part 2 holds here: a failing tool returns an error string instead of raising. The agent treats any string starting with `Error:` (or similar) as a signal to fall back."*

**Blocks transfer:** Yes — students writing their own fallback chains need to know the pattern is "return recognizable error strings," not exception routing.

**Outline-backed:** Yes — outline calls for "Fallback strategies — alternative tool, graceful failure."

---

**Summary:** Four findings. The notebook teaches the right patterns and the code is clear, but Parts 1, 4, and 5 each leave a small but specific question unanswered for a first-time reader. Findings 1 and 2 are the highest-leverage — they fix the same underlying gap (the student doesn't know the SDK's default behavior at the point they need to know it).

---

## OpenAI Review

**Severity:** [unexplained term]  
**Location:** Part 1 intro  
**Quote:** “We disable the SDK's default error handler with `failure_error_function=None` so tool exceptions crash the run — this makes the failure mode visible.”  
**What the student experiences:** The student understands Python exceptions, but `failure_error_function` appears as a new SDK concept with no plain-English definition. They can follow the demo mechanically, but not what this parameter actually controls or when they'd ever touch it themselves.  
**Smallest fix:** Add one short clause defining it in-place: “`failure_error_function` is the SDK hook that normally turns a tool exception into an error message for the model.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** Part 2 header/introduction  
**Quote:** “Adding try/except inside the tool gives you control over that message — so the agent can recover with useful, actionable information instead of a vague 'tool errored' string.”  
**What the student experiences:** They see that returning a string avoids a crash, but they still may not connect this to a real project need. The missing point is that agents can only react to tool results they can read; a raw exception gives the user a failure, while a clear returned message lets the agent explain next steps or choose an alternative path.  
**Smallest fix:** Add one sentence: “This matters because the agent can reason over a returned error message, but it can't recover from a run that already crashed.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Part 3, “Why This Works”  
**Quote:** “The retry loop is entirely inside the tool — the agent never knows a retry happened. In production, use exponential backoff with jitter instead of fixed waits.”  
**What the student experiences:** The demo works, but the jump to “exponential backoff with jitter” is too abrupt. A student new to agent reliability now has a named production pattern but no clue what to change in their own code beyond “not this.”  
**Smallest fix:** Add a short parenthetical definition: “(increase the wait after each failure, plus a small random offset so many clients don't all retry at once).”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Part 4 intro  
**Quote:** “The agent chooses the fallback through its instructions, but the tools still need to return errors in a form the agent can recognize.”  
**What the student experiences:** The student sees the pattern, but “a form the agent can recognize” is still vague. They may not know whether that means exceptions, special schemas, exact wording, or just a clear error string.  
**Smallest fix:** Clarify with one concrete phrase: “In this notebook, that just means returning a clear string like `Error: ...` instead of raising an exception.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** Part 5, Runner-level recovery demo  
**Quote:** “Wrap `Runner.run()` in a try/except as a safety net for API errors, rate limits, auth failures, and network issues.”  
**What the student experiences:** The student understands the Python wrapper, but the section stops just short of naming when to use it in a real app. Without that connection, it can feel like a generic defensive-programming trick rather than a standard boundary around every user-facing agent call.  
**Smallest fix:** Add one sentence after the intro or demo: “In a real app, this wrapper usually sits at your app boundary — the one place every user request passes through before you show a response.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

This notebook is otherwise in strong shape for a first-time agent student. The main issues are small teaching beats where one extra sentence would make the patterns easier to apply independently.

---

## Gemini Review

This notebook passed third-pass review with three findings. The core concepts are taught well, but a few key terms are left unexplained, and the benefit of one pattern isn't made explicit.

---

**Severity:** `[confidence gap]`
**Location:** Part 3: Retry Pattern, "Why This Works" cell
**Quote:** `In production, use exponential backoff with jitter instead of fixed waits.`
**What the student experiences:** "Okay, I understand the retry loop, but what are 'exponential backoff' and 'jitter'? I'm being told the demo pattern isn't what I should use in a real project, but I don't know how to implement the 'real' version."
**Smallest fix:** Add a short parenthetical explanation of the terms. Change the line to: "In production, use exponential backoff (waiting longer after each failure) with jitter (adding a small random delay) instead of fixed waits."
**Blocks transfer:** Yes
**Outline-backed:** No

---

**Severity:** `[unexplained term]`
**Location:** Part 3: Retry Pattern, Security note cell
**Quote:** `⚠️ **Security note:** Only retry read-only or idempotent actions.`
**What the student experiences:** "What does 'idempotent' mean? I can't apply this security warning if I don't understand this key term, and I'm worried I might duplicate actions by mistake."
**Smallest fix:** Add a brief definition to the note. Change the line to: "⚠️ **Security note:** Only retry read-only or idempotent actions (actions that have the same effect whether run once or multiple times)."
**Blocks transfer:** Yes
**Outline-backed:** No

---

**Severity:** `[lost why]`
**Location:** Part 4: Fallback Strategy (after the demo cell)
**Quote:** (This finding refers to a missing explanation after the demo)
**What the student experiences:** "I see that I can tell the agent to use a fallback tool in the instructions. But why is this better than just writing a Python function that tries the first tool and then calls the second one in an `except` block? I don't understand the unique benefit of this agent-based approach."
**Smallest fix:** Add a `### 💡 Why This Works` cell after the demo cell with this text: "The fallback logic lives in the instructions, not in Python code. This lets you change the agent's recovery strategy just by changing the prompt, making its behavior more flexible and easier to update as you add or change tools."
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Grok Review

**Severity:** `[unexplained term]`

**Location:** Part 1 markdown cell (right before the fragile_agent code)

**Quote:** "We disable the SDK's default error handler with `failure_error_function=None` so tool exceptions crash the run — this makes the failure mode visible. In Part 2, we'll remove the flag and handle errors properly inside the tool."

**What the student experiences:** I've just finished Lesson 07 on structured outputs and suddenly there's this `failure_error_function` parameter I've never seen. I don't know what the SDK's "default error handler" is, whether I should ever touch it in my own projects, or why it's only on the fragile version. It feels like insider SDK knowledge that wasn't introduced.

**Smallest fix:** Replace the sentence with: "By default the SDK catches tool exceptions and returns a generic message to the model. For this demo only we pass `failure_error_function=None` so the raw exception crashes the run and we can see the failure mode clearly."

**Blocks transfer:** Yes

**Outline-backed:** No (outline lists tool exceptions and try/except patterns but does not call for teaching this SDK parameter)



**Severity:** `[confidence gap]`

**Location:** Part 5 ("Runner-Level Recovery") demo cell + following prose

**Quote:** "response = await run_safely(safe_agent, \"What is the capital of France?\")" followed by the success output and the note "In production, log the full error for debugging while returning a safe user-facing message."

**What the student experiences:** The wrapper looks useful but the demo never triggers the except block, so I only see the happy path. I don't have a mental model of what an actual API/rate-limit failure looks like or how often I'd reach for `run_safely()` in my own code versus just letting the exception bubble up.

**Smallest fix:** After the printed response, add one sentence: "If the API were unavailable or rate-limited, you would instead see the friendly fallback message and the error would be logged for debugging."

**Blocks transfer:** Yes

**Outline-backed:** Yes (outline explicitly calls for "Wrapping `Runner.run()` for API-level failures")



**Severity:** `[unexplained term]`

**Location:** Security note cell after the retry "Why This Works"

**Quote:** "Only retry read-only or idempotent actions. Tools that create orders, send messages, or write records may have partially succeeded — retrying risks duplication."

**What the student experiences:** The warning makes sense for "read-only" but "idempotent" is completely new terminology to me. I don't know how to decide whether one of my own tools is idempotent, so the advice doesn't translate to my projects yet.

**Smallest fix:** Change to: "Only retry read-only or idempotent actions (operations that can safely run multiple times with the same effect)."

**Blocks transfer:** Yes

**Outline-backed:** Yes (outline calls for "Retrying safely without duplicating actions")



**Severity:** `[lost why]`

**Location:** Part 4 intro markdown (right before the two weather tools)

**Quote:** "When a primary tool fails, a fallback tool provides a degraded but still useful response.

The agent chooses the fallback through its instructions, but the tools still need to return errors in a form the agent can recognize."

**What the student experiences:** I see that the live tool returns an error string and the instructions mention "if get_live_weather fails or returns an error", but I don't understand why this particular string format matters or how the agent distinguishes a real error from a normal response that happens to contain the word "Error". The "so what" for real projects stays fuzzy.

**Smallest fix:** Append one sentence: "By returning a clear error string, we give the agent something it can reliably detect and act on per the instructions."

**Blocks transfer:** No (the pattern is still visible in the code)

**Outline-backed:** Yes



This notebook is otherwise very clear. The progression from "watch it crash" → tool-level handling → retry → fallback → runner wrapper gives a motivated student a solid mental model. With the four small additions above, a student coming straight from Lesson 07 would finish feeling they could apply these patterns independently.

---

## DeepSeek Review

## Third-Pass Review: Notebook 08 — Error Handling & Recovery

This is a well-constructed notebook. The pacing is logical, the demos are clear, and the connections between Parts are explicit. However, a few moments may leave a student who is new to agents with minor comprehension gaps or uncertainty about how to apply these patterns independently.

---

### Finding 1

**Severity:** `[unexplained term]`

**Location:** Part 1, code cell after `## 💥 Part 1: What Happens Without Error Handling`

**Quote:**
```
@function_tool(failure_error_function=None)
def lookup_product_fragile(product_id: str) -> str:
```

**What the student experiences:**
The student sees a parameter they've never encountered (`failure_error_function=None`) with only a brief prose explanation that it "disables the SDK's default error handler." They don't know what the default handler does, why it exists, or what would have happened without this flag. A few cells later, Part 2 reveals the default behavior, but in the moment the student may feel like they missed something important.

**Smallest fix:**
Add one sentence to the markdown cell immediately before this code cell:

```
The SDK normally converts tool exceptions into a generic error message the agent can read.
We disable that here so the exception crashes the run — making the failure mode visible.
```

**Blocks transfer:** No — the student doesn't need this parameter in their own work; they'll handle errors inside the tool instead.

**Outline-backed:** No — the Course Outline does not specify teaching this SDK parameter.

---

### Finding 2

**Severity:** `[confidence gap]`

**Location:** Part 3, code cell for `fetch_with_retry`

**Quote:**
```
@function_tool
def fetch_with_retry(query: str) -> str:
    ...
    for attempt in range(1, max_attempts + 1):
        try:
            result = unreliable_api_call(query)
            print(f"    ✅ Succeeded on attempt {attempt}")
            return result
        except ConnectionError as e:
            print(f"    ⚠️  Attempt {attempt} failed: {e}")
            if attempt < max_attempts:
                time.sleep(wait_seconds)
```

**What the student experiences:**
The demo works, but the student sees `print()` statements inside the tool that reveal internal retry logic. A novice might copy this pattern into production code, where printing inside a tool would leak implementation details to the user. Additionally, the demo uses a fixed 1-second wait; the student may not know how to adapt this to a real API call with exponential backoff.

**Smallest fix:**
Add one sentence to the `Why This Works` cell after the retry demo:

```
In a real application, remove the print statements and use exponential backoff with jitter
instead of a fixed wait — the pattern inside the loop is what matters.
```

**Blocks transfer:** Yes — the student may not recognize that the `print()` calls and fixed `time.sleep()` are demo-only choices, not production defaults.

**Outline-backed:** Yes — the outline calls for "Retry patterns with limits," and the student needs to know how to apply it correctly in their own project.

---

### Finding 3

**Severity:** `[confidence gap]`

**Location:** Part 4, code cell for fallback agent and the demo cell that follows

**Quote:**
```
instructions = (
    "Get weather information using get_live_weather.\n"
    "If get_live_weather fails or returns an error, use get_weather_forecast as a fallback.\n"
    ...
)
```

**What the student experiences:**
The demo works smoothly — the agent uses the cached forecast when the primary tool fails. But the student may not understand *how* the agent decides to switch tools. They see `get_live_weather` return an error string, and the agent obediently calls `get_weather_forecast`. The notebook never explicitly states that the agent reads the tool's output string and acts on it according to the instructions. A student might think the fallback is automatic at the SDK level, or they might not realize that the tool's return value is what drives the decision.

**Smallest fix:**
Add one sentence to the `Why This Works` cell (or as a standalone note after the demo):

```
The agent reads the tool's output string and follows the instructions — it chose the
fallback because the primary tool returned an error, not through any automatic mechanism.
```

**Blocks transfer:** Yes — if the student doesn't understand the mechanism, they won't know how to write effective fallback instructions or how to format tool error messages for the agent to parse.

**Outline-backed:** Yes — the outline lists "Fallback strategies — alternative tools and graceful degradation," and the mechanism is the key teaching point.

---

### Finding 4

**Severity:** `[unexplained term]`

**Location:** Part 3, security note after the retry demo

**Quote:**
```
⚠️ **Security note:** Only retry read-only or idempotent actions. Tools that create orders,
send messages, or write records may have partially succeeded — retrying risks duplication.
```

**What the student experiences:**
The term "idempotent" appears without definition. Some Python developers may know it, but the course does not assume it (it was not taught in earlier lessons). A student who doesn't know the word may lose confidence or ignore the note entirely, missing the critical safety message.

**Smallest fix:**
Add a parenthetical definition: "idempotent (safe to repeat — same result every time)"

**Blocks transfer:** No — the concept is explained through the examples that follow. The definition just removes the vocabulary barrier.

**Outline-backed:** No — the outline does not mandate teaching the term "idempotent" specifically, but the safety principle is outline-backed.

---

### Summary

The notebook has no major comprehension blockers. The four findings above are small gaps that a motivated student could work through, but each has a one-sentence fix that would make the lesson more self-contained and build student confidence in applying these patterns independently.
