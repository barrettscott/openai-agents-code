# Review Report: Real-World MCP Servers
Generated: 2026-05-10 | Notebook: /Users/scott/Dropbox/Notebooks/openai-agents/Week_5_MCP_And_Production/28_Real_World_MCP_Servers.ipynb
Models: Anthropic (claude-opus-4-7 (CLI/Max)) · OpenAI (gpt-5.4) · Gemini (gemini-2.5-pro) · Grok (grok-4.20-0309-reasoning) · DeepSeek (deepseek-reasoner) · Dedup (claude-opus-4-7 CLI/Max)

---

# Grouped Findings: Real-World MCP Servers

# Third-Pass Synthesis — Notebook 28: Real-World MCP Servers

Five reviewers ran independent third-pass reviews. Strong consensus on two findings (5/5 and 4/5), with three single-reviewer findings worth considering. The notebook is in good shape overall — earlier passes did their job. The remaining gaps are narrow and fixable with one or two sentences plus possibly one short code addition.

---

## Finding 1 — Part 5 promises an actionable skill but delivers only prose

**Severity:** `[confidence gap]`

**Location:** Part 5: Testing MCP Servers Before Trusting Them — the "Test a tool directly (outside an agent)" bullet

**Reviewers:** All 5 (Anthropic, OpenAI, Gemini, Grok, DeepSeek)

**Quote:** `"Test a tool directly (outside an agent) before putting it in mcp_servers=[]"`

**What the student experiences:** "Good advice, but how? Every demo has put servers in `mcp_servers=[]` and let the agent call tools. I've seen `list_tools()` but I've never seen a tool actually invoked outside an agent. I can't follow this advice on my own project because I don't know the syntax — and Part 5 is entirely prose, no code at all." DeepSeek and Grok both note Part 5 sets an expectation of actionable knowledge but delivers only bullet points.

**Smallest fix:** Reviewers proposed two flavors — pick one:
- **Inline syntax (smallest):** Reword the bullet to show the call shape: *"Test a tool directly with `await server.call_tool('read_file', {'path': '...'})` before putting it in `mcp_servers=[]`."* OpenAI/Grok variant: add a sentence after the checklist showing the preflight — connect → `list_tools()` → optionally invoke one tool → then add to `Agent`.
- **Tiny demo cell (Gemini/DeepSeek preference):** Add one short code cell in Part 5 showing a direct `await server.call_tool(...)` (or equivalent) on the already-running filesystem server, so the testing pattern is concrete.

Note: the exact method name (`call_tool` vs `run_tool`) needs to be verified against the SDK before adding — reviewers used different names.

**Blocks transfer:** Yes (4/5 reviewers say yes; Anthropic says no only because they offered a softer alternative fix)

**Outline-backed:** Yes — outline explicitly lists "Testing MCP tools before trusting them in an agent"

---

## Finding 2 — Tool filtering: students can copy the snippet but don't grasp when/why to reach for it

**Severity:** `[confidence gap]` / `[lost why]` (reviewers split — see below)

**Location:** Part 4: Tool Filtering — demo cell and surrounding intro

**Reviewers:** 4 (OpenAI, Gemini, Grok, DeepSeek)

**Quote:** `"Use create_static_tool_filter to allowlist specific tools"` and the demo cell using `tool_filter=create_static_tool_filter(...)`

**What the student experiences:** Three related-but-distinct gaps surfaced:
- **Adaptation (OpenAI):** "I can copy this exact snippet, but if a different MCP server has different tool names, I won't know what to put in `allowed_tool_names`."
- **Proof it works (Gemini):** "The demo only shows the agent doing safe reads. I never see the filter actually *block* a write attempt, so I'm not confident it's a real guardrail."
- **When to use it (DeepSeek, Grok):** "I already scoped the filesystem to a workspace directory. Why do I need tool filtering on top? When does this matter versus just trusting the server or writing tighter instructions?"

These are three facets of the same comprehension hole: students see *what* the filter does but not *why/when* or *how to adapt*.

**Smallest fix:** Two one-line additions cover all three concerns:
1. Before the demo: *"Scoping the directory limits *which files* the agent can reach; filtering tools limits *what the agent can do* with those files — two complementary safety layers. This is safer than instructions alone: even if a later prompt says 'delete everything,' the write tools are invisible to the agent."* (DeepSeek + Grok)
2. After `tools = await readonly_fs.list_tools()`: *"In your own project, call `list_tools()` first to see the exact tool names, then put only the ones you want into `allowed_tool_names`."* (OpenAI)

Gemini's stronger fix — adding a follow-up `Runner.run` that attempts a disallowed write and shows the agent refusing — would also resolve the "proof" concern but adds a second model call. **Recommend the two one-liners; skip the second `Runner.run` unless you want the demo to also serve as proof.**

**Blocks transfer:** Yes (3/4 reviewers say yes; DeepSeek says no — student can use the API, just won't know when to reach for it)

**Outline-backed:** Yes — outline calls out "Reasoning about server permissions — when not to expose a whole filesystem"

---

## Finding 3 — `uvx` introduced as a prerequisite without saying what it is

**Severity:** `[unexplained term]`

**Location:** Setup → Check Prerequisites cell

**Reviewers:** 2 (Anthropic, Gemini)

**Quote:** `"Check uvx (for web fetch MCP server)"` and `"For uvx: run 'pip install uv' in your terminal, then restart JupyterLab"`

**What the student experiences:** "I know `pip` and `npx`. The cell tells me to install `uv` to get `uvx` — but never says what either is or why this server uses it instead of plain Python. I'll install it because the cell told me to, but I won't know what I just added to my environment." Notebook 27 explained `npx` similarly; the parallel explanation for `uvx` is missing.

**Smallest fix:** Add one comment or sentence near the prerequisite check: *"`uvx` is a Python package runner from the `uv` toolchain — the Python equivalent of `npx`. The web fetch MCP server ships as a Python package and uses it as its runtime."*

**Blocks transfer:** No

**Outline-backed:** No — outline names the web fetch server but not the runtime explanation

---

## Single-reviewer findings worth a quick decision

These were each flagged by one reviewer only. Listed for Scott to accept or reject; none are clearly wrong, but consensus is weaker.

### 3a — `create_static_tool_filter` import lands without a gloss

**Severity:** `[unexplained term]` · **Reviewer:** Grok (1) · **Location:** top imports + Part 4 header

A student sees `from agents.mcp import ... create_static_tool_filter` in the imports and meets it as a "magic SDK helper." Grok suggests a one-line gloss at the Part 4 header: *"`create_static_tool_filter(allowed_tool_names=[...])` tells the SDK to hide any tools not on your list — the agent literally cannot see or call them."*

**Recommendation:** This naturally folds into the Finding 2 fix. If you add the "two complementary layers" sentence, also add Grok's gloss in the same paragraph. Cheap.

**Blocks transfer:** Yes (per Grok) · **Outline-backed:** Yes

---

### 3b — Why use the web fetch *MCP server* instead of `requests` + `@function_tool`?

**Severity:** `[lost why]` · **Reviewer:** Gemini (1) · **Location:** Part 2, after Web Fetch demo

A student who has seen `@function_tool` in earlier notebooks may wonder why MCP is better than rolling their own. Gemini suggests one short markdown cell: *"The advantage of using an MCP server for web fetching is that it's a pre-built, sandboxed component. You don't need to add `requests`/`beautifulsoup` to your project or write boilerplate for HTTP and HTML parsing — the server returns clean markdown ready for the agent."*

**Recommendation:** Worth adding only if the broader "why MCP at all" theme isn't already paid off in Part 3 (Anthropic's finding 3c below partly covers this). If Part 3's "compose servers someone else wrote" framing is added, this Part 2 question is largely answered. Optional.

**Blocks transfer:** Yes (per Gemini) · **Outline-backed:** Yes

---

### 3c — Part 3 (combining servers) demonstrates the headline MCP benefit but never names it

**Severity:** `[lost why]` · **Reviewer:** Anthropic (1) · **Location:** Part 3 opening

The demo works; the student sees "you can pass two servers." The actual lesson — *MCP lets you compose capabilities from independently-built servers with zero glue code* — is implied but never stated.

**Smallest fix:** One sentence in the Part 3 intro: *"This is the payoff of the MCP standard — you can give an agent local file access and web access by composing two servers someone else wrote, with zero integration code on your side."*

**Recommendation:** Add. Cheapest possible "why" fix and it pulls double duty by partially answering 3b.

**Blocks transfer:** No · **Outline-backed:** Yes

---

### 3d — "Adversarial text" is too soft for a beginner

**Severity:** `[unexplained term]` · **Reviewer:** OpenAI (1) · **Location:** Part 5 security bullet

OpenAI suggests changing *"adversarial text"* to *"text designed to manipulate the agent (prompt injection)"* — names the threat model concretely.

**Recommendation:** Trivial wording change; accept.

**Blocks transfer:** No · **Outline-backed:** Yes

---

### 3e — Part 5 lacks a one-line orienting opener

**Severity:** `[lost thread]` · **Reviewer:** OpenAI (1) · **Location:** Part 5 header

OpenAI suggests one sentence under the header to frame Part 5 as a reusable preflight: *"Use this as your default preflight sequence whenever you try a new MCP server: connect, inspect `list_tools()`, test the server in isolation, then give it to an agent."*

**Recommendation:** This overlaps heavily with Finding 1's fix. If you take the "inline syntax" version of Finding 1, this opener does additional work. If you take the "tiny demo cell" version, this opener becomes redundant. Skip unless going with the inline-syntax fix.

**Blocks transfer:** No · **Outline-backed:** Yes

---

## Recommended action set (minimal)

If Scott wants the smallest viable patch:

1. **Finding 1** — pick one form (inline syntax vs. tiny demo cell). Verify the SDK method name first.
2. **Finding 2** — two one-liners (the "two layers" framing + the `list_tools()`-first adaptation hint).
3. **Finding 3** — one-sentence `uvx` gloss in the prerequisite cell.
4. **3a** — fold the `create_static_tool_filter` gloss into the Finding 2 paragraph.
5. **3c** — one sentence in Part 3 intro naming the compose-without-glue payoff.
6. **3d** — replace "adversarial text" with "text designed to manipulate the agent (prompt injection)."

That's roughly five sentences and one optional code cell. 3b and 3e can be dropped.

---

# Individual Reviews

The grouped findings above are your primary working surface. The full individual reviews below are for verifying nuance.

---

## Anthropic Review

# Third-Pass Review — Notebook 28: Real-World MCP Servers

Three findings. The notebook is mostly student-friendly — it builds cleanly on Lesson 27 and the demos are concrete. The issues below are narrow gaps where a student would silently lose confidence.

---

**Severity:** `[unexplained term]`

**Location:** Setup → Check Prerequisites cell

**Quote:** `"Check uvx (for web fetch MCP server)"` and `"For uvx: run 'pip install uv' in your terminal, then restart JupyterLab"`

**What the student experiences:** "What is `uvx`? I know `pip` and `npx`. The cell tells me to install `uv` to get `uvx` — but never says what either is or why this server uses it instead of plain Python. I'll install it because the cell told me to, but I won't know what I just added to my environment."

**Smallest fix:** Add one sentence in the Check Prerequisites markdown intro (or as a comment in the cell): *"`uvx` is a Python package runner from the `uv` toolchain — the Python equivalent of `npx`. We use it because the web fetch MCP server ships as a Python package."*

**Blocks transfer:** No

**Outline-backed:** No (outline names the web fetch server but not the runtime explanation)

---

**Severity:** `[confidence gap]`

**Location:** Part 5: Testing MCP Servers Before Trusting Them

**Quote:** `"Test a tool directly (outside an agent) before putting it in mcp_servers=[]"`

**What the student experiences:** "Good advice — but how? Every demo so far has put the server in `mcp_servers=[]` and let the agent call tools. I've seen `list_tools()` but never seen a tool actually invoked outside an agent. I can't follow this advice on my own project because I don't know the syntax."

**Smallest fix:** Either show the call shape inline in that bullet (e.g., *"Test a tool directly with `await server.call_tool('read_file', {'path': '...'})` before putting it in `mcp_servers=[]`"*) or, if the bullet is meant only conceptually, soften it to *"Verify the tool list before wiring the server into an agent — `list_tools()` is enough to confirm the server is healthy and exposing what you expect."*

**Blocks transfer:** Yes

**Outline-backed:** Yes (outline lists "Testing MCP tools before trusting them in an agent")

---

**Severity:** `[lost why]`

**Location:** Part 3: Combining Multiple MCP Servers — opening cell

**Quote:** `"Pass multiple servers to mcp_servers=[]. The agent aggregates all tools from all servers and uses them together."`

**What the student experiences:** The demo works, but the student sees this as a mechanical "you can pass two servers" point. The actual lesson — that MCP lets you compose *capabilities from independently-built servers* without any glue code, which is the headline benefit of the standard — is implied by the demo but never named.

**Smallest fix:** Add one sentence to the Part 3 intro: *"This is the payoff of the MCP standard — you can give an agent local file access and web access by composing two servers someone else wrote, with zero integration code on your side."*

**Blocks transfer:** No

**Outline-backed:** Yes (outline lists "Combining multiple MCP servers in one agent" as a teaching beat)

---

**Summary:** Three small additions — one term gloss, one syntactic example, one "why this matters" sentence — would close the only gaps a sequential student is likely to hit. Nothing structural needs to move.

---

## OpenAI Review

**Severity:** [lost why]  
**Location:** Part 4: Tool Filtering  
**Quote:** "MCP servers can expose many tools. Sometimes you only want to give the agent access to a subset — for safety, focus, or simplicity. Use `create_static_tool_filter` to allowlist specific tools."  
**What the student experiences:** The student sees that filtering exists, but not when they would reach for it in a real project instead of just trusting the full server. The demo works, but the practical decision rule is still fuzzy.  
**Smallest fix:** Add one sentence before the demo: "This matters when a server exposes both safe read tools and risky write/delete tools — filtering lets you reuse the same server while giving each agent only the capabilities it actually needs."  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Part 4: Tool Filtering demo  
**Quote:** `tool_filter=create_static_tool_filter(`  
**What the student experiences:** The student can copy this exact snippet, but may not know what to change when a different MCP server exposes different tool names. They can follow the demo, but not confidently adapt it.  
**Smallest fix:** Add a brief note after `tools = await readonly_fs.list_tools()`: "In your own project, first call `list_tools()` on the unfiltered server to see the exact tool names, then put only the ones you want into `allowed_tool_names`."  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Part 5: Testing MCP Servers Before Trusting Them  
**Quote:** "- Test a tool directly (outside an agent) before putting it in `mcp_servers=[]`"  
**What the student experiences:** This is useful advice, but it stays abstract. A beginner with MCP now knows they *should* test directly, but not what "directly" means in practice or what code shape to write.  
**Smallest fix:** Add one short sentence after the checklist: "In practice, that means connecting to the server, calling `list_tools()`, and optionally invoking a tool yourself before involving an `Agent`."  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [unexplained term]  
**Location:** Part 5: Testing MCP Servers Before Trusting Them  
**Quote:** "- Treat all tool-returned content as untrusted input — web pages, file contents, and database results can all contain adversarial text"  
**What the student experiences:** "Adversarial text" is likely understandable in a vague way, but not precise enough for a student new to agent security in the MCP context. They may not connect it concretely to "text that tries to manipulate the agent."  
**Smallest fix:** Change "adversarial text" to "text designed to manipulate the agent (prompt injection)" or add that phrase in parentheses.  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [lost thread]  
**Location:** Part 5: Testing MCP Servers Before Trusting Them  
**Quote:** "## 🔍 Part 5: Testing MCP Servers Before Trusting Them"  
**What the student experiences:** The notebook promises testing MCP tools before trusting them, but this section is only a checklist plus failure modes after several demos. By this point, the student has seen multiple working patterns but still hasn't been shown a crisp reusable "preflight" habit they could apply first on their own.  
**Smallest fix:** Add a one-sentence opener under the Part header: "Use this as your default preflight sequence whenever you try a new MCP server: connect, inspect `list_tools()`, test the server in isolation, then give it to an agent."  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

## Gemini Review

This notebook passed third-pass review with several opportunities to improve student comprehension and confidence. While the core concepts are demonstrated clearly, a few small additions would better connect the "what" to the "why" and ensure students can apply these patterns independently.

---

**Severity:** `[unexplained term]`
**Location:** Part 1, Setup, Prerequisites check cell
**Quote:**
```python
# Check uvx (for web fetch MCP server)
try:
    uvx_v = subprocess.check_output(["uvx", "--version"], text=True).strip()
    checks["uvx"] = (True, uvx_v)
except FileNotFoundError:
    checks["uvx"] = (False, "not found — install with: pip install uv")
```
**What the student experiences:** "Okay, I need `npx` for the filesystem server and this new thing `uvx` for the web fetch server. What is `uvx`? Is it another package manager like `npx`? I'm being told to install a tool without any context for what it is."
**Smallest fix:** Add a one-line comment explaining what `uv` is, just like the previous notebook did for `npx`. For example: `uvx is a command runner from the 'uv' package, a fast Python package manager and installer. The web fetch MCP server uses it as its runtime.`
**Blocks transfer:** No
**Outline-backed:** No

---

**Severity:** `[lost why]`
**Location:** Part 2, after the Web Fetch MCP Server demo
**Quote:** The entire demo cell for the Web Fetch server.
**What the student experiences:** "This is cool, the agent can fetch a web page. But I could have written a Python function with the `requests` library and decorated it with `@function_tool` to do the same thing. Why is using an MCP server for this better? What problem does this solve for me?"
**Smallest fix:** Add a short markdown cell after the demo explaining the benefit. For example: "The advantage of using an MCP server for web fetching is that it's a pre-built, sandboxed component. You don't need to add `requests` or `beautifulsoup` to your project dependencies or write the boilerplate for handling HTTP requests and parsing HTML—the server does it for you, returning clean markdown ready for the agent to use."
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`
**Location:** Part 4, Tool Filtering demo
**Quote:** The code cell demonstrating `create_static_tool_filter`.
**What the student experiences:** "The demo shows that the agent can use the allowed tools, which makes sense. But what happens if I ask it to do something that requires a *disallowed* tool, like writing a file? The whole point of this is safety, but I haven't seen the filter actually block anything. I'm not confident it works as a guardrail."
**Smallest fix:** After the successful `Runner.run` call, add a second one that attempts a disallowed action and print the agent's response. For example:
```python
# --- Add this block to the end of the cell ---
print("\\n--- ATTEMPTING DISALLOWED ACTION ---")
result_fail = await Runner.run(agent, input="Now, please create a new file named test.txt with the content 'hello'.")
print(f"Agent response: {result_fail.final_output}")
```
This would show the agent correctly stating it cannot perform the action because it lacks the necessary tool, proving the filter works.
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`
**Location:** Part 5, Testing MCP Servers Before Trusting Them
**Quote:**
```
- Test a tool directly (outside an agent) before putting it in mcp_servers=[]
```
**What the student experiences:** "This is a great piece of advice, but how do I actually do it? I've only seen the agent call tools. I don't know the syntax for calling a tool directly from the server object. I can't follow this advice without seeing an example."
**Smallest fix:** Add a small code cell to Part 5 demonstrating a direct tool call. This would be a powerful debugging pattern to teach. Example:
```python
### 💡 Testing a Tool Directly

# Connect to the server
async with MCPServerStdio(...) as fs_server:
    
    # Call a tool directly using server.run_tool()
    print("Directly calling 'list_directory' tool:")
    tool_result = await fs_server.run_tool("list_directory", {"path": "."})
    print(tool_result)
```
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Grok Review

**Third-Pass Review — Student Comprehension**

This notebook is in **excellent shape**. A student coming straight from Notebook 27 would follow the progression cleanly, the demos build directly on the fundamentals they just learned (`MCPServerStdio`, `mcp_servers=[...]`, `cache_tools_list=True`, `list_tools()`), and the new material (web fetch, combining servers, filtering) is scoped appropriately. The security notes land at the right moments, the “why” for combining servers is visible in the final demo, and the pacing stays reasonable.

I only found **three** genuine comprehension blockers for a motivated first-time agent builder. All are small and fixable with one-sentence additions.

---

**Severity:** `[unexplained term]`

**Location:** Top import cell + Part 4 header + code cell

**Quote:** `from agents.mcp import MCPServerStdio, create_static_tool_filter`  
and  
"Use `create_static_tool_filter` to allowlist specific tools."

**What the student experiences:** I just learned in the previous notebook that I pass whole servers to `mcp_servers=[server]`. Now there’s this new `create_static_tool_filter` thing and a `tool_filter=` parameter I’ve never seen. I don’t know what “static” means here or why I’d ever want to filter tools instead of just letting the server expose everything. It feels like a magic SDK helper I’m supposed to already understand.

**Smallest fix:** After the Part 4 header, add one sentence:  
"`create_static_tool_filter(allowed_tool_names=[...])` tells the SDK to hide any tools not on your list — the agent literally cannot see or call them."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`

**Location:** Part 5 (“Testing MCP Servers Before Trusting Them”) – the entire checklist markdown cell

**Quote:** "**Checklist:** … - Test a tool directly (outside an agent) before putting it in `mcp_servers=[]` …"

**What the student experiences:** The checklist tells me this is important, but the notebook never shows *how* to test a tool directly. After the combined demo I feel like I understand the happy path, but I have no concrete pattern I could copy into my own project when I want to sanity-check a new MCP server before trusting it in production.

**Smallest fix:** After the bullet “Test a tool directly (outside an agent)…”, add:  
"For example, after connecting you can run `tools = await server.list_tools()` and then call a tool manually to see its raw output before giving it to an `Agent`."

**Blocks transfer:** Yes

**Outline-backed:** Yes (outline explicitly lists “Testing MCP tools before trusting them in an agent”)

---

**Severity:** `[lost why]`

**Location:** Part 4 demo cell (right after the filtered agent runs)

**Quote:** `result = await Runner.run(agent, input="List the files and read customer_feedback.txt")`

**What the student experiences:** I see that the tool list is now shorter, but the demo only asks the agent to do safe read operations. I don’t get a clear “aha” moment showing that the filter actually *prevents* dangerous actions even if my instructions or user prompt tried to trigger them. So I’m not sure when I would reach for this pattern in my own work versus just writing tighter instructions.

**Smallest fix:** After `print(f"\nAgent response: {result.final_output}")`, add one sentence:  
"This is safer than instructions alone — even if the prompt later says 'delete everything', the write tools are invisible to the agent."

**Blocks transfer:** Yes

**Outline-backed:** Yes (outline calls out “Reasoning about server permissions” and the safety theme)

---

These three tiny additions would remove the only points where I (as a student who just finished Notebook 27) would have quietly wondered “wait, how/why do I actually use this?” The rest of the notebook feels solid and ready.

---

## DeepSeek Review

Now I'll review the notebook as a student who just completed Notebook 27 (MCP Fundamentals).

**Student's known concepts from N-27:**
- `MCPServerStdio`, `cache_tools_list`, `async with` connection lifecycle
- MCP server exposes tools automatically via `list_tools()`
- Pass `mcp_servers=[server]` to the agent
- Filesystem server scoped to a directory
- Security note on filesystem write/delete access

---

## Finding 1

**Severity:** `[confidence gap]`

**Location:** Part 5 — "Testing MCP Servers Before Trusting Them" (the checklist markdown cell and the "Common failure modes" cell)

**Quote:** "Test a tool directly (outside an agent) before putting it in `mcp_servers=[]`" and "If a tool behaves unexpectedly, check the server logs before assuming a model issue"

**What the student experiences:** The entire Part 5 is prose — no code, no demo. The checklist tells me to "test a tool directly" but I've never seen the code for that. I know `list_tools()` from N-27, but what does "test a tool directly" look like? Do I call a method on the server object? Do I run the agent with a tiny prompt? The instruction is aspirational — I agree I *should* test, but after this section I couldn't write the test. Similarly, "check the server logs" — what logs? Where would I find them? The notebook sets an expectation of actionable knowledge but only delivers bullet points.

**Smallest fix:** Replace the "Test a tool directly" bullet with an actionable pattern the student can copy. For example: "Run a minimal agent with a single test prompt to verify the tool returns what you expect — this isolates tool issues from agent reasoning issues." Or add one short code cell showing the testing pattern inline with the demo flow.

**Blocks transfer:** Yes — the student cannot independently test an MCP server after this section despite the outline promising this skill.

**Outline-backed:** Yes — the outline explicitly calls for "Testing MCP tools before trusting them in an agent" and "Common MCP failure modes — debugging tool discovery and flaky servers."

---

## Finding 2

**Severity:** `[lost why]`

**Location:** Part 4 — "Tool Filtering"

**Quote:** "Sometimes you only want to give the agent access to a subset — for safety, focus, or simplicity. Use `create_static_tool_filter` to allowlist specific tools."

**What the student experiences:** I already learned in N-27 and Part 1 of this notebook to scope the filesystem server to a dedicated workspace directory. That limits *which files* the agent can reach. Now I'm told to also filter tools. I could reasonably think: "Why both? Doesn't scoping the directory already prevent the agent from touching anything outside it?" The demo shows *what* tool filtering does but never answers *why I'd need it on top of directory scoping*. The connection between the two safety layers is never stated, so I walk away knowing there's an API but not when I should reach for it on my own.

**Smallest fix:** Add one sentence before or after the demo: "Scoping the directory limits *which files* the agent can reach; filtering the tool list limits *what the agent can do* with those files — two complementary layers of control."

**Blocks transfer:** No — I can still use the API correctly, but I might not know when the extra layer matters.

**Outline-backed:** Yes — "Reasoning about server permissions — when not to expose a whole filesystem" is in the outline. Tool filtering is a concrete implementation of this principle; the "why" connects the two.

---

**Summary:** This notebook is well-structured and moves at a good pace through Parts 1–4. The two issues are (1) Part 5 promises an actionable skill (testing MCP servers) but delivers only prose, and (2) the tool filtering section misses the one-sentence bridge that tells the student why it adds value beyond directory scoping. Both are small fixes.
