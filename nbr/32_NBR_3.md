# Review Report: Deploying Agents with Gradio
Generated: 2026-05-10 | Notebook: /Users/scott/Dropbox/Notebooks/openai-agents/Week_5_MCP_And_Production/32_Deploying_With_Gradio.ipynb
Models: Anthropic (claude-opus-4-7 (CLI/Max)) · OpenAI (gpt-5.4) · Gemini (gemini-2.5-pro) · Grok (grok-4.20-0309-reasoning) · DeepSeek (deepseek-reasoner) · Dedup (claude-opus-4-7 CLI/Max)

---

# Grouped Findings: Deploying Agents with Gradio

# Third-Pass Synthesis — Notebook 32: Deploying Agents with Gradio

Five reviewers ran third-pass review. Four produced findings; **DeepSeek** returned no findings, arguing that streaming events were introduced in Lesson 30 and each Part introduces only one new concept. The other four reviewers disagreed in specific places — those disagreements are surfaced below rather than collapsed.

The findings cluster around two areas: **Part 2's history snippet**, **Part 3's streaming event classes**, and **Part 6's deployment beat** (the lesson's payoff and the weakest part of the notebook for first-time HF Spaces users).

---

## Finding 1 — Hugging Face Spaces deployment is hand-waved

**Severity:** `[confidence gap]`
**Location:** Part 6: Deploying to Hugging Face Spaces
**Reviewers:** Anthropic, OpenAI, Gemini (3 of 5). DeepSeek explicitly considered and dismissed this ("concept-level notebook, student can use HF docs"). Grok did not flag.
**Quote:** "A Hugging Face Space is a free hosted environment for Gradio apps. Create a Space, push your files, and your agent gets a public URL."
**What the student experiences:** Three reviewers converge on the same gap from different angles — Gemini: "What is Hugging Face? Is it a company, a product, a community?"; Anthropic: "sign up where? click what? use git or drag files?"; OpenAI: "what exactly do I copy from this notebook into `app.py`?". The deployed agent is the headline outcome of the whole course, and the actual deployment workflow is skipped.
**Smallest fix:** Two short additions to the Part 6 intro:
- One sentence framing HF: "Hugging Face is a central platform for the AI community; a Space is a free hosted environment for Gradio apps."
- 3–4 bullets of concrete workflow: sign in at `huggingface.co` → "Create new Space" → choose Gradio SDK → upload files in the web UI or `git push` to the Space's repo → set secrets in Space settings.
**Blocks transfer:** Yes — student cannot complete Exercise 2 or actually deploy without this.
**Outline-backed:** Yes — outline lists "Deploying to Hugging Face Spaces" as a topic.

---

## Finding 2 — `APP_PY` template silently drops tool visibility

**Severity:** `[confidence gap]`
**Location:** Part 6 deployment template (`APP_PY`) vs. Parts 3–5
**Reviewers:** Anthropic (1 of 5).
**Quote:** Implicit — the template combines streaming + try/except but omits the `RunItemStreamEvent` block from Part 4.
**What the student experiences:** The notebook shows four progressively richer apps, but the deployment template silently drops tool visibility. A student deploying Capstone 1, 3, or 4 (all tool-using agents) won't realize they need to add it back.
**Smallest fix:** One sentence above `APP_PY`: "This template combines streaming + failure handling. If your agent uses tools, copy the `RunItemStreamEvent` block from Part 4 into this `chat()` function."
**Blocks transfer:** Yes — for any tool-using agent.
**Outline-backed:** Yes — outline lists tool visibility as a deployment topic.

---

## Finding 3 — `history` object shape is undocumented

**Severity:** `[confidence gap]`
**Location:** Part 2, history-vs-memory warning cell, immediately before the `context = "\n".join(...)` snippet
**Reviewers:** Gemini, Grok (2 of 5). DeepSeek did not flag; Anthropic and OpenAI flagged Part 2 for a different reason (Finding 4).
**Quote:** `context = "\n".join([f"User: {h['content']}" if h["role"] == "user" else f"Assistant: {h['content']}" for h in history[-6:]])`
**What the student experiences:** The snippet uses `h['role']` and `h['content']` without ever stating what shape Gradio passes — list of dicts? tuples? what about the first message? Student can copy but not adapt or debug.
**Smallest fix:** One line before the snippet: "Gradio passes `history` as a list of `{'role': 'user'|'assistant', 'content': text}` dicts (most recent last)."
**Blocks transfer:** Yes.
**Outline-backed:** Split — Grok says yes, Gemini says no. The teaching beat (history vs memory) is in the outline; the specific data-shape doc is not. Treat as outline-adjacent.

---

## Finding 4 — When to use the history snippet vs. sessions is unclear

**Severity:** `[confidence gap]`
**Location:** Part 2, history-vs-memory warning cell (same cell as Finding 3, different problem)
**Reviewers:** OpenAI (1 of 5). Distinct from Finding 3 — this is the *decision rule*, not the *data shape*.
**Quote:** "To include recent context, build the input from history before passing it to the agent:"
**What the student experiences:** The student sees the mechanics but not "in my own app, when do I do this instead of using sessions?"
**Smallest fix:** One sentence before the snippet: "Use this pattern for lightweight short-term context in a simple chat app; use sessions when you want the SDK to manage conversation state for you."
**Blocks transfer:** Yes.
**Outline-backed:** Yes.

---

## Finding 5 — Streaming event classes appear without a conceptual model

**Severity:** `[unexplained term]` / `[pacing break]`
**Location:** Part 3 streaming template (and recurring in Part 4)
**Reviewers:** Gemini, Grok (2 of 5). **DeepSeek explicitly disagreed**, noting the streaming API was introduced in Lesson 30 and the student needs no re-introduction. Worth verifying against L30 before fixing — if L30 already names these classes with a conceptual frame, the fix is unnecessary; if L30 only mentioned `Runner.run_streamed()` without naming event types, the fix lands.
**Quote:**
```python
if isinstance(event, RawResponsesStreamEvent):
    data = event.data
    if isinstance(data, ResponseTextDeltaEvent):
...
if isinstance(event, RunItemStreamEvent):
    item = event.item
    if isinstance(item, ToolCallItem):
```
**What the student experiences:** The class names feel like magic constants to copy. Student doesn't know where they come from, what other events exist, or how to discover them.
**Smallest fix:** One sentence in the Part 3 intro paragraph: "`Runner.run_streamed()` emits different event types — `RawResponsesStreamEvent` carries text deltas, `RunItemStreamEvent` + `ToolCallItem` signal tool activity — so you can react as the run unfolds."
**Blocks transfer:** Yes (per Gemini/Grok), No (per DeepSeek). **Recommend verifying the L30 framing first.**
**Outline-backed:** Yes.

---

## Finding 6 — "Bridge" reads like jargon

**Severity:** `[unexplained term]`
**Location:** Part 3: Streaming Responses
**Reviewers:** OpenAI (1 of 5).
**Quote:** "Bridge `Runner.run_streamed()` from Lesson 30 into this pattern."
**What the student experiences:** "Bridge" reads like a known technical pattern instead of plain-English action.
**Smallest fix:** Replace with: "Use `Runner.run_streamed()` and pass each text chunk into Gradio with `yield`."
**Blocks transfer:** No.
**Outline-backed:** Yes.

---

## Finding 7 — Deployment template hard-codes mini model with no decision frame

**Severity:** `[lost why]`
**Location:** Part 6 / Deployment Checklist cell
**Reviewers:** Grok (1 of 5). Anthropic flagged the same cell for a different reason (Finding 8).
**Quote:** "`APP_PY` uses `os.getenv()` for `MODEL` instead of importing from `config.py`. On Hugging Face Spaces, there is no `config.py` — constants come from environment variables set in the Space settings."
**What the student experiences:** Lesson 31 taught a `MODEL` vs `REASONING_MODEL` decision framework. The deployment template hard-codes the mini model with no comment on when to upgrade or how to pass that via secrets.
**Smallest fix:** Adjust to: "Use `os.getenv('MODEL', 'gpt-5-mini')` — or `REASONING_MODEL` when orchestration quality matters more than latency. Set the value in your Space's Repository secrets."
**Blocks transfer:** Yes.
**Outline-backed:** Yes — Lesson 31 decision framework is referenced for this notebook.

---

## Finding 8 — "There is no `config.py`" reads like a platform constraint

**Severity:** `[lost why]`
**Location:** Part 6 Deployment Checklist note (same cell as Finding 7, different concern)
**Reviewers:** Anthropic (1 of 5).
**Quote:** "On Hugging Face Spaces, there is no `config.py` — constants come from environment variables set in the Space settings."
**What the student experiences:** Lesson 30 just taught project structure with `config.py` and `tools.py`. The phrasing implies HF Spaces forbids that structure, leaving the student wondering whether they have to flatten their L30 project to deploy.
**Smallest fix:** Reframe: "We're packaging this as a single `app.py` for deployment simplicity — you can still use the Lesson 30 module structure on Spaces by uploading those files alongside `app.py`. The change here is reading `MODEL` from `os.getenv()` so it can be set via Space secrets."
**Blocks transfer:** No, but leaves L30→L32 relationship muddy.
**Outline-backed:** Yes.

---

## Finding 9 — Parts 2–5 progression lacks an orienting line

**Severity:** `[lost thread]` / `[pacing break]`
**Location:** Parts 2–5 overall
**Reviewers:** OpenAI, Grok (2 of 5).
**Quote:** "## 💬 Part 2: Wrap an Agent in `gr.ChatInterface`" (and successive Part headers)
**What the student experiences:** Four similar `chat()` variants accumulate (minimal → streaming → tool visibility → failure handling). By Part 5 the student isn't sure which is "the real one" to start from in their own project.
**Smallest fix:** One sentence at the start of Part 2 or Part 3: "These next four parts build the same app step by step — start with Part 2, then layer on streaming, status updates, and error handling."
**Blocks transfer:** Yes (OpenAI), No (Grok). Either way, fix is one sentence.
**Outline-backed:** Yes.

---

## Finding 10 — Tool visibility section forward-references Part 5

**Severity:** `[pacing break]`
**Location:** Part 4 "Why This Works" cell
**Reviewers:** Anthropic (1 of 5). Borderline — could read as a second-pass forward-reference issue, but the comprehension impact is real (student scrolls looking for code that isn't there).
**Quote:** "For failure handling, the outer `try/except` in Part 5 catches tool errors and shows a friendly message rather than crashing the app."
**What the student experiences:** The sentence references a `try/except` not in Part 4's code. Student scrolls back, doesn't find it, wonders if the example is incomplete.
**Smallest fix:** Cut that sentence. Let Part 5 introduce error handling cleanly.
**Blocks transfer:** No.
**Outline-backed:** No — sequencing issue, not a missing teaching beat.

---

## Finding 11 — Tool visibility's "why" reads as cosmetic

**Severity:** `[lost why]`
**Location:** Part 4: Tool Visibility
**Reviewers:** OpenAI (1 of 5).
**Quote:** "Adding a brief status update makes the experience feel responsive and shows that something is happening."
**What the student experiences:** Reads like UX polish — student may skip it as nice-to-have rather than implement in real projects.
**Smallest fix:** Add: "This matters most when tools are slow — without a status update, users often assume the app froze or the request failed."
**Blocks transfer:** No.
**Outline-backed:** Yes.

---

## Recommended priority

Fix-first (transfer blockers, multi-reviewer or high-impact):
1. **Finding 1** — HF Spaces concrete steps + what HF is (3 reviewers)
2. **Finding 2** — `APP_PY` tool visibility note (Anthropic, but high blast radius — affects most capstones)
3. **Finding 3** — `history` shape one-liner (2 reviewers)
4. **Finding 7** — model/reasoning_model in deployment template (ties back to L31 decision framework)

Verify-then-fix:
5. **Finding 5** — streaming event classes. **Check Lesson 30 first** — if L30 already frames these, no change needed (DeepSeek's read).

One-line polish:
6. Findings 4, 6, 8, 9, 10, 11 — each is a one-sentence add or cut.

---

# Individual Reviews

The grouped findings above are your primary working surface. The full individual reviews below are for verifying nuance.

---

## Anthropic Review

# Third-Pass Review — Notebook 32: Deploying Agents with Gradio

## Finding 1

**Severity:** [confidence gap]

**Location:** Part 6: Deploying to Hugging Face Spaces

**Quote:** "A Hugging Face Space is a free hosted environment for Gradio apps. Create a Space, push your files, and your agent gets a public URL."

**What the student experiences:** The student understands the file structure but has no idea what "create a Space" or "push your files" actually means in practice — sign up where? Click what? Use git? Drag files in a browser? This is the headline outcome of the entire course (a deployed agent), and the actual deployment steps are skipped.

**Smallest fix:** Add 3–4 bullet points before the file-set diagram giving the concrete workflow: sign in at `huggingface.co`, click "Create new Space", choose "Gradio" SDK, then either drop files into the web UI or `git push` to the Space's repo.

**Blocks transfer:** Yes — student cannot complete Exercise 2 or actually deploy without these steps.

**Outline-backed:** Yes — the outline lists "Deploying to Hugging Face Spaces" as a topic.

---

## Finding 2

**Severity:** [confidence gap]

**Location:** Part 6 deployment template (`APP_PY`) vs. Parts 3–5

**Quote:** From Key Takeaways: "Tool visibility and failure handling keep the app feeling solid"

**What the student experiences:** The notebook shows four progressively richer apps (minimal → streaming → tool visibility → failure handling), but the `APP_PY` deployment template silently drops tool visibility. The student wonders: which one do I actually deploy? Should I combine them? Did the author forget tool visibility, or is it intentionally cut?

**Smallest fix:** One sentence above `APP_PY`: "This template combines streaming + failure handling. If your agent uses tools, copy the `RunItemStreamEvent` block from Part 4 into this `chat()` function the same way."

**Blocks transfer:** Yes — a student deploying a tool-using agent (Capstone 1, 3, or 4) won't know to add tool visibility.

**Outline-backed:** Yes — outline lists tool visibility as a deployment topic, so the deployed app should show it.

---

## Finding 3

**Severity:** [pacing break]

**Location:** Part 4: Tool Visibility — Why This Works cell

**Quote:** "For failure handling, the outer `try/except` in Part 5 catches tool errors and shows a friendly message rather than crashing the app."

**What the student experiences:** The Why This Works references a `try/except` that is not in the Part 4 code. The student scrolls back up looking for it, doesn't find it, and is unsure whether the Part 4 example is incomplete or whether they're missing something.

**Smallest fix:** Cut that sentence. Let Part 5 introduce error handling cleanly without Part 4 forward-referencing it.

**Blocks transfer:** No — a small confusion, not a transfer blocker.

**Outline-backed:** No — this is a sequencing issue, not a missing teaching beat.

---

## Finding 4

**Severity:** [lost why]

**Location:** Part 6 — Deployment Checklist note

**Quote:** "`APP_PY` uses `os.getenv()` for `MODEL` instead of importing from `config.py`. On Hugging Face Spaces, there is no `config.py` — constants come from environment variables set in the Space settings."

**What the student experiences:** Lesson 30 just taught them to factor agent code into `config.py`, `tools.py`, and an agent module. The phrasing "there is no `config.py`" reads as a platform constraint, so the student wonders if HF Spaces forbids the project structure they just learned, or if they need to flatten their Lesson 30 project to deploy it.

**Smallest fix:** Reframe the sentence: "We're packaging this as a single `app.py` for deployment simplicity — you can still use the Lesson 30 module structure on Spaces by uploading those files alongside `app.py`. The change here is reading `MODEL` from `os.getenv()` so it can be set via Space secrets."

**Blocks transfer:** No, but it leaves the student confused about how the L30 project structure relates to deployment.

**Outline-backed:** Yes — outline lists "Secret management for deployment — platform secrets on Hugging Face Spaces" as a topic, and this note is part of that beat.

---

## Summary

Notebook 32 is in good shape overall — the streaming bridge and progressive app variants teach cleanly. The four findings above cluster around the deployment beat (Part 6), which is the lesson's payoff and currently the weakest part of the notebook for a first-time HF Spaces user. Findings 1 and 2 are the most important to fix; 3 and 4 are smaller polish items.

---

## OpenAI Review

**Severity:** [unexplained term]  
**Location:** Part 3: Streaming Responses  
**Quote:** “Bridge `Runner.run_streamed()` from Lesson 30 into this pattern.”  
**What the student experiences:** The student understands “streaming” at a high level, but “bridge” reads like a known technical pattern rather than a plain-English action. They have to stop and infer that this just means “connect the SDK stream to Gradio’s `yield` pattern.”  
**Smallest fix:** Replace “Bridge `Runner.run_streamed()` from Lesson 30 into this pattern.” with “Use `Runner.run_streamed()` and pass each text chunk into Gradio with `yield`.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Part 2, history warning cell  
**Quote:** “To include recent context, build the input from history before passing it to the agent:”  
**What the student experiences:** This is the exact point where a student asks, “So in my own app, when should I do this instead of using sessions?” The notebook shows the mechanics, but not the decision rule, so the student can copy the snippet without understanding when it’s the right pattern.  
**Smallest fix:** Add one sentence before the code snippet: “Use this pattern for lightweight short-term context in a simple chat app; use sessions when you want the SDK to manage conversation state for you.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** Part 4: Tool Visibility  
**Quote:** “Adding a brief status update makes the experience feel responsive and shows that something is happening.”  
**What the student experiences:** The student sees the UX improvement, but not why this matters enough to implement in a real project. Without one more sentence, it can feel cosmetic instead of practical.  
**Smallest fix:** Add: “This matters most when tools are slow — without a status update, users often assume the app froze or the request failed.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [pacing break]  
**Location:** Part 4: Tool Visibility  
**Quote:** “## 🔧 Part 4: Tool Visibility”  
**What the student experiences:** This section introduces two new ideas at once: a new stream event type (`RunItemStreamEvent` / `ToolCallItem`) and a UI pattern where one yielded message is later replaced by streamed text. The student can follow the code, but it is less clear which idea is the main takeaway.  
**Smallest fix:** Add one orienting sentence right after the section intro: “Focus on one new idea here: listen for tool-start events and surface them as temporary chat status messages.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Part 6: Deploying to Hugging Face Spaces  
**Quote:** “Create a Space, push your files, and your agent gets a public URL.”  
**What the student experiences:** The student leaves understanding the required files, but not how to map notebook work into a real deployable project folder. They may still wonder, “What exactly do I copy from this notebook into `app.py`?”  
**Smallest fix:** Add one sentence after that line: “In practice, you’ll copy one of the chat patterns from Parts 2–5 into `app.py`, then add `requirements.txt` and set your secret in the Space settings.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [lost thread]  
**Location:** Parts 2–5 overall  
**Quote:** “## 💬 Part 2: Wrap an Agent in `gr.ChatInterface`”  
**What the student experiences:** The notebook presents four app patterns in sequence — minimal, streaming, tool visibility, failure handling — but by Part 5 a first-time Gradio user may not be sure which version is the “real” one they should start from in their own project. The progression is visible, but the thread of “build from simple to production-ish” is not stated explicitly.  
**Smallest fix:** Add one sentence at the start of Part 2 or Part 3: “These next four parts build the same app step by step — start with Part 2, then layer on streaming, status updates, and error handling.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

## Gemini Review

This notebook passed third-pass review with three findings. The overall structure is clear and it successfully guides the student from a working agent to a deployed web application. The findings below address small gaps in context that would improve student confidence and transferability.

---

**Severity:** `[confidence gap]`
**Location:** Part 2, in the markdown cell that starts `⚠️ **History vs memory:**`
**Quote:** `context = "\\n".join([f"User: {h['content']}" if h["role"] == "user" else f"Assistant: {h['content']}" for h in history[-6:]])`
**What the student experiences:** "The code snippet for using chat history is helpful, but I don't know what the `history` object actually contains. Where do the `role` and `content` keys come from? I wouldn't know how to adapt this snippet if I wanted to format history differently."
**Smallest fix:** Before the code snippet that uses `history`, add a sentence explaining its structure: "Gradio provides `history` as a list of dictionaries, where each dictionary has `role` ('user' or 'assistant') and `content` keys."
**Blocks transfer:** Yes
**Outline-backed:** No

---

**Severity:** `[pacing break]`
**Location:** Part 3: Streaming Responses, and Part 4: Tool Visibility
**Quote:** `if isinstance(event, RawResponsesStreamEvent):\n            data = event.data\n            if isinstance(data, ResponseTextDeltaEvent):`
**What the student experiences:** "I'm seeing a lot of very specific class names like `RawResponsesStreamEvent` and `RunItemStreamEvent` without understanding the system they are part of. It feels like I'm just copying magic code without knowing why it works or what other events might be available."
**Smallest fix:** In the first paragraph of Part 3, add a sentence to provide a conceptual model for the event stream. For example: "`Runner.run_streamed()` emits different event types that signal stages of the agent's run. By listening for specific events, we can see text as it's generated, know when a tool is called, and more."
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

**Severity:** `[unexplained term]`
**Location:** Part 6: Deploying to Hugging Face Spaces
**Quote:** "A Hugging Face Space is a free hosted environment for Gradio apps."
**What the student experiences:** "What is Hugging Face? I've never heard of it. Is it a company, a product, a community? I feel like I'm missing context before I'm asked to deploy my app there."
**Smallest fix:** Add one sentence of context at the beginning of the first paragraph. For example: "Hugging Face is a central platform for the AI community, offering models, datasets, and tools. A Hugging Face Space is a free hosted environment for Gradio apps."
**Blocks transfer:** No
**Outline-backed:** Yes

---

## Grok Review

**Severity:** `[unexplained term]`

**Location:** Part 3 (streaming template) and Part 4 (tool visibility template)

**Quote:** 
```python
from agents.stream_events import RawResponsesStreamEvent, RunItemStreamEvent
from openai.types.responses import ResponseTextDeltaEvent
...
if isinstance(event, RawResponsesStreamEvent):
    data = event.data
    if isinstance(data, ResponseTextDeltaEvent):
...
if isinstance(event, RunItemStreamEvent):
    item = event.item
    if isinstance(item, ToolCallItem):
```

**What the student experiences:** I've never seen `RawResponsesStreamEvent`, `ResponseTextDeltaEvent`, `RunItemStreamEvent`, or `ToolCallItem` before. The notebook says "bridge `Runner.run_streamed()` from Lesson 30", but these concrete classes feel like magic constants I have to copy exactly. I don't know where they come from, what other events exist, or how I'd discover them for my own agent.

**Smallest fix:** After the sentence "Bridge `Runner.run_streamed()` from Lesson 30 into this pattern." add: "The SDK exposes event classes (`RawResponsesStreamEvent` for text deltas, `RunItemStreamEvent` + `ToolCallItem` for tool activity) so you can react as the run unfolds."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`

**Location:** The ⚠️ History vs memory cell (immediately after Part 2)

**Quote:** 
```python
context = "\n".join([f"User: {h['content']}" if h["role"] == "user" else f"Assistant: {h['content']}" for h in history[-6:]])
full_input = f"{context}\nUser: {message}" if context else message
result = await Runner.run(agent, input=full_input)
```

**What the student experiences:** This looks useful for giving the agent context, but I have no idea what shape Gradio actually passes for `history`. Are the items dicts with exactly those keys? Tuples? What happens on the very first message? I could copy it, but I wouldn't know how to adapt or debug it in my own project.

**Smallest fix:** Add one line immediately before the code block: "Gradio passes `history` as a list of `{'role': 'user'|'assistant', 'content': text}` dicts (most recent last)."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[pacing break]`

**Location:** Parts 3–5 (the three successive large template cells)

**Quote:** 
`## 📡 Part 3: Streaming Responses`  
`## 🔧 Part 4: Tool Visibility`  
`## ⚠️ Part 5: Handling Failure States`

**What the student experiences:** Each Part adds another layer (generator + specific events, then more events for tools, then try/except) inside what is essentially the same `chat()` function. By the third long printed template I'm holding four similar-but-different versions in my head at once and the single clear takeaway per section is getting fuzzy.

**Smallest fix:** After the Part 3 header add one sentence: "We'll evolve the same `chat()` function across these three parts — streaming first, then tool visibility, then error handling."

**Blocks transfer:** No

**Outline-backed:** Yes

---

**Severity:** `[lost why]`

**Location:** Part 6 / Deployment Checklist cell

**Quote:** 
"Note: `APP_PY` uses `os.getenv()` for `MODEL` instead of importing from `config.py`. On Hugging Face Spaces, there is no `config.py` — constants come from environment variables set in the Space settings."

**What the student experiences:** I see the code change but I don't understand the broader implication. In Lesson 31 we were told to default to `MODEL` and only upgrade to `REASONING_MODEL` with evidence. Here the deployment template hard-codes the mini model with no comment on how I'd intentionally use the reasoning model in production or pass that choice via secrets.

**Smallest fix:** Change the note to: "Use `os.getenv('MODEL', 'gpt-5-mini')` (or `REASONING_MODEL` when orchestration quality matters more than latency). Set the value in your Space's Repository secrets."

**Blocks transfer:** Yes

**Outline-backed:** Yes (Lesson 31 decision framework is explicitly referenced in the outline for this notebook)

---

This notebook is in excellent shape overall — the UI-history-vs-agent-memory callout is exactly the honesty reminder the guidelines require, the templates are clear, the exercises are appropriately scoped for a final project, and the Course Complete section lands the course arc well. Only the four moments above would have left me (as a first-time agent builder) quietly confused or unsure how to transfer the pattern.

---

## DeepSeek Review

**Result:** This notebook passed third-pass review with no significant comprehension issues.

The notebook is clean for a student who has completed all previous notebooks. Here's why no findings are warranted:

- **Unexplained terms:** The streaming API (`Runner.run_streamed()`, `RawResponsesStreamEvent`, `ResponseTextDeltaEvent`, `RunItemStreamEvent`, `ToolCallItem`) was introduced in Lesson 30 (streaming agent output in a script). The student encountered these there and needs no re-introduction here. The "Repository Secrets" term is explained inline.

- **Lost why:** Each pattern has a clear motivation — notebooks are private (Part 1), streaming improves feel (Part 3), tool visibility prevents dead-air UI (Part 4), error catching prevents crashes (Part 5), deployment makes it public (Part 6). The "Why This Works" cells land the relevance.

- **Lost thread:** The notebook is lean (6 short Parts + exercises). No long setup buries the core idea. The setup cell is minimal (import check only). Each Part has one printed template and one explanation cell.

- **Confidence gap:** The templates are self-contained and the exercises ask the student to write their own variant. The deployment section gives a checklist and tells them to test locally first. The student has seen this template pattern in Lesson 30; they can transfer.

- **Pacing break:** New concepts (streaming bridge, tool visibility events, deployment files) appear one per Part. No Part crams multiple new ideas.

The only minor observation — the deployment section says "Create a Space, push your files" without a step-by-step for creating a Hugging Face Space — but this is a concept-level notebook, and the student can use HF documentation or the Troubleshooting link. It does not block transfer.

**Blocks transfer:** No findings to assess.
