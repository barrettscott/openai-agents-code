# Review Report: Capstone #3 — Production Customer Service Agent
Generated: 2026-05-10 | Notebook: /Users/scott/Dropbox/Notebooks/openai-agents/Week_4_Memory_Safety_Observability/26_Capstone_3_Customer_Service.ipynb
Models: Anthropic (claude-opus-4-7 (CLI/Max)) · OpenAI (gpt-5.4) · Gemini (gemini-2.5-pro) · Grok (grok-4.20-0309-reasoning) · DeepSeek (deepseek-reasoner) · Dedup (claude-opus-4-7 CLI/Max)

---

# Grouped Findings: Capstone #3 — Production Customer Service Agent

# Third-Pass Synthesis — Notebook 26 (Capstone #3 — Production Customer Service Agent)

Five reviewers found a small, consistent set of comprehension issues. Two structural artifacts (duplicate code cells) and one threshold mismatch are the most serious. The remaining findings are one-sentence additions that turn implicit teaching beats into explicit ones — most map directly to outline-backed capstone features (sessions, tracing, approval flow).

---

## HIGHEST PRIORITY — Blockers

### Finding 1 — Duplicate guardrail code cells in Phase 2

- **Severity:** `[lost thread]` / `[confidence gap]`
- **Location:** Phase 2 (Guardrail) — two consecutive code cells, plus a markdown cell containing runnable-looking code
- **Reviewers:** 3 (Grok, DeepSeek, Gemini — Gemini's F2 about `RunContextWrapper`/`TResponseInputItem` is the same root cause)
- **Quote:** Two near-identical definitions of `SupportTopicCheck` and `support_topic_guardrail`. The second adds type hints `ctx: RunContextWrapper` and `input: str | list[TResponseInputItem]` that are not imported in the setup cell.
- **What the student experiences:** The student sees the same class and function defined twice, can't tell which is canonical, and if they run the typed version it fails with `NameError`. The demo then breaks for a reason that has nothing to do with the lesson.
- **Smallest fix:** Delete one of the two cells. **Reviewer split on which to keep:** Grok prefers the typed version; DeepSeek and Gemini prefer the untyped version because the types aren't imported and add cognitive load. The defensible call is to keep the untyped version (it runs as-is and matches the simpler patterns from Lesson 22).
- **Blocks transfer:** Yes
- **Outline-backed:** Yes

---

### Finding 2 — Duplicate pipeline function + threshold mismatch ($50 vs $100)

- **Severity:** `[confidence gap]`
- **Location:** Phase 4 (`handle_customer_message`) — two consecutive code cells, plus contradicting pre-demo markdown cells
- **Reviewers:** 3 (Anthropic, Gemini, DeepSeek)
- **Quote:** First code cell sets `AUTO_APPROVE_THRESHOLD = 100.0` and signature `handle_customer_message(message, session, auto_approve=True, agent=None)`. Second code cell sets `AUTO_APPROVE_THRESHOLD = 50.0` with no `agent` parameter. Pre-demo markdown says both `"exceeds the $100 threshold"` and `"above the $50 auto-approve threshold"`.
- **What the student experiences:** Two contradictory thresholds, two contradictory function signatures. The student can't tell which to trust — and if they copy the pattern, they don't know whether to trust narrative or code. The exercise then references `agent=` which only exists in the overwritten version.
- **Smallest fix:** Pick one threshold (recommend `100.0` since the demo refund is $149.99) and one signature. Keep the `agent=None` parameter (it's needed by the exercise — see Finding 3). Update both pre-demo markdown cells to match. Delete the redundant code cell.
- **Blocks transfer:** Yes
- **Outline-backed:** Yes (outline calls for "explicit pause point" — must be unambiguous)

---

### Finding 3 — Exercise references an `agent` parameter the function doesn't have

- **Severity:** `[confidence gap]`
- **Location:** Practice Exercise 1 referencing `handle_customer_message`
- **Reviewers:** 2 (Gemini, OpenAI; DeepSeek F2 surfaces the same root cause)
- **Quote:** Exercise: `"Run handle_customer_message() but pass extended_support_agent (you'll need to modify handle_customer_message or create a new version)"`. Active function signature does not accept an `agent` parameter.
- **What the student experiences:** The exercise asks them to swap in a new agent, but the pipeline function has no agent parameter. They feel asked to discover a design move the lesson never named.
- **Smallest fix:** Update the (kept) function signature to `async def handle_customer_message(message, session, auto_approve=True, agent=None):`, add `run_agent = agent or support_agent` at the top, and use `run_agent` in the two `Runner.run()` calls. Add one orienting sentence before the exercises: *"A useful extension pattern is to parameterize your orchestration function with `agent`, so you can swap in upgraded versions without rewriting the approval flow."*
- **Blocks transfer:** Yes
- **Outline-backed:** Yes

---

### Finding 4 — Tracing callout has no concrete checklist

- **Severity:** `[confidence gap]` / `[lost why]`
- **Location:** "🔬 Tracing" callout after Phase 4
- **Reviewers:** 2 (Anthropic, Grok)
- **Quote:** `"Every Runner.run() call in this pipeline creates a trace automatically. Open the OpenAI dashboard to inspect the guardrail check, tool calls, and approval flow in a single trace view."`
- **What the student experiences:** Lesson 25 trained them to read traces with a checklist; this callout says "inspect" but names nothing concrete to find. After running this multi-feature pipeline they may skip the dashboard entirely. The capstone is the natural place to lock in production tracing as a habit.
- **Smallest fix:** Replace the single sentence with 2–3 concrete bullets: the `TopicChecker` guardrail span (why a request was blocked), the `process_refund` interruption span (the pause point), the resumed run after approval, and token counts on multi-turn sessions.
- **Blocks transfer:** Yes
- **Outline-backed:** Yes ("Tracing and monitoring" is a capstone component)

---

## MEDIUM PRIORITY — One-sentence additions

### Finding 5 — `needs_approval=True` is unexplained

- **Severity:** `[lost why]` / `[unexplained term]`
- **Location:** Phase 1 — `process_refund` definition + first security note
- **Reviewers:** 2 (OpenAI, Grok)
- **Quote:** `@function_tool(needs_approval=True)`
- **What the student experiences:** They can follow the code but don't get the transferable rule (approval is about *side effects*, not refunds specifically), and they don't see what the SDK actually does mechanically when the flag is set.
- **Smallest fix:** Two adjacent additions — (a) one sentence in the security note: *"The general rule: read-only tools auto-run; write actions that change money, records, or external systems pause for approval."* (b) Comment above the decorator: *"`needs_approval=True` tells the SDK to pause before executing this tool and populate `result.interruptions` (handled in the pipeline below)."*
- **Blocks transfer:** Yes (for the rule); No (for the mechanics — but the gap nags)
- **Outline-backed:** Yes

---

### Finding 6 — Sessions payoff in the multi-turn demo is muted

- **Severity:** `[lost why]`
- **Location:** Multi-turn conversation demo (Turn 2, where customer doesn't repeat ORD-001)
- **Reviewers:** 2 (Anthropic, OpenAI)
- **Quote:** `"They stopped working after 3 days. Can I get a refund?"` (no order ID)
- **What the student experiences:** This is the moment the session feature pays off, but nothing points the student to *watch* for it. They'll read the agent's correct answer as "the agent is smart" rather than "sessions made this work." Sessions are a headline capstone feature.
- **Smallest fix:** Add one sentence above the demo: *"Watch turn 2 — the customer doesn't repeat the order ID. The session is what lets the agent carry ORD-001 forward, so customers don't restate context every turn."*
- **Blocks transfer:** No (pattern is in code), but the conceptual payoff lands flat.
- **Outline-backed:** Yes ("Sessions for conversation memory")

---

### Finding 7 — Interruption/state machinery is shown but not named

- **Severity:** `[confidence gap]`
- **Location:** Phase 4 — the `while result.interruptions:` block in `handle_customer_message`
- **Reviewers:** 1 (Grok)
- **Quote:** The full `while result.interruptions: state = result.to_state(); ... state.approve(interruption); ... result = await Runner.run(support_agent, state)` block.
- **What the student experiences:** They see refunds trigger an approval prompt but have no idea how the interruption/state pattern actually works. To add approval to their own tool in a different project, they don't know where to start.
- **Smallest fix:** One sentence above the `while` block: *"The SDK pauses execution and populates `result.interruptions` when it hits a tool marked `needs_approval=True`. We convert the result to a mutable state, decide whether to approve or reject, then resume the run."*
- **Blocks transfer:** Yes
- **Outline-backed:** Yes

---

## LOWER PRIORITY — Single reviewer, comprehension-only

### Finding 8 — No bridge from Phase 1 to the guardrail code

- **Severity:** `[lost thread]`
- **Location:** Between the Phase 1 security note and the `SupportTopicCheck` code (Phase 2 has no header)
- **Reviewers:** 1 (Anthropic)
- **What the student experiences:** They jump from a security note straight into unfamiliar code with no orientation that this is the guardrail layer.
- **Smallest fix:** Add a `## 🛡️ Phase 2: Block Off-Topic Requests` markdown cell with one orienting sentence: *"A guardrail agent screens the message before it reaches the support agent — non-support requests get rejected up front."*
- **Blocks transfer:** No
- **Outline-backed:** Yes
- **Note:** This may already be addressed if Finding 1's cleanup includes restoring/keeping a Phase 2 header.

---

### Finding 9 — Why use an agent (not a keyword check) for the guardrail

- **Severity:** `[lost why]`
- **Location:** Phase 2 guardrail definition
- **Reviewers:** 1 (Gemini)
- **What the student experiences:** "Why run a whole agent inside a guardrail? This seems expensive for topic checking."
- **Smallest fix:** One sentence before the code: *"A keyword check is brittle; a dedicated classifier agent handles typos, paraphrasing, and conversational language."*
- **Blocks transfer:** Yes (per Gemini)
- **Outline-backed:** Yes

---

### Finding 10 — "saved to session" hides the mechanism

- **Severity:** `[unexplained term]`
- **Location:** System Architecture diagram (`Response (saved to session)`)
- **Reviewers:** 1 (OpenAI)
- **What the student experiences:** Unsure whether saving is automatic, manual in `handle_customer_message`, or done by `SQLiteSession`.
- **Smallest fix:** Short clause: *"Because we pass a `SQLiteSession` into `Runner.run()`, history is stored automatically between turns."*
- **Blocks transfer:** No
- **Outline-backed:** Yes

---

### Finding 11 — "orchestration layer" lacks a transferable bridge

- **Severity:** `[confidence gap]` / `[pacing break]`
- **Location:** Phase 4 intro
- **Reviewers:** 1 (OpenAI — two related findings collapsed)
- **What the student experiences:** "Orchestration layer" sounds like a named SDK thing rather than the transferable pattern (a plain Python wrapper around `Runner.run()` where app-specific rules live). Phase 4 also stacks three new ideas at once (orchestration + thresholds + resume-after-interrupt).
- **Smallest fix:** One orienting sentence under the heading: *"Focus on one idea here: `handle_customer_message()` is a plain Python wrapper around `Runner.run()` where you add app-specific rules — approval thresholds, logging, fallback behavior."*
- **Blocks transfer:** Yes (for the pattern)
- **Outline-backed:** Yes

---

### Finding 12 — `auto_approve=True` parameter name contradicts the demo's pause

- **Severity:** `[confidence gap]`
- **Location:** Pre-demo markdown / `handle_customer_message` signature
- **Reviewers:** 1 (DeepSeek)
- **What the student experiences:** `auto_approve=True` reads as "everything is automatic," but the demo pauses at an `input()` prompt. They hesitate — is this expected? What do they type?
- **Smallest fix:** Add to the pre-demo markdown: *"You'll see an `input()` prompt at Turn 2 — type `yes` to approve the refund."*
- **Blocks transfer:** No
- **Outline-backed:** Yes

---

## Filtered out (as third-pass scope)

- **DeepSeek F4 (exercise structure: code cell before header) and F5 (missing `## 💪 Practice Exercises` header)** — these are first-pass / structural concerns, not comprehension blockers. Worth noting if structural cleanup is happening anyway, but not third-pass.
- **Gemini's recommendation to strip type hints** — folded into Finding 1 as a fix-direction note rather than a separate finding.

---

## Top-of-list for Scott

1. **Findings 1, 2, 3** — clean up the duplicate cells and reconcile the threshold and `agent=` parameter. These are the only items that materially break the demo or the exercise.
2. **Finding 4** — turn the tracing callout into a concrete checklist; it's the cheapest way to make the capstone reinforce a Lesson 25 habit.
3. **Findings 5–7** — three one-sentence additions that lock in the transferable rules behind `needs_approval`, sessions, and the interruption pattern.

Everything else is one-sentence polish that can be batched.

---

# Individual Reviews

The grouped findings above are your primary working surface. The full individual reviews below are for verifying nuance.

---

## Anthropic Review

# Third-Pass Review — Notebook 26 (Capstone #3 — Production Customer Service Agent)

Reviewing as a student who has just finished Lesson 25 and is now seeing this capstone for the first time.

---

**Finding 1**

- **Severity:** [lost thread]
- **Location:** Between Phase 1 (Tools) and Phase 3 (Build the Agent) — there is no Phase 2 header
- **Quote:** The notebook goes from the Phase 1 security note (`"...the approval gate checks permission, not correctness."`) directly into a code cell beginning `class SupportTopicCheck(BaseModel):` with no introductory markdown.
- **What the student experiences:** After reading about tools and a security note, they hit unfamiliar code (`SupportTopicCheck`, `topic_checker`, `support_topic_guardrail`) with no orientation telling them this is the guardrail layer or why it sits before the main agent. The architecture diagram up top mentioned an Input Guardrail, but the bridge to "here's that guardrail being built" is missing.
- **Smallest fix:** Add a `## 🛡️ Phase 2: Block Off-Topic Requests` markdown cell before the `SupportTopicCheck` code cell, with one sentence framing: "A guardrail agent screens the message before it reaches the support agent — non-support requests get rejected up front."
- **Blocks transfer:** No
- **Outline-backed:** Yes — the outline calls for guardrails as a capstone component.

---

**Finding 2**

- **Severity:** [confidence gap]
- **Location:** Phase 4 narrative + code cell + demo intro
- **Quote:** Phase 4 markdown says `"refunds under $100 auto-approve; higher amounts pause for manual review"` and the demo intro says `"The $149.99 refund exceeds the $100 threshold"` — but the code cell sets `AUTO_APPROVE_THRESHOLD = 50.0` and a separate note says `"above the $50 auto-approve threshold"`.
- **What the student experiences:** Two contradictory thresholds in the same section. The student doesn't know which number is real — and if they copy this pattern into their own project, they won't know whether to trust the narrative or the code.
- **Smallest fix:** Pick one value (recommend `100.0` since the demo refund is $149.99) and make narrative, code, and both pre-demo notes match.
- **Blocks transfer:** Yes
- **Outline-backed:** No (operational consistency, not an outline beat)

---

**Finding 3**

- **Severity:** [lost why]
- **Location:** Customer Service Demo — Turn 2
- **Quote:** Turn 2 of the conversation: `"They stopped working after 3 days. Can I get a refund?"` (no order ID).
- **What the student experiences:** This is the moment the session feature actually pays off — the agent knows the order is ORD-001 only because the session preserved turn 1. But nothing in the surrounding markdown points the student to *watch* for this. They'll see the agent answer correctly and assume "the agent is smart" rather than "sessions made this work." Sessions are a headline capstone feature; this is the demo that proves it.
- **Smallest fix:** Add one sentence above the demo cell: "Notice turn 2 — the customer doesn't repeat the order ID. The session is what lets the agent carry ORD-001 forward."
- **Blocks transfer:** No (they have the pattern in code), but the conceptual payoff is muted.
- **Outline-backed:** Yes — outline calls out "Sessions for conversation memory" as a capstone feature.

---

**Finding 4**

- **Severity:** [confidence gap]
- **Location:** "🔬 Tracing" callout after Phase 4
- **Quote:** `"Every Runner.run() call in this pipeline creates a trace automatically. Open the OpenAI dashboard to inspect the guardrail check, tool calls, and approval flow in a single trace view."`
- **What the student experiences:** Lesson 25 trained the student to look at traces with a checklist. Here they're told to "inspect" but given nothing concrete to find. After running this multi-feature pipeline, they may skip the dashboard because they don't know what's interesting in *this* trace specifically. The capstone is the natural place to lock in production tracing as a habit.
- **Smallest fix:** Replace the single sentence with 2–3 bullets pointing at what should be visible: the `TopicChecker` span firing before the main run, the `process_refund` interruption marking the pause point, and the resumed run after approval landing as a continuation. One concrete checklist, like the patterns in Lesson 25.
- **Blocks transfer:** Yes — without this, the student doesn't reinforce trace-reading on a real production-shaped agent.
- **Outline-backed:** Yes — outline lists "Tracing and monitoring" as a capstone component.

---

**Summary:** Four findings. Findings 1 and 2 are quick fixes that remove genuine confusion. Findings 3 and 4 are one-sentence-or-bullet additions that turn implicit teaching beats into explicit ones — both connect directly to outline-backed capstone features (sessions, tracing) that currently land flatter than they should.

---

## OpenAI Review

**Severity:** [lost why]  
**Location:** `## 🛠️ Phase 1: Define the Tools` / refund tool + first security note  
**Quote:** `@function_tool(needs_approval=True)`  
**What the student experiences:** The notebook marks `process_refund` as requiring approval, but a first-time agent builder still may not understand why approval is needed here but not for the other tools beyond “because the notebook says so.” They can follow the code, but they don’t yet get the real project rule they should reuse: approval is about side effects, not about refunds specifically.  
**Smallest fix:** Add one sentence right after the tool definitions or in the existing security note: “The general rule is: read-only tools can usually auto-run, but write actions that change money, records, or external systems should pause for approval.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [unexplained term]  
**Location:** `## 🏗️ System Architecture`  
**Quote:** `Response (saved to session)`  
**What the student experiences:** The student knows what sessions are from Lesson 19, but “saved to session” lands like a hidden mechanism here. They may wonder whether the response is being saved because of `SQLiteSession`, because `handle_customer_message()` does it manually, or because the SDK does it automatically.  
**Smallest fix:** Add a short clause in that cell: “Because we pass a `SQLiteSession` into `Runner.run()`, the conversation history is stored automatically between turns.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** `## 🚀 Phase 4: The Full Pipeline`  
**Quote:** `The orchestration layer handles sessions, guardrails, and the approval flow.`  
**What the student experiences:** This is the core capstone pattern, but the student is asked to accept “orchestration layer” as a named thing without a bridge to their own projects. They can read the helper function, but may not realize the transferable pattern is “wrap `Runner.run()` in your own application function where you add policy and routing logic.”  
**Smallest fix:** Add one sentence under that heading: “In your own project, this is usually a plain Python wrapper around `Runner.run()` where you add app-specific rules like approval thresholds, logging, and fallback behavior.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [pacing break]  
**Location:** `## 🚀 Phase 4: The Full Pipeline`  
**Quote:** `## 🚀 Phase 4: The Full Pipeline`  
**What the student experiences:** This phase introduces three new things at once: the orchestration function, threshold-based approval policy, and resumed execution after interruptions. A novice agent builder can still run it, but it’s easy to lose which idea is the main lesson versus which lines are just plumbing.  
**Smallest fix:** Add one orienting sentence before the code cell: “Focus on one idea here: `handle_customer_message()` is the integration point where app rules sit around the agent run.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Exercise section / extension test instructions  
**Quote:** `Run handle_customer_message() but pass extended_support_agent\n            (you'll need to modify handle_customer_message or create a new version)`  
**What the student experiences:** The exercise correctly asks them to extend the agent, but the notebook never explicitly points out that the reusable way to do that is to make the pipeline function accept an `agent` parameter. The student may feel like they’re being asked to discover a design move that the lesson itself didn’t name.  
**Smallest fix:** Add one sentence before the exercises: “A useful extension pattern is to parameterize your orchestration function with `agent`, so you can swap in upgraded agent versions without rewriting the approval flow.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** demo before the multi-turn conversation run  
**Quote:** `conversation = [\n    "Hi, my order is ORD-001. I need help with my headphones.",\n    "They stopped working after 3 days. Can I get a refund?",\n    "What's your return policy?",\n]`  
**What the student experiences:** The student sees a multi-turn session demo, but the notebook never explicitly lands why session memory matters in customer support beyond “it remembers stuff.” They need the practical reason: customers shouldn’t have to restate order IDs and issue context every turn.  
**Smallest fix:** Add one sentence in the markdown immediately before this demo: “This matters in real support flows because the customer shouldn’t have to repeat their order ID and problem details on every turn.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

## Gemini Review

This notebook passed third-pass review with four findings. The overall structure is strong, using a capstone to integrate concepts from the previous week effectively. The main comprehension issues stem from inconsistencies between different versions of code blocks within the notebook, which create direct contradictions and block the student from completing the exercises as written.

---

**Severity:** `[lost why]`
**Location:** Phase 2: Define the Guardrail
**Quote:**
```python
@input_guardrail
async def support_topic_guardrail(
    ctx: RunContextWrapper,
    agent: Agent,
    input: str | list[TResponseInputItem]
) -> GuardrailFunctionOutput:
    """Block requests unrelated to customer support."""

    result = await Runner.run(topic_checker, input, context=ctx.context)
    # ...
```
**What the student experiences:** "I see that you can run an agent inside a guardrail, but why would I do that instead of a simple keyword check? This seems really complicated and expensive for just checking the topic."
**Smallest fix:** Add a one-sentence markdown cell before the code, explaining the rationale. Example: "A simple keyword check can be brittle. Using a dedicated classifier agent makes the guardrail more robust against typos and conversational language."
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

**Severity:** `[unexplained term]`
**Location:** Phase 2: Define the Guardrail
**Quote:**
```python
async def support_topic_guardrail(
    ctx: RunContextWrapper,
    agent: Agent,
    input: str | list[TResponseInputItem]
) -> GuardrailFunctionOutput:
```
**What the student experiences:** "What are `RunContextWrapper` and `TResponseInputItem`? They weren't in the guardrails lesson, and I don't know where they come from. Do I need to understand and import these complex types to build my own guardrails?"
**Smallest fix:** Remove the specific type hints for `ctx` and `input`, as they are not necessary for the student to understand the pattern and may add cognitive load. The function will work correctly with `async def support_topic_guardrail(ctx, agent, input)`.
**Blocks transfer:** Yes
**Outline-backed:** No

---

**Severity:** `[confidence gap]`
**Location:** Markdown cells immediately before the main demo run
**Quote:**
Cell 1: `The $149.99 refund exceeds the $100 threshold — you'll see the approval prompt before the transaction completes.`
Cell 2: `⚠️ Turn 2 will pause for refund approval — ORD-001 is $149.99, above the $50 auto-approve threshold.`
**What the student experiences:** "Is the auto-approve threshold $100 or $50? These two sentences contradict each other. I'm not sure what to expect from the demo, and I'm worried I missed something."
**Smallest fix:** Remove one of the two conflicting markdown cells and ensure the text in the remaining one matches the value of `AUTO_APPROVE_THRESHOLD` in the runnable code cell (`50.0`).
**Blocks transfer:** No
**Outline-backed:** No

---

**Severity:** `[confidence gap]`
**Location:** Phase 4: The Full Pipeline (function definition) and Practice Exercise 1
**Quote:**
(from code)
```python
async def handle_customer_message(message, session, auto_approve=True):
```
(from exercise)
```python
# TODO 3: ... Run handle_customer_message() but pass extended_support_agent
```
**What the student experiences:** "The exercise tells me to pass my new agent to `handle_customer_message`, but the function definition doesn't have a parameter for an agent. How am I supposed to do this? I can't complete the exercise without rewriting the function myself."
**Smallest fix:** Update the function signature in the runnable code cell to accept an optional agent: `async def handle_customer_message(message, session, auto_approve=True, agent=None):`. Then, at the start of the function body, add `run_agent = agent or support_agent` and change the two `Runner.run()` calls to use `run_agent`.
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Grok Review

**Third-Pass Review — Notebook 26**

This notebook is in very good shape overall. A student coming straight from Notebook 25 (who already knows sessions, guardrails, human-in-the-loop, and tracing) can follow the capstone's intent and see how the pieces integrate. The layered safety story (read vs write tools, guardrail first, approval on refunds) is clear and true to Capstone #3's identity.

I found four genuine comprehension issues a first-time student would hit.

---

**Severity:** `[lost thread]`

**Location:** Guardrail section (markdown cell immediately before the "Phase 3" code cell)

**Quote:** 
```markdown
class SupportTopicCheck(BaseModel):
    is_support_related: bool
    reasoning: str

topic_checker_instructions = (
    "Determine if the message is related to customer support for an online store.\n"
    ...
)
```

**What the student experiences:** "Why is there a full block of Python code inside a markdown cell right before an almost-identical code cell? Is the first one explanation or something I'm supposed to run? Which version is correct?"

**Smallest fix:** Delete the markdown cell that contains runnable code. Keep only the clean code cell that follows it (the one with proper type hints `RunContextWrapper` and `TResponseInputItem`).

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`

**Location:** Phase 4: The Full Pipeline (the long `handle_customer_message` function)

**Quote:** 
```python
while result.interruptions:
    state = result.to_state()

    for interruption in state.get_interruptions():
        tool_name = interruption.raw_item.name
        args = json.loads(interruption.raw_item.arguments)
        ...
        state.approve(interruption)
        ...
        result = await Runner.run(support_agent, state)
```

**What the student experiences:** "I see that refunds trigger an approval prompt, but I have no idea how this interruption/state machinery actually works. If I wanted to add approval to *my* own tool in a different project, I wouldn't know where to start."

**Smallest fix:** Add one short prose sentence immediately before the `while result.interruptions:` block:

"The SDK pauses execution and populates `result.interruptions` when it hits a tool marked `needs_approval=True`. We convert the result to a mutable state, decide whether to approve or reject, then resume the run."

**Blocks transfer:** Yes

**Outline-backed:** Yes (the outline explicitly calls for "explicit pause point that reinforces read vs write tool safety")

---

**Severity:** `[unexplained term]`

**Location:** Phase 1: Define the Tools (right after the `process_refund` definition)

**Quote:** 
```python
@function_tool(needs_approval=True)
def process_refund(order_id: str, amount: float, reason: str) -> str:
```

**What the student experiences:** "I know from Lesson 24 that we can require human approval, but I don't remember seeing `needs_approval=True` on the decorator before. Is this new? What exactly does the SDK do when it sees this flag?"

**Smallest fix:** Change the comment above the function to:

```python
# `needs_approval=True` tells the SDK to pause before executing this tool
# and populate `result.interruptions` (handled in the pipeline below).
@function_tool(needs_approval=True)
```

**Blocks transfer:** No (student could still run the notebook), but it would leave a small nagging gap.

**Outline-backed:** Yes

---

**Severity:** `[lost why]`

**Location:** Tracing subsection (the short markdown cell under Phase 4)

**Quote:** 
"Every `Runner.run()` call in this pipeline creates a trace automatically. Open the [OpenAI dashboard](https://platform.openai.com/traces) to inspect the guardrail check, tool calls, and approval flow in a single trace view."

**What the student experiences:** "I already know tracing is automatic from Lesson 25, but I don't see *why it matters* in this production customer-service context. What would I actually look for in these traces that would help me debug a real support agent?"

**Smallest fix:** Add one sentence after the existing text:

"Look for the guardrail span (to see why a request was blocked), the interruption span on `process_refund`, and the token count on multi-turn sessions — these are the signals you monitor in production."

**Blocks transfer:** Yes — the student would miss the operational value of tracing in the exact scenario this capstone is teaching.

**Outline-backed:** Yes (outline explicitly lists "Tracing and monitoring")

---

**Summary**

The notebook is close to passing. The duplicate code cell in the guardrail section is the biggest immediate student friction point. The other three findings are small but meaningful gaps in "why" and "how would I use this myself."

No other comprehension blockers jumped out. The capstone identity (safety + production reliability) is preserved, the exercise correctly pushes evaluation and extension, and the Key Takeaways land the layered architecture cleanly.

Ready for Gemini instructions once these four items are addressed.

---

## DeepSeek Review

# Third-Pass Review — Notebook 26: Capstone #3 — Production Customer Service Agent

This notebook has multiple comprehension issues that would confuse a motivated student. The most serious are duplicate code cells with contradictory implementations and an exercise section structured backwards. Several findings would block a student from completing the demo or understanding what's happening.

---

**Finding 1: [lost thread] — Duplicate guardrail definitions in Phase 2**

**Location:** Phase 2 — two consecutive code cells after the `## 🛡️ Phase 2: Guardrails` markdown cell

**Quote (first code cell):**
```python
class SupportTopicCheck(BaseModel):
    is_support_related: bool
    reasoning: str
...
@input_guardrail
async def support_topic_guardrail(
    ctx, agent: Agent, input
) -> GuardrailFunctionOutput:
```
**Quote (second code cell):**
```python
class SupportTopicCheck(BaseModel):
    is_support_related: bool
    reasoning: str
...
@input_guardrail
async def support_topic_guardrail(
    ctx: RunContextWrapper,
    agent: Agent,
    input: str | list[TResponseInputItem]
) -> GuardrailFunctionOutput:
```

**What the student experiences:** Two nearly identical code cells with the same class, same instructions, same agent, and same guardrail function. The only differences are type annotations (`RunContextWrapper`, `TResponseInputItem`) that appear in the second cell but are not imported. The student has no way to know which cell to run, whether both are needed, or why the material appears twice. If they run both, the second overwrites the first — but the second will fail because `RunContextWrapper` is not in the setup imports. The demo then breaks on a mysterious `NameError`.

**Smallest fix:** Delete the second code cell entirely. The first version (without `RunContextWrapper`/`TResponseInputItem`) runs correctly with the existing imports. No further changes needed.

**Blocks transfer:** Yes — the student cannot complete the demo with an error on import.

**Outline-backed:** Yes — the outline promises a working capstone; duplicate broken code blocks that.

---

**Finding 2: [lost thread] — Duplicate pipeline function definitions in Phase 4**

**Location:** Phase 4 — two consecutive code cells after `## 🚀 Phase 4: The Full Pipeline`

**Quote (first cell, threshold 100.0):**
```python
AUTO_APPROVE_THRESHOLD = 100.0

async def handle_customer_message(message, session, auto_approve=True, agent=None):
```
**Quote (second cell, threshold 50.0, no agent parameter):**
```python
AUTO_APPROVE_THRESHOLD = 50.0

async def handle_customer_message(message, session, auto_approve=True):
```

**What the student experiences:** Two consecutive cells define the same function with different thresholds and different signatures. The second cell overwrites the first — the student sees both and has no clue which is the "real" one. The threshold text in an earlier markdown cell says "$100 threshold" but the active code has 50.0. The exercise TODO references an `agent` parameter that only exists in the overwritten first version.

**Smallest fix:** Delete the first cell (the one with `agent=None` and threshold 100.0). Keep only the second cell. Update the earlier markdown cell that reads "The $149.99 refund exceeds the $100 threshold" to say "$50 threshold" to match the active code.

**Blocks transfer:** Yes — the student cannot reason about the approval flow when two contradictory thresholds coexist.

**Outline-backed:** Yes — the outline calls for "Human-in-the-loop for refund approvals — explicit pause point." The student needs to see one clear threshold, not two conflicting ones.

---

**Finding 3: [lost why / confidence gap] — No explanation of what happens when the input prompt fires**

**Location:** The demo conversation cell and its preceding markdown cells

**Quote (markdown before demo):**
```
⚠️ Turn 2 will pause for refund approval — ORD-001 is $149.99, above the $50 auto-approve threshold.
```

**What the student experiences:** The student knows a pause will happen from the warning, but the `handle_customer_message` function has `auto_approve=True` as default, which sounds like "everything is automatic." When Turn 2 actually pauses with an `input()` prompt, the student may hesitate — is this expected? Should they type "yes" or "no"? What happens if they type anything else? The label "auto_approve" directly contradicts the behavior of the demo (pausing for manual input), which erodes confidence.

**Smallest fix:** In the markdown cell before the conversation demo, add one sentence: "You'll see an `input()` prompt at Turn 2 — type `yes` to approve the refund." Alternatively, rename the parameter to `auto_approve_small` or `auto_approve_threshold` to make it clear the threshold still applies.

**Blocks transfer:** No — the student can guess, but they may not feel confident.

**Outline-backed:** Yes — the outline calls for "Human-in-the-loop for refund approvals — explicit pause point." The student needs to understand what the pause looks like and what to do.

---

**Finding 4: [pacing break] — Exercise section starts with a code cell before any markdown header**

**Location:** After the Cleanup code cell and its following markdown divider

**Quote (cell order):**
```
[divider markdown cell]
[CODE CELL — starts with `# --------------------------------------------------------------\n# 💪 Exercise 1: Extend the Production Agent` — contains all TODOs and scaffolding]
[MARKDOWN CELL — `### Exercise 1: Extend the Production Agent\n\n*Covers: agent extension...`]
```

**What the student experiences:** The student sees a divider, then a code cell full of comments and TODOs, then a markdown cell with the exercise header. They have to read through the code comments before seeing the header that frames the exercise. This reverses the standard pattern (header → description → code cell) and disorients the student about where the exercise begins.

**Smallest fix:** Move the markdown cell (`### Exercise 1: Extend the Production Agent...`) before the code cell. Add a `## 💪 Practice Exercises` markdown header before it to match the standard format. Keep the code cell's comment header intact.

**Blocks transfer:** No — the student can still figure out the exercise, but the structure is confusing.

**Outline-backed:** Yes — the exercise section is outline-backed and should follow the established pattern.

---

**Finding 5: [unexplained term] — Missing `## 💪 Practice Exercises` markdown header**

**Location:** The area between the Cleanup section and the exercise code cell

**Quote (missing header):** There is no `## 💪 Practice Exercises` markdown cell anywhere in the notebook. The exercise section begins directly with the code cell's comment header.

**What the student experiences:** The student reads through the demo and cleanup, then suddenly encounters a code cell full of TODOs with no intermediate header saying "Practice Exercises" or "Practice Exercise." They have to infer from the `💪` emoji in the code comments that this is exercise content. A clear section header signals the transition from "watching" to "doing" — without it, the student may miss the boundary.

**Smallest fix:** Add a single markdown cell: `## 💪 Practice Exercises` (plural, since the exercise contains four TODO extensions) between the Cleanup's trailing markdown divider and the exercise code cell.

**Blocks transfer:** No — the student can still find the exercises, but the transition is less clear.

**Outline-backed:** Yes — Practice Exercises are specified in the outline.

---

**Assessment summary:** This notebook has two serious blockers (Findings 1 and 2 — duplicate broken code cells) and two moderate clarity issues (Findings 3 and 4). The duplicates appear to be editing artifacts where a refined version was pasted alongside the original without deleting the first copy. These are the highest-priority fixes because they directly prevent the student from completing the demo and understanding the approval flow.
