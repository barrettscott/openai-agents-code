# Review Report: Parallel Execution
Generated: 2026-05-10 | Notebook: /Users/scott/Dropbox/Notebooks/openai-agents/Week_3_Multi_Agent_Systems/16_Parallel_Execution.ipynb
Models: Anthropic (claude-opus-4-7 (CLI/Max)) · OpenAI (gpt-5.4) · Gemini (gemini-2.5-pro) · Grok (grok-4.20-0309-reasoning) · DeepSeek (deepseek-reasoner) · Dedup (claude-opus-4-7 CLI/Max)

---

# Grouped Findings: Parallel Execution

# Third-Pass Synthesis — Notebook 16: Parallel Execution

Five reviewers converged on a small, concrete punch list. The strongest signal: the timeout section needs structural framing (5/5 reviewers), with two reviewers also catching a duplicated markdown cell that is the root cause of the disorientation. Partial-result handling drew transfer-confidence concerns from four reviewers, each flagging a different angle of the same underlying gap.

---

## Findings

---

**Severity:** `[lost thread]` + `[pacing break]`

**Location:** Between Part 6's "Why This Works" cell and the Practice Exercises — the `run_with_timeout` section

**Reviewers:** 5/5 — Anthropic, OpenAI, Gemini, Grok, DeepSeek

**Quote:** Section opens cold with `async def run_with_timeout(agent, message, timeout_seconds=10):` — no Part header, no intro markdown. Gemini and DeepSeek additionally flag that the function definition appears **twice** — once in a markdown cell (rendered as plain text, no code fence) and again in a real code cell immediately below.

**What the student experiences:** Two compounding problems. First, the duplicated markdown cell looks like a typo or an instruction the student missed — "do I run this? read it? why is it duplicated?" Second, even after the duplicate, the timeout content arrives without a Part header after a clearly numbered Parts 1–6 sequence, so it reads as a stray helper rather than the third major reliability pattern. Key Takeaways lists timeouts as a top-level point, conflicting with the body presentation.

**Smallest fix:** Two-step edit:
1. Delete the duplicated markdown cell (copy-paste artifact)
2. Insert `## ⏱️ Part 7: Timeouts and Fallbacks` with one-sentence intro before the code cell: *"`return_exceptions=True` handles errors, but a hung or slow agent can still block the whole `gather()`. Timeouts give every task a deadline."*

**Blocks transfer:** No (Grok says yes, but majority view is the content is intact, only framing is missing)

**Outline-backed:** Yes — outline explicitly lists "Timeout and fallback behavior"

---

**Severity:** `[confidence gap]`

**Location:** Part 6 → "Partial Result Handling" cell

**Reviewers:** 4/5 — Anthropic, OpenAI, Grok, DeepSeek (each flagged a different facet of the same transfer gap)

**Quote:**
> ```python
> good = [(t, r) for t, r in zip(topics, results) if not isinstance(r, Exception)]
> ...
> if len(good) == 0:
>     print("❌ All tasks failed — abort")
> elif len(good) < 2:
>     print("⚠️  Too few results — consider retrying failed tasks")
> ```

**What the student experiences:** Three overlapping confidence problems: (a) the thresholds `0` and `< 2` look like a standard pattern but are tied to this demo's three tasks — a student with five specialists has no guide for "enough"; (b) the demo uses an artificial `bad_task` list rather than the `market_agent`/`tech_agent`/`risk_agent` from Part 4, breaking the link to the happy-path pattern; (c) `results` and `topics` are reused from earlier cells without acknowledgment, briefly suggesting a missing cell.

**Smallest fix:** Two short additions to the existing cell:
1. Top of cell comment: `# Using results from the previous cell, topics from the first demo`
2. After the synthesis print, append: *"In your own pipeline, replace the artificial `bad_task` list with `asyncio.gather(...)` of your real specialist agents from Part 4, and set the threshold based on which results are critical — e.g., abort if a required specialist failed, proceed if at least one of each category succeeded."*

**Blocks transfer:** Yes — without this, students copy `< 2` literally or guess

**Outline-backed:** Yes — outline lists "Partial result handling"

---

**Severity:** `[unexplained term]`

**Location:** Part 4 intro and Key Takeaways → "Fan-out / Fan-in is the full pattern"

**Reviewers:** 2/5 — Anthropic, Gemini

**Quote:** Key Takeaways introduces "Fan-out" / "Fan-in" as a named pattern, but the body never labels it. "Fan-out" appears once in passing in Part 4's Why This Works; "Fan-in" debuts in the takeaways.

**What the student experiences:** Hitting "fan-in" in the takeaways feels like a vocab quiz on terms that were never taught. The student has seen the pattern in code but has no formal name to use, recognize, or talk about with others.

**Smallest fix:** Add one sentence to Part 4's intro markdown: *"This is the **fan-out / fan-in** pattern — fan out to launch specialists in parallel, fan in to merge their outputs."*

**Blocks transfer:** No

**Outline-backed:** No (outline says "Merging results from parallel agents" — pattern naming aids transfer but isn't required)

---

**Severity:** `[unexplained term]`

**Location:** Async Recap section

**Reviewers:** 2/5 — Gemini, Grok (with different fixes)

**Quote:** *"`asyncio.gather()` — launches multiple coroutines at the same time and waits for all of them."*

**What the student experiences:** "Coroutine" is brand new for students coming from Notebook 15, where they only saw `await Runner.run(...)`. The term is used as if they should already know it.

**Reviewer split on fix:** Gemini wants to *replace* "coroutine" with "async task" (simpler, less jargon). Grok wants to *gloss* it: "async tasks (called coroutines)" (preserves the real term so students recognize it later in docs/Stack Overflow). Grok's approach is stronger — students will encounter "coroutine" in real-world Python async code and shouldn't be sheltered from the standard term.

**Smallest fix:** Change to: *"`asyncio.gather()` — launches multiple async tasks (called coroutines) at the same time and waits for all of them."*

**Blocks transfer:** No

**Outline-backed:** Yes

---

## Lower-priority findings (1 reviewer each — include if doing a polish pass)

- **OpenAI, Part 3 [confidence gap]:** Add one sentence naming the conversion pattern: *"In your own code, the conversion is usually simple: take several independent `await Runner.run(...)` calls and place those same coroutines inside one `await asyncio.gather(...)` call instead."* — Outline-backed, Yes; Blocks transfer, Yes.
- **OpenAI, Part 4 [lost why]:** Add one sentence to Part 4 intro: *"This is the pattern to use when one user request needs multiple independent perspectives before you produce a single answer."* — Largely subsumed by the fan-out/fan-in naming fix above.
- **OpenAI, Part 6 [pacing break]:** Add a one-sentence roadmap under Part 6 intro: *"We'll handle this in three steps: keep partial results, decide whether there's enough to continue, then add timeouts so slow tasks don't block the workflow."* — Becomes less necessary once the Part 7 header lands, since timeouts will no longer be inside Part 6.
- **Grok, Part 5 [lost why]:** Append to the post-table paragraph: *"Use parallel mini-model specialists when you need several independent perspectives quickly and the total token cost is still lower than one long reasoning-model call."*

---

## Summary

Four primary findings, all small. The Part 7 header + duplicate-cell deletion is the single highest-leverage edit (5/5 reviewers, fixes the most disorienting moment in the notebook, and is the only one that is structurally broken rather than just unclear). The partial-result handling fix is the only one tagged "blocks transfer" — students will copy the demo thresholds verbatim without it. Naming fan-out/fan-in and glossing "coroutine" are quick adds that close vocabulary gaps without adding length.

---

# Individual Reviews

The grouped findings above are your primary working surface. The full individual reviews below are for verifying nuance.

---

## Anthropic Review

# Third-Pass Review — Notebook 16: Parallel Execution

## Findings

---

**Severity:** `[unexplained term]`

**Location:** Key Takeaways → "Fan-out / Fan-in is the full pattern"

**Quote:**
> **Fan-out / Fan-in is the full pattern:**
> - Fan out: launch specialists in parallel
> - Fan in: merge all outputs with a synthesis agent

**What the student experiences:** "Fan-out" was used once in passing in Part 4's Why This Works ("Fan-out reduces total latency..."), and "Fan-in" appears here for the first time. The student has seen the pattern in code (Part 4) but never had it labeled as a named pattern in the body. Hitting "fan-in" in the takeaways feels like a vocab quiz on terms that were never taught.

**Smallest fix:** In Part 4's intro cell, add one sentence naming the pattern: *"This is the **fan-out / fan-in** pattern — fan out to launch specialists in parallel, fan in to merge their outputs."* That single addition lets the takeaway language land.

**Blocks transfer:** No

**Outline-backed:** No (outline says "Merging results from parallel agents" — not specifically named patterns, but naming aids transfer)

---

**Severity:** `[lost thread]`

**Location:** The timeout section (cells between Part 6's Why This Works and Practice Exercises)

**Quote:** The section opens cold with `async def run_with_timeout(agent, message, timeout_seconds=10):` — no Part header, no intro markdown cell explaining why timeouts matter or how they relate to Part 6's failure handling.

**What the student experiences:** After absorbing Part 6 (gather failures, return_exceptions, partial results), the student hits a `run_with_timeout` function definition with no setup. They've been navigating clearly numbered Parts (1–6); now timeouts arrive without a header, so it reads like a stray helper rather than the third major reliability pattern in the notebook. The Key Takeaways list timeouts as a top-level point, which conflicts with the body presentation.

**Smallest fix:** Add a Part 7 header and one-sentence intro before the function definition:
> ## ⏱️ Part 7: Timeouts and Fallbacks
>
> `return_exceptions=True` handles errors, but a hung or slow agent can still block the whole `gather()`. Timeouts give every task a deadline.

**Blocks transfer:** No

**Outline-backed:** Yes — outline explicitly lists "Timeout and fallback behavior" as a teaching beat for this lesson

---

**Severity:** `[confidence gap]`

**Location:** Part 6 → Partial Result Handling cell

**Quote:**
> ```python
> if len(good) == 0:
>     print("❌ All tasks failed — abort")
> elif len(good) < 2:
>     print("⚠️  Too few results — consider retrying failed tasks")
> else:
>     print("✅ Enough results — proceed with synthesis")
> ```

**What the student experiences:** The thresholds (`0`, `< 2`, `else`) are presented as if they're a standard pattern, but they're tied to this demo's three tasks. A student copying this into their own project with five specialists would have no guide for what "enough" means. They see a working shape but can't transfer the decision rule.

**Smallest fix:** Add one sentence above or below the cell:
> *In your own pipeline, set the threshold based on which results are critical — e.g., abort if any required specialist failed, proceed if at least one of each category succeeded.*

**Blocks transfer:** Yes — the student would either copy the `< 2` literally or guess

**Outline-backed:** Yes — outline lists "Partial result handling" explicitly

---

## Summary

Three findings — all small additions, no structural rewrites required. The notebook teaches the parallel mechanics clearly; the gaps are at the seams (naming the fan-out/fan-in pattern, framing timeouts as Part 7, generalizing the partial-result thresholds). Strongest single fix is adding the Part 7 header — it's outline-backed and fixes the most disorienting moment in the notebook.

---

## OpenAI Review

**Severity:** [confidence gap]  
**Location:** Part 3: “Parallel with `asyncio.gather()`”  
**Quote:** "`asyncio.gather()` runs multiple coroutines concurrently and returns their results in the same order they were passed in."  
**What the student experiences:** The student sees the syntax work, but still may not know what to change in their own project to go from sequential code to parallel code. The pattern is implicit rather than named: “replace separate awaited runs with one `gather(...)` call over the same `Runner.run(...)` coroutines.”  
**Smallest fix:** Add one sentence after the intro line: “In your own code, the conversion is usually simple: take several independent `await Runner.run(...)` calls and place those same coroutines inside one `await asyncio.gather(...)` call instead.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** Part 4: “Parallel + Merge”  
**Quote:** "The real pattern: run specialists in parallel, then merge their outputs into one final response with a synthesis agent."  
**What the student experiences:** The student understands what the code is doing, but not yet why this is a distinct real-world pattern instead of just “another demo.” They need one concrete use case to connect this to agent design: research, review generation, triage summaries, etc.  
**Smallest fix:** Add one sentence to the Part intro: “This is the pattern to use when one user request needs multiple independent perspectives before you produce a single answer.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [pacing break]  
**Location:** Part 6  
**Quote:** "## ⚠️ Part 6: When One Task Fails"  
**What the student experiences:** This section asks the student to absorb several new ideas in one stretch: default `gather()` failure behavior, `return_exceptions=True`, partial-result decision logic, and timeout/fallback handling. Even though each piece is individually clear, together they blur into one broad “error handling” block, so the student may lose the main takeaway.  
**Smallest fix:** Add a one-sentence roadmap directly under the Part 6 intro: “We’ll handle this in three steps: keep partial results, decide whether there’s enough to continue, then add timeouts so slow tasks don’t block the workflow.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** “### Partial Result Handling”  
**Quote:** "With partial results you have three options depending on how critical the failed task is."  
**What the student experiences:** The student sees the branching logic, but may not understand why “continue with partial results” is acceptable in some agent workflows and dangerous in others. Without that framing, the demo feels like arbitrary control flow rather than a design decision.  
**Smallest fix:** Add one sentence after the existing line: “For example, a summary or research workflow can often continue with two of three inputs, while a workflow that needs every required check should abort.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [lost thread]  
**Location:** Timeout section, immediately after Part 6  
**Quote:** "async def run_with_timeout(agent, message, timeout_seconds=10):"  
**What the student experiences:** The lesson abruptly shifts into timeout code without a Part header or framing sentence, so it feels like a disconnected helper rather than the final failure-handling pattern promised in the Topics list. A first-time student may wonder, “Are we still in Part 6, or is this a new idea?”  
**Smallest fix:** Insert a short markdown header before the helper function, such as: `### Timeout and Fallback Behavior` with one sentence: “A different failure mode is not crashing, but hanging too long — timeouts let you stop waiting and choose a fallback.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

## Gemini Review

This notebook passed third-pass review with three findings. The core concepts are taught well, but a copy-paste error and two small clarity gaps could cause minor confusion.

---

**Severity:** `[unexplained term]`
**Location:** ⚡ Async Recap
**Quote:** `await` — pauses until one coroutine finishes before moving to the next line.
**What the student experiences:** "I know `async` functions and `await`, but I'm not sure what a 'coroutine' is. Is it just another name for an async function, or is it something else? The term is used as if I should already know it."
**Smallest fix:** Replace the term "coroutine" with "async task" to use more common language. The line would read: `await` — pauses until an async task finishes before moving to the next line.
**Blocks transfer:** No
**Outline-backed:** No

---

**Severity:** `[confidence gap]`
**Location:** Part 4: Parallel + Merge
**Quote:** ## 🤝 Part 4: Parallel + Merge
**What the student experiences:** "I understand the code, which runs agents in parallel and then combines their results. The Key Takeaways calls this 'Fan-out / Fan-in', but the notebook never named the pattern before showing it. I wouldn't feel confident that I'd learned a formal pattern I could use or talk about elsewhere."
**Smallest fix:** Add one sentence to the Part 4 intro markdown cell to explicitly name the pattern: "This pattern — running multiple tasks in parallel (fan-out) and then combining their results (fan-in) — is a common and powerful way to build fast agent systems."
**Blocks transfer:** No
**Outline-backed:** No

---

**Severity:** `[pacing break]`
**Location:** After Part 6, before the timeout demo.
**Quote:** (There are two consecutive cells defining the `run_with_timeout` function — one is a markdown cell with a code block, the next is a code cell with the identical code.)
**What the student experiences:** "I just saw this function definition in a markdown block, and now here it is again in a runnable code block. Is this a typo? Am I supposed to do something different with each one? This duplication breaks the flow and makes me wonder if I've missed something."
**Smallest fix:** Delete the first markdown cell containing the `run_with_timeout` code. Before the remaining code cell, add a new markdown cell with a proper Part header: `## ⏱️ Part 7: Timeout and Fallback Behavior`.
**Blocks transfer:** No
**Outline-backed:** Yes

---

## Grok Review

**Severity:** `[unexplained term]`

**Location:** Async Recap section

**Quote:** "`asyncio.gather()` — launches multiple coroutines at the same time and waits for all of them."

**What the student experiences:** I saw `await Runner.run(...)` a few times in Notebook 15, but "coroutine" is brand new. I don't have a clear picture of what is actually being passed to `gather()`, so the recap feels slightly over my head even though the rest of the sentence makes sense.

**Smallest fix:** Change the line to: "`asyncio.gather()` — launches multiple async tasks (called coroutines) at the same time and waits for all of them."

**Blocks transfer:** No

**Outline-backed:** Yes

---

**Severity:** `[pacing break]`

**Location:** Part 6 (failure handling) through the timeout helper and demo (the block that starts with the `async def run_with_timeout` markdown cell)

**Quote:** "`return_exceptions=True` converts exceptions from \"crash the gather\" to \"return alongside results.\" ... async def run_with_timeout(agent, message, timeout_seconds=10):"

**What the student experiences:** I just finished digesting `return_exceptions`, partial-result lists, and the synthesis logic when the notebook immediately introduces another new asyncio primitive (`asyncio.wait_for`) plus a helper function and two demo calls. I can't tell whether the main point of this section is exception handling, partial results, or timeouts — I'm holding three new concepts at once right before the exercises.

**Smallest fix:** Insert a new header `## ⏰ Part 7: Timeouts and Fallbacks` (with one-sentence intro) immediately before the `run_with_timeout` definition cell.

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`

**Location:** End of the "Partial Result Handling" code cell (the final synthesis block inside the `if len(good) >= 2` branch)

**Quote:** "partial_synthesis_agent = Agent(...) ... result = await Runner.run(partial_synthesis_agent, input=...)"

**What the student experiences:** I see the `good`/`failed` filtering pattern, but because it's demonstrated with the artificial `bad_task` list instead of the real `market_agent`/`tech_agent`/`risk_agent` from earlier in the notebook, I'm not sure what the equivalent code would look like in my own project. The connection between the happy-path Part 4 and this error-handling code isn't explicit enough for me to confidently combine them.

**Smallest fix:** After the final `print(f"\nSynthesis:\n{result.final_output[:300]}")` line, add: "In a real project you would replace the artificial `bad_task` list with your `asyncio.gather(...)` of the actual specialist agents shown in Part 4."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[lost why]`

**Location:** Part 5: Cost & Speed Tradeoffs (the paragraph after the table)

**Quote:** "One exception: running several mini-model agents in parallel can be faster and cheaper than a single reasoning-model call for the same work."

**What the student experiences:** The table is clear on latency vs cost, but after I finish reading I still don't have a crisp answer to "when would I actually choose parallel specialists over one bigger model in a real application?" The exception sentence gestures at it but doesn't give me a concrete scenario I can picture using tomorrow.

**Smallest fix:** Append this sentence to the existing paragraph: "Use parallel mini-model specialists when you need several independent perspectives quickly and the total token cost is still lower than one long reasoning-model call."

**Blocks transfer:** No

**Outline-backed:** Yes

---

This notebook is otherwise strong — the core fan-out/fan-in pattern is demonstrated cleanly, the "Why This Works" cells land the key insights, and the exercises directly reinforce the teaching beats from the outline. With the four small additions above a student finishing Notebook 15 would leave this lesson confident they could apply parallel execution in their own multi-specialist projects.

---

## DeepSeek Review

## Third-Pass Review — Notebook 16: Parallel Execution

This notebook is in solid shape. The core concepts are taught clearly, and the sequential → parallel → async patterns build logically. However, there are three issues that would trip up a student at this level — one of them is a structural error that would cause significant confusion.

---

**Severity:** `[lost thread]`  
**Location:** Immediately after the "Why This Works" cell for Part 6, before the Practice Exercises section.  
**Quote (markdown cell, verbatim):**  
```
async def run_with_timeout(agent, message, timeout_seconds=10):
    """Run an agent with a timeout. Returns output or None if timed out."""
    try:
        result = await asyncio.wait_for(
            Runner.run(agent, input=message),
            timeout=timeout_seconds
        )
        return result.final_output
    except asyncio.TimeoutError:
        return None

# --------------------------------------------------------------
print("✅ run_with_timeout() ready")
```
**What the student experiences:** The student sees this function definition rendered as plain text in a markdown cell. There is no code fence, no syntax highlighting, and no indication that it's meant to be read, not run. The very next cell is a real code cell with exactly the same function. The student is confused: do they run the markdown? Is it a description? Why is it duplicated? The flow of the notebook is broken.

**Smallest fix:** Delete the markdown cell entirely. It is a copy-paste artifact — the real code cell below does the work.

**Blocks transfer:** No (the real code cell is present and correct)  
**Outline-backed:** No

---

**Severity:** `[lost thread]`  
**Location:** The section between Part 6 and the Practice Exercises, currently starting with the duplicated markdown cell.  
**Quote (the section has no Part header — the student moves from Part 6 directly into a raw function definition):**  
There is no `## ⏱️ Part 7: Timeout & Fallback` header or any equivalent.

What the student experiences: After finishing Part 6 (handling failed tasks), the student sees a raw code block in a markdown cell, then a real code cell, then a `#### Run with Timeout` sub-header. They do not realize a new major section has started. The topic — timeouts — is distinct from Part 6 (exception handling), and the student loses the structural cue that they've entered a new teaching beat.

**Smallest fix:** After removing the duplicated markdown cell, insert a `## ⏱️ Part 7: Timeout & Fallback` markdown header cell between the last "Why This Works" of Part 6 and the `run_with_timeout` code cell. Leave the existing `#### Run with Timeout` sub-header where it is — it becomes the sub-header *within* Part 7.

**Blocks transfer:** No (the timeout concept is still taught)  
**Outline-backed:** Yes — the course outline lists "Timeout and fallback behavior" as a distinct section.

---

**Severity:** `[confidence gap]`  
**Location:** The "Partial Result Handling" code cell.  
**Quote (code cell):**
```python
# Pattern: collect what succeeded, decide how to proceed
good = [(t, r) for t, r in zip(topics, results) if not isinstance(r, Exception)]
failed = [t for t, r in zip(topics, results) if isinstance(r, Exception)]
```
**What the student experiences:** The student sees two variables — `results` and `topics` — that are not defined in this cell or in any code immediately visible above it. The student knows they persist from earlier cells (the `return_exceptions=True` demo and the first cell of Part 6), but this is the first time the notebook asks them to mentally backtrack to find where a variable was defined. The student briefly worries they missed a cell or that the notebook is out of order.

**Smallest fix:** Add one comment at the top of the cell:
```python
# Using `results` from the previous cell, `topics` from the first demo
```
This reassures the student that the variables are intentionally reused and tells them where to look if they want to verify.

**Blocks transfer:** No (the variables are defined, just not in immediate view)  
**Outline-backed:** Yes (partial result handling is outline-backed, but the issue is a confidence gap in presentation, not missing content)

---

## Summary

The notebook passes third-pass review with three issues. The duplicated markdown cell is the most serious — it will confuse any student who reaches it. The missing Part 7 header is a small structural gap easily fixed. The reuse of `results` without an explanatory comment is a minor confidence gap. Fix these three, and the notebook is clean for a student at this point in the course.
