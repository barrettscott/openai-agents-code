# Review Report: Capstone #4 — MCP-Powered Personal Assistant
Generated: 2026-05-10 | Notebook: /Users/scott/Dropbox/Notebooks/openai-agents/Week_5_MCP_And_Production/29_Capstone_4_MCP_Assistant.ipynb
Models: Anthropic (claude-opus-4-7 (CLI/Max)) · OpenAI (gpt-5.4) · Gemini (gemini-2.5-pro) · Grok (grok-4.20-0309-reasoning) · DeepSeek (deepseek-reasoner) · Dedup (claude-opus-4-7 CLI/Max)

---

# Grouped Findings: Capstone #4 — MCP-Powered Personal Assistant

# Third-Pass Synthesis — Notebook 29: Capstone #4 (MCP-Powered Personal Assistant)

Five reviewers converged on a tight set of comprehension issues — all clustered around Phase 2's introduction of server-level approval. The pattern is consistent: the notebook *uses* `require_approval` and the interruption loop without ever *explaining* them, and then bundles everything into one large demo cell that obscures the lesson.

---

## Finding 1 — `require_approval` and the interruption loop are unexplained

**Severity:** `[unexplained term]`
**Location:** Phase 2 — main code cell, `MCPServerStdio` for Filesystem and the `while result.interruptions:` loop
**Reviewers:** 5/5 (Anthropic, OpenAI, Gemini, Grok, DeepSeek — all flagged this; OpenAI framed it as `[confidence gap]` on the loop specifically, others as `[unexplained term]` on the config)
**Quote:**
```python
require_approval={
    "always": {"tool_names": ["write_file", "create_directory", "move_file", "delete_file"]},
    "never": {"tool_names": ["read_file", "list_directory", "get_file_info", "search_files"]}
}
...
while result.interruptions:
    state = result.to_state()
    for interruption in state.get_interruptions():
        ...
```
**What the student experiences:** Coming straight from Notebook 28 (which only covered `tool_filter` and `list_tools()`), the student has never seen `require_approval`, `interruptions`, `to_state()`, `approve()`, or `reject()`. They can run the cell but have no mental model for the dict shape (`always`/`never`), what an interruption is, or how the pause/inspect/approve/resume loop generalizes. They cannot write this pattern from scratch in their own project.
**Smallest fix:** Add one short markdown cell immediately before the Phase 2 demo, titled something like `### 🔐 Server-Level Approval`, that says:
> `require_approval` lets the MCP server pause the run before specific tools execute. The policy is a dict with `always` (require approval) and `never` (auto-approve); anything not listed runs without approval. When a protected tool is called, `Runner.run()` returns with `result.interruptions` populated. The reusable pattern is: `result.to_state()` → loop through `state.get_interruptions()` → call `.approve()` or `.reject()` on each → resume with `Runner.run(agent, state)`.

**Blocks transfer:** Yes
**Outline-backed:** Yes (outline calls for "Server-level approval before writing or deleting")

---

## Finding 2 — The "saving is a side effect" beat is buried in a code comment

**Severity:** `[lost why]`
**Location:** Phase 2 — comment block above Task 2
**Reviewers:** 3/5 (Anthropic, OpenAI, Grok)
**Quote:**
```
# Note: saving to a file is a write side effect — in production,
# auto-approving bypasses the same controls you just built in Task 1.
```
**What the student experiences:** This is a core teaching beat from the outline ("Saving results is a side effect — handle it with the same care as write actions"), but it lives inside a code comment and the wording is confusing — it mentions "auto-approving" even though Task 2 *does* gate the save with the same approval flow. Students read it once, get confused, and move on without absorbing why a "harmless" save belongs in the same risk bucket as any other write.
**Smallest fix:** Pull it out of the comment into a one-line markdown cell between Task 1 and Task 2:
> Saving results is still a write — `write_file` triggers the same approval flow as any other write. A "harmless" save shouldn't get an exemption.

**Blocks transfer:** Yes — without this, students will design assistants that auto-approve "just saving" and reintroduce the risk approval was meant to prevent.
**Outline-backed:** Yes

---

## Finding 3 — Phase 2 is one giant code cell that buries the lesson

**Severity:** `[lost thread]` / `[pacing break]`
**Location:** Phase 2 — the single ~90-line code cell containing server setup, agent definition, Task 1 with approval loop, and Task 2 with approval loop
**Reviewers:** 4/5 (Anthropic `[lost thread]`, OpenAI `[pacing break]`, Grok `[lost thread]`, DeepSeek `[pacing break]`)
**Quote:** Section header `## 🤖 Phase 2: Build the Assistant` followed by one monolithic cell.
**What the student experiences:** By Task 2, the student has scrolled past `require_approval`, the agent definition, Task 1's prompt, and Task 1's approval loop. The capstone's headline — "approval-gated multi-server assistant" — dilutes into "a long script that does two things." The student doesn't notice that Task 2's approval loop is structurally identical to Task 1's, which is the whole pedagogical point: the same pattern handles both research-with-save and any other write.
**Smallest fix:** Split the cell at the Task 1 / Task 2 boundary into two code cells, with a short `#### Task 2: Web Research + Save` markdown header (and a one-line goal) between them. Keep the `async with` block wrapping both. OpenAI also suggests a one-line roadmap under the Phase 2 header: *"Focus on two ideas: combining multiple MCP servers in one agent, and handling approval interrupts before side effects."*
**Blocks transfer:** No — the code works either way, but the lesson lands much harder when the repeated approval loop is visually a *pattern* rather than a wall of code.
**Outline-backed:** No (structural)

---

## Finding 4 — Practice Exercise gives no template for the approval loop

**Severity:** `[confidence gap]`
**Location:** Practice Exercise scaffolding
**Reviewers:** 2/5 (DeepSeek, Grok)
**Quote:**
```
# TODO: Design a task that exercises filesystem + fetch + your new server
#        Handle any write approvals in the loop
```
**What the student experiences:** The exercise asks the student to "handle any write approvals" but provides no reminder of the method names (`to_state`, `get_interruptions`, `approve`, `reject`) and no loop skeleton. After watching the demo once, most students cannot reproduce the loop from memory and will either skip approval handling or implement it incorrectly. Grok additionally noted Option B ("Another fetch/search approach") is too vague — students have no concrete second-server suggestion.
**Smallest fix:** Add a commented template inside the exercise cell:
```python
# Example approval loop (reuse this pattern):
# while result.interruptions:
#     state = result.to_state()
#     for interruption in state.get_interruptions():
#         print(f"Approval needed: {interruption.raw_item.name}")
#         state.approve(interruption)  # or state.reject(interruption, message=...)
#     result = await Runner.run(assistant, state)
```
And replace Option B with a concrete suggestion (Grok: Memory server `@modelcontextprotocol/server-memory` for persistent notes).
**Blocks transfer:** Yes
**Outline-backed:** Yes (exercise is meant to reinforce the approval pattern)

---

## Finding 5 — Two approval loops are written slightly differently

**Severity:** `[confidence gap]`
**Location:** Phase 2 — Task 1 and Task 2 approval loops
**Reviewers:** 1/5 (Gemini)
**Quote:** The two `while result.interruptions:` blocks differ in whether they print arguments and in how they parse the user's input (one accepts `"y"`, the other doesn't).
**What the student experiences:** "These two loops do the same job but look different — is one better? Which template should I copy?"
**Smallest fix:** Standardize both loops to the more informative version (print tool name + arguments, accept `yes`/`y`).
**Blocks transfer:** Yes per Gemini, but this is a single-reviewer finding and overlaps with Finding 3 — splitting the cell makes the structural identity of the two loops more visible. Combining the standardization with the cell split would resolve both at once.
**Outline-backed:** No

---

## Minor findings (single-reviewer, low priority)

- **OpenAI — "coordination-heavy tasks" in System Architecture:** mild jargon; one-clause clarification (`...like deciding which server to use first, what to read next, and when to save results`) would resolve. `[unexplained term]`, does not block transfer.
- **OpenAI — Phase 2 intro lost why:** the "three servers + approval + reasoning model" line lists ingredients without motivation; one sentence (*"This is the point where MCP stops being a toy demo and starts looking like a real assistant"*) would resolve. Subsumed by the Finding 3 roadmap line if that's added.

---

## Summary

Four of the five findings cluster around a single root cause: the notebook introduces server-level approval (the capstone's headline new concept) entirely through code, with no prose setup, inside a monolithic cell. Three small additions resolve most of it:

1. One markdown cell explaining `require_approval` + the interruption loop **before** Phase 2 (Finding 1).
2. Pull the "saving is a side effect" beat out of a comment into a markdown cell (Finding 2).
3. Split the Phase 2 cell at the Task 1/Task 2 boundary, standardize the two loops while you're there (Findings 3 + 5).

Finding 4 is independent — add a commented loop template and a concrete Option B to the practice exercise.

---

# Individual Reviews

The grouped findings above are your primary working surface. The full individual reviews below are for verifying nuance.

---

## Anthropic Review

# Third-Pass Review — Notebook 29: Capstone #4 — MCP-Powered Personal Assistant

---

**Finding 1**

**Severity:** [unexplained term]

**Location:** Phase 2 — main code cell, the `MCPServerStdio` for Filesystem

**Quote:**
> ```python
> require_approval={
>     "always": {"tool_names": ["write_file", "create_directory", "move_file", "delete_file"]},
>     "never": {"tool_names": ["read_file", "list_directory", "get_file_info", "search_files"]}
> }
> ```

**What the student experiences:** A new parameter appears in code with a non-obvious nested dict structure. The student saw human-in-the-loop in Lesson 24 and `MCPServerStdio` in Lesson 28, but `require_approval` as an MCP-server-level setting — and the `always`/`never` policy shape — is brand new and never explained in prose. They can copy it but won't know how to design the policy on their own project.

**Smallest fix:** Add one short markdown cell or a "Why This Works" line right before the code cell:
> `require_approval` lets the MCP server pause the run before specific tools execute. The policy is a dict with two keys: `always` (always require approval) and `never` (auto-approve, no pause). Anything not listed runs without approval.

**Blocks transfer:** Yes
**Outline-backed:** Yes (outline calls for "Server-level approval before writing or deleting")

---

**Finding 2**

**Severity:** [lost why]

**Location:** Phase 2 — inline comment block above Task 2

**Quote:**
> ```
> # Task 2: Web research + save
> # Note: saving to a file is a write side effect — in production,
> # auto-approving bypasses the same controls you just built in Task 1.
> ```

**What the student experiences:** This is an outline-backed teaching beat ("Saving results is a side effect — handle it with the same care as write actions"), but it's buried in a code comment, and the wording is confusing — the comment mentions "auto-approving" even though Task 2 *does* handle approvals identically to Task 1. The student reads it once, gets confused about what's actually happening, and moves on without absorbing the point.

**Smallest fix:** Pull it out of the comment and into a one-line markdown cell between Task 1 and Task 2 output, with clearer phrasing:
> Saving results is still a write — `write_file` triggers the same approval flow as any other write. A "harmless" save shouldn't get an exemption.

**Blocks transfer:** Yes — without grasping this, students will design assistants that auto-approve "just saving" and reintroduce the risk approval was meant to prevent.
**Outline-backed:** Yes

---

**Finding 3**

**Severity:** [lost thread]

**Location:** Phase 2 — the single large code cell containing server setup, agent definition, Task 1 with approval loop, and Task 2 with approval loop

**Quote:** Section header `## 🤖 Phase 2: Build the Assistant`

**What the student experiences:** By the time the student reaches Task 2's approval loop, they've already scrolled past the `require_approval` config (the lesson's core new beat), the agent definition, Task 1's input prompt, and Task 1's interruption-handling loop. The capstone's headline — "approval-gated multi-server assistant" — gets diluted into "a long script that does two things." A student running this end-to-end may not notice the second approval loop is structurally identical to the first, which is the whole point.

**Smallest fix:** Split the cell at the Task 1 / Task 2 boundary into two code cells with a short `####` markdown header between them (e.g., `#### Task 2: Web Research + Save`). The approval loop pattern repeats — that's the lesson — but the student needs visual separation to see it as a *pattern* rather than a wall of code.

**Blocks transfer:** No — the code works either way, but the pedagogical point lands harder when the repetition is visible.
**Outline-backed:** No (structural, not a content beat)

---

This notebook is otherwise in solid shape. Three findings, one of which (Finding 1) is the only true blocker for student transfer.

---

## OpenAI Review

**Severity:** [unexplained term]  
**Location:** System Architecture  
**Quote:** `the agent makes one tool call at a time; using a stronger model improves those choices on coordination-heavy tasks.`  
**What the student experiences:** The student can infer that `REASONING_MODEL` is “stronger,” but “coordination-heavy tasks” is new jargon here. They may not know what kind of task counts as coordination-heavy in practice, beyond “this demo has multiple servers.”  
**Smallest fix:** Add one short clarifying clause: `...improves those choices on coordination-heavy tasks like deciding which server to use first, what to read next, and when to save results.`  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** Phase 2 intro  
**Quote:** `Three MCP servers, write approval on filesystem, \`REASONING_MODEL\` for decision-making.`  
**What the student experiences:** The notebook tells the student what ingredients are in the demo, but not why this capstone matters beyond “more MCP.” A first-time agent builder may not connect this to a real project pattern like “one assistant that can research, check time, and save work safely.”  
**Smallest fix:** Add one sentence after this line: `This is the point where MCP stops being a toy demo and starts looking like a real assistant: one agent can combine outside information, local files, and side effects in a single request.`  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Phase 2 code cell, approval loop  
**Quote:** `while result.interruptions:`  
**What the student experiences:** The approval flow works, but the key reusable pattern is buried inside a long demo cell. A student can follow the execution, but may not come away knowing, “this is the generic pause/inspect/approve/reject/resume loop I should reuse anytime I enable approvals.”  
**Smallest fix:** Add a short markdown cell immediately before the code cell or right above Task 1 in prose: `This approval loop is the reusable pattern: run the agent, inspect interruptions, approve or reject each action, then resume with the updated state.`  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** Phase 2, Task 2 comment  
**Quote:** `# Note: saving to a file is a write side effect — in production,`  
`# auto-approving bypasses the same controls you just built in Task 1.`  
**What the student experiences:** The note warns against auto-approval, but the broader lesson doesn’t quite land: saving research results feels harmless unless the student is told why it belongs in the same risk bucket as other writes. Without that, “side effect” stays abstract.  
**Smallest fix:** Add one sentence in markdown after Task 2 or before it starts: `Even “helpful” writes like saving a summary change the outside world, so they need the same approval mindset as any other file modification.`  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [pacing break]  
**Location:** `## 🤖 Phase 2: Build the Assistant`  
**Quote:** `## 🤖 Phase 2: Build the Assistant`  
**What the student experiences:** This one phase introduces three servers, server-level approval, interruption handling, `REASONING_MODEL`, and two different task patterns in one long cell. A student can run it, but it’s hard to tell which concept is the main takeaway versus what is just demo detail.  
**Smallest fix:** Add a one-line roadmap under the Phase 2 header: `Focus on two ideas here: combining multiple MCP servers in one agent, and handling approval interrupts before side effects are allowed.`  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

## Gemini Review

This notebook passed third-pass review with two findings. The core concept of combining multiple MCP servers in a capstone project is strong, but the introduction of a new server-level approval mechanism and inconsistencies in the demo code create comprehension gaps for a student at this stage.

---
**Severity:** `[pacing break]`
**Location:** Part 2: Build the Assistant
**Quote:**
```python
require_approval={
    "always": {"tool_names": ["write_file", "create_directory", "move_file", "delete_file"]},
    "never": {"tool_names": ["read_file", "list_directory", "get_file_info", "search_files"]}
}
```
**What the student experiences:** "Whoa, what is this giant dictionary? I've never seen `require_approval` before, and now I have to learn this new syntax and follow the capstone's logic at the same time. I don't have a mental model for what this does."
**Smallest fix:** Add a short markdown cell before the "Phase 2: Build the Assistant" header that introduces the concept:

"In a previous lesson, we built a human-in-the-loop approval workflow. MCP servers provide a shortcut for this pattern. By passing a `require_approval` policy to the server at startup, we can tell it to automatically pause the agent before specific tools are run, giving us a chance to approve or reject the action."
**Blocks transfer:** Yes. Without a conceptual introduction, the student is unlikely to remember or understand this dictionary-based configuration well enough to apply it to their own projects.
**Outline-backed:** Yes. The outline calls for teaching "Server-level approval for write and delete operations."

---
**Severity:** `[confidence gap]`
**Location:** Part 2: Build the Assistant (main code cell)
**Quote:** The two `while result.interruptions:` loops for Task 1 and Task 2.
**What the student experiences:** "These two loops do the same job, but they're written slightly differently. One prints the tool arguments and the other doesn't. One accepts 'y' for 'yes' and the other doesn't. Is one version better? Is there a reason for the difference? I'm not sure which pattern to copy for my own project."
**Smallest fix:** Make the two loops identical to establish a single, clear pattern. Use the more informative version from Task 2 for both loops, and standardize the input check.

```python
# Standardized loop for both Task 1 and Task 2
while result.interruptions:
    state = result.to_state()
    for interruption in state.get_interruptions():
        print(f"\n\u270d\ufe0f  Approval needed for tool: {interruption.raw_item.name}")
        print(f"    Arguments: {interruption.raw_item.arguments}")
        decision = input("    Approve? (yes/no): ").strip().lower()
        if decision in ["yes", "y"]:
            state.approve(interruption)
        else:
            state.reject(interruption, message="User rejected this operation.")

    result = await Runner.run(assistant, state)
```
**Blocks transfer:** Yes. When presented with two slightly different implementations of the same core pattern, the student is left without a clear, repeatable template to use in their own work.
**Outline-backed:** No. The outline requires teaching the approval workflow, but the specific implementation details that create this inconsistency are not part of the core teaching beat.

---

## Grok Review

**Severity:** `[unexplained term]`

**Location:** Phase 2 demo cell (the `require_approval=` dict and the `while result.interruptions:` loops)

**Quote:** 
```python
# Require approval before any write or delete operations
require_approval={
    "always": {"tool_names": ["write_file", "create_directory", "move_file", "delete_file"]},
    "never": {"tool_names": ["read_file", "list_directory", "get_file_info", "search_files"]}
}
...
while result.interruptions:
    state = result.to_state()
    for interruption in state.get_interruptions():
        ...
        state.approve(interruption)
    ...
    result = await Runner.run(assistant, state)
```

**What the student experiences:** I've just finished Notebook 28, which only showed `tool_filter` and `list_tools()`. I have no idea what `require_approval` does, what an "interruption" is, or why we're calling `to_state()`, `get_interruptions()`, `approve()`, and `reject()`. The code works but feels like magic I'm not meant to understand yet.

**Smallest fix:** Add one markdown cell immediately before the big demo cell titled `### 💡 Server-Level Approval` with two sentences: "MCP servers can require human approval before running dangerous tools. The `require_approval` parameter tells the server which tools must be approved; when those tools are called, `Runner.run()` returns an `interruptions` list that we handle with `to_state()` / `approve()`." 

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[lost why]`

**Location:** Comment inside Task 2 and the surrounding prose

**Quote:** 
```
# Note: saving to a file is a write side effect — in production,
# auto-approving bypasses the same controls you just built in Task 1.
```

**What the student experiences:** I see the agent saving `mcp_research.txt`, and I see the approval loop, but I don't understand why the notebook calls this a "side effect" or what I should do differently in a real project. The comment feels like an afterthought rather than the main teaching point the Topics list promised.

**Smallest fix:** Replace the comment with a one-sentence markdown cell right before Task 2: "Saving a file is a side effect that should go through the same approval flow as any other write. In production you would wire the approval decision to a real UI or policy instead of an `input()` prompt."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[lost thread]`

**Location:** Phase 2 markdown header + the single giant code cell

**Quote:** 
```
## 🤖 Phase 2: Build the Assistant

Three MCP servers, write approval on filesystem, `REASONING_MODEL` for decision-making.
```

**What the student experiences:** After the long workspace setup and architecture diagram, I'm dropped into a 90-line cell that does three server connections, agent creation, two completely different tasks, two approval loops, and verification. By the time the cell finishes I'm no longer sure what the core lesson of the capstone was supposed to be.

**Smallest fix:** Split the giant cell after the first approval loop (right before the Task 2 comment). Add a markdown cell between them titled `### Task 2: Research then write` that repeats the one-sentence goal ("Now we combine web fetch with a write so we can see the side-effect approval path").

**Blocks transfer:** No

**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`

**Location:** Practice Exercise (the entire scaffolding)

**Quote:** 
```
# Option B: Another fetch/search approach
# command: "uvx", args: ["mcp-server-fetch"] with different instructions

# TODO: Replace the Time server params with your chosen server
# TODO: Design a task that exercises filesystem + fetch + your new server
```

**What the student experiences:** The exercise tells me to "choose a server... and update the assistant" but gives almost no concrete guidance on what the time server actually provided or what a memory server would look like. After watching the main demo I still don't feel confident I could pick a new MCP server from the GitHub repo and integrate it correctly on my own project.

**Smallest fix:** Change the Option B bullet to "Option B: Memory server (`@modelcontextprotocol/server-memory`) — it lets the agent store and retrieve notes." Add one TODO after "Update the agent instructions": "TODO: Add one sentence describing what the new server can do (e.g. 'persistent note storage')."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Summary:** This notebook has four genuine comprehension blockers for someone coming straight from Notebook 28. The approval mechanism and side-effect concept are the biggest gaps; they sit at the heart of what makes this capstone different from the previous lesson. The other two findings are smaller but would still leave a student feeling shaky about transferring the patterns.

The notebook is otherwise well-structured and follows the capstone identity (external integration via multiple MCP servers). With the four small additions above it would be solid.

---

## DeepSeek Review

## Third-Pass Review: Notebook 29 — Capstone #4 — MCP-Powered Personal Assistant

**Severity:** `[unexplained term]`

**Location:** Phase 2 code cell — `require_approval` parameter on `MCPServerStdio`

**Quote:**
```python
require_approval={
    "always": {"tool_names": ["write_file", "create_directory", "move_file", "delete_file"]},
    "never": {"tool_names": ["read_file", "list_directory", "get_file_info", "search_files"]}
}
```

**What the student experiences:** “I’ve never seen `require_approval` before. What does `“always”` vs `“never”` mean? Is this a new SDK feature? And then there’s a `while result.interruptions` loop with `state.approve()` and `state.reject()` — none of this was in Notebook 28. I don’t know what an interruption is, how approval pauses the run, or how to write this pattern in my own project.”

**Smallest fix:** Add a short markdown cell immediately before the Phase 2 demo that introduces the concept:

```
### 🔐 Server-Level Approval

The `require_approval` parameter on `MCPServerStdio` lets you mark specific tools
as requiring human confirmation before execution.

Set `"always"` for dangerous tools (write/delete) and `"never"` for safe ones (read/list).

When the agent calls a protected tool, the run pauses. `result.interruptions`
contains the pending approvals. Use `result.to_state()`, then `.approve()` or
`.reject()` on each interruption, then resume with `Runner.run(agent, state)`.
```

**Blocks transfer:** Yes — without this explanation the student cannot independently use approval patterns.

**Outline-backed:** Yes — the Course Outline for Capstone 4 explicitly calls for “Server-level approval before writing or deleting.”

---

**Severity:** `[pacing break]`

**Location:** Phase 2 code cell (the single large demo cell)

**Quote:** *(The entire code cell containing Task 1 and Task 2 with approval loops.)*

**What the student experiences:** “This one cell is doing a lot: starting three servers, defining instructions, running Task 1 with an approval loop, then Task 2 with another approval loop, then printing verification. By the time I get to Task 2, I’ve lost track of the approval logic because it’s buried in the middle of a wall of code. I can’t easily scan back to the approval declaration.”

**Smallest fix:** Split the demo into two separate code cells — one for each task — with a short markdown divider between them (e.g., `### 📋 Task 1: Time-stamped daily summary` followed by its code cell, then a `### 📋 Task 2: Research and save` divider before the second code cell). Keep the three-server `async with` block wrapping both, but let each task be a focused unit.

**Blocks transfer:** No — the student can still follow the demo, but the pacing makes it harder to absorb the approval pattern.

**Outline-backed:** No — the outline doesn’t specify cell structure.

---

**Severity:** `[confidence gap]`

**Location:** Practice Exercise code cell — `Handle any write approvals in the loop`

**Quote:**
```
# -------------------------------------------------------
# Step 4: Run a task that uses all three servers
# -------------------------------------------------------
# TODO: Design a task that exercises filesystem + fetch + your new server
#        Handle any write approvals in the loop
```

**What the student experiences:** “I saw the approval loop in the demo, but I don’t know how to write it from memory. The demo used `result.interruptions`, `result.to_state()`, `state.approve()`, and `state.reject()` — that’s a lot of new API surface. The exercise gives me no template, no reminder of the method names, and no hint about the loop structure. I’m guessing I’d get it wrong.”

**Smallest fix:** Add a commented template snippet inside the exercise cell that shows the approval loop shape, e.g.:

```
# Example approval loop (reuse this pattern):
# while result.interruptions:
#     state = result.to_state()
#     for interruption in state.get_interruptions():
#         print(f"Approval needed: {interruption.raw_item.name}")
#         # ask user or auto-approve
#         state.approve(interruption)
#     result = await Runner.run(assistant, state)
```

**Blocks transfer:** Yes — without a reusable pattern the student will likely skip the approval handling or implement it incorrectly.

**Outline-backed:** Yes — the exercise is meant to reinforce the approval pattern from the capstone.

---

**Overall assessment:** Three strong findings. The notebook can be fixed with one new markdown cell and two small structural changes. No other comprehension issues detected.
