# Review Report: Persistent Memory
Generated: 2026-05-10 | Notebook: /Users/scott/Dropbox/Notebooks/openai-agents/Week_4_Memory_Safety_Observability/20_Persistent_Memory.ipynb
Models: Anthropic (claude-opus-4-7 (CLI/Max)) · OpenAI (gpt-5.4) · Gemini (gemini-2.5-pro) · Grok (grok-4.20-0309-reasoning) · DeepSeek (deepseek-reasoner) · Dedup (claude-opus-4-7 CLI/Max)

---

# Grouped Findings: Persistent Memory

# Third-Pass Synthesis — Notebook 20: Persistent Memory

Five reviewers ran. The notebook is in strong shape, but there's one structural bug everyone caught and a cluster of "how do I actually apply this?" gaps in Parts 3 and 4 that real students would feel.

---

## Finding 1 — Cell ordering bug breaks Part 3

**Severity:** `[lost thread]`
**Location:** Part 3, first code cell after the "Avoiding Noisy Memory" header
**Reviewers:** All 5 (Anthropic, OpenAI, Gemini, Grok, DeepSeek)
**Quote:**
```python
# First: inspect what's in a session
items = await noisy_session.get_items()
```
**What the student experiences:** Running cells in order produces `NameError: name 'noisy_session' is not defined`. The session isn't created until the next code cell ("Build a Noisy Session"). Student stops, scrolls back assuming they missed a step, then realizes the cells are inverted. Whatever momentum they had from Part 2 is gone before Part 3 even starts teaching.
**Smallest fix:** Move the inspection cell to *after* the "Build a Noisy Session" cell. Anthropic notes the inspection is then redundant with the existing `items_before = await noisy_session.get_items()` three cells later — could merge instead of just reorder.
**Blocks transfer:** Yes (notebook doesn't run as written)
**Outline-backed:** Yes

---

## Finding 2 — Part 3 doesn't bridge from demo to "what I'd write in my own code"

**Severity:** `[confidence gap]`
**Location:** Part 3, "Why This Works" cell at the end of the summarize → clear → store demo
**Reviewers:** 3 (Grok, DeepSeek, OpenAI partial)
**Quote:** *"Apply this pattern proactively — set a turn count threshold (e.g. every 20 turns) rather than waiting for quality to degrade."*
**What the student experiences:** Sees the pattern work, but can't picture where the threshold logic lives in a real app — wrapper around `Runner.run()`? Background task? Decorator? DeepSeek adds a related question: why a *separate* summarizer agent rather than asking the main assistant to summarize itself? Both gaps prevent the student from confidently lifting the pattern into their own project.
**Smallest fix:** Append one sentence to Why This Works: *"In your own code, maintain a counter on the session (or check `len(await session.get_items())`) and trigger the summarize → clear → store sequence when it crosses your threshold. A separate summarizer agent keeps concerns clean — the assistant focuses on the user, the summarizer compresses history."*
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Finding 3 — Part 3 demo doesn't show why a clean session needs summarizing

**Severity:** `[lost why]`
**Location:** Part 3 intro and "Why This Works" cell
**Reviewers:** 2 (Anthropic, Gemini)
**Quote:** *"The agent retains everything that matters in a fraction of the tokens."* / *"Treat memory as a curated record rather than an exhaustive log…"*
**What the student experiences:** Section is titled "Avoiding Noisy Memory" but the demo shows 5 short, on-topic, perfectly coherent turns about a Python web scraper. Student watches a clean conversation get pruned and may think "the original was fine — why would I throw it away?" The "fewer tokens" framing also lands abstractly without the concrete cost/context-window stakes.
**Smallest fix:** Add one sentence linking demo to principle: *"Even a clean session like this one grows unbounded — every turn costs tokens, and once the history exceeds the model's context window, older facts start dropping silently. Compress proactively, not reactively."*
**Blocks transfer:** Yes — student won't reach for the pattern in their own app because they'll wait for visible "noise" that the lesson never actually shows.
**Outline-backed:** Yes

---

## Finding 4 — Part 4 correction demo doesn't explain *why* it works or what to do when conflicts repeat

**Severity:** `[confidence gap]`
**Location:** Part 4, correction demo and following "Why This Works" cell
**Reviewers:** 3 (Gemini, OpenAI, Grok)
**Quote:** *"If a user corrects an earlier fact, treat the most recent statement as the current truth."*
**What the student experiences:** The demo works, but three related gaps remain: (1) Gemini — student may credit "the agent figured it out" rather than the specific instruction line; (2) OpenAI — single correction is shown, but student doesn't know what to do when conflicts pile up over weeks; (3) Grok — student doesn't know how to design their own agent's instructions to handle this reliably without re-demoing it each time.
**Smallest fix:** Two sentences in Why This Works: *"The correction works because of the instruction line `If a user corrects an earlier fact, treat the most recent statement as the current truth.` — without it, the agent sees two conflicting facts and may get confused. Include this once in your agent's instructions and the persistent session keeps the latest version automatically; for repeated conflicts, fall back to the summarize → clear → store pattern from Part 3."*
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Finding 5 — Topics list promises "user-controlled forgetting" but lesson only delivers correction

**Severity:** `[confidence gap]`
**Location:** Part 4, "Conflicting Memory and Forgetting" subsection
**Reviewers:** 1 (Anthropic) — singleton, but outline-backed and substantive
**Quote:** *"For repeated conflicts, apply the summarize → clear → store pattern from Part 3 rather than letting contradictions stack."*
**What the student experiences:** The Topics list and outline both call for "user-controlled forgetting." Student sees correction (latest wins) and a pointer to summarize → clear → store, but never sees what to do when a user says *"forget my old job title"* or *"don't remember what I told you about my health."* `clear_session()` is the only forgetting tool shown, and it's all-or-nothing.
**Smallest fix:** One sentence acknowledging the limitation: *"The SDK doesn't support per-fact deletion — for true forgetting, the practical pattern is to summarize the session, drop the unwanted fact during summarization, then clear and restore."* (Or drop "user-controlled forgetting" from the Topics list if Scott prefers not to add the content.)
**Blocks transfer:** Yes — first real assistant a student builds will hit a "forget that" request.
**Outline-backed:** Yes

---

## Finding 6 — Security note warns against storing sensitive data but doesn't say how to prevent it

**Severity:** `[confidence gap]`
**Location:** Part 4, security note in "What NOT to Store"
**Reviewers:** 1 (DeepSeek) — singleton, but a real gap
**Quote:** *"Never store passwords, API keys, financial account details, or health information in agent memory."*
**What the student experiences:** Sessions auto-store everything the user says — so if the user types a password, it's persisted. Student reads "never store sensitive data" and thinks "but how do I stop the session from doing exactly that?" The warning is correct but leaves the implementation question unanswered.
**Smallest fix:** One sentence after the security note: *"In practice, filter sensitive input before passing it to the agent, or use output guardrails (Lesson 22) to detect and block sensitive data from being persisted."*
**Blocks transfer:** Yes — student may avoid persistent memory entirely rather than risk storing the wrong thing.
**Outline-backed:** Yes

---

## Singletons that did not meet the bar

- **Part 3 pacing break (OpenAI, Grok):** Both flagged that Part 3 introduces several mechanics at once, but their fixes (a one-line orienting sentence under the Part header, or a "the pattern is summarize → clear → store" pull-quote) overlap heavily with Finding 1's reorder and Finding 2's transfer fix. Likely resolved by those.
- **Part 2 "REST API" example (OpenAI only):** Borderline — students taking an Agents SDK course generally know what a REST API is. Cosmetic at best. Skip unless Scott wants to swap the prompt as a tiny polish.
- **Part 2 "why preferences matter operationally" (OpenAI only):** A reasonable add but the existing "preferences — things the user tells the agent once that should apply to every future interaction" already conveys the point. Style preference, not a comprehension blocker.
- **Key Takeaways "embeddings/semantic search" (OpenAI only):** This is a forward reference to the next lesson — filtered out per synthesis rules.

---

**Bottom line:** Six real findings, dominated by the Part 3 ordering bug and a cluster of "how do I apply this in my own code?" gaps in Parts 3 and 4. All fixes are 1–2 sentences except the cell reorder, which is a pure swap. With these in, the notebook should clear third-pass cleanly.

---

# Individual Reviews

The grouped findings above are your primary working surface. The full individual reviews below are for verifying nuance.

---

## Anthropic Review

# Third-Pass Review — Notebook 20: Persistent Memory

Three findings. The notebook is largely clear for a student who has just finished Lesson 19, but Part 3 has a cell-order issue that breaks the lesson flow, and a couple of beats from the outline get gestured at without landing.

---

**Severity:** `[lost thread]`
**Location:** Part 3, first code cell (immediately after the "## ⚠️ Part 3: Avoiding Noisy Memory" markdown)
**Quote:** `# First: inspect what's in a session\nitems = await noisy_session.get_items()`
**What the student experiences:** A student running cells in order will hit `NameError: name 'noisy_session' is not defined` on the very first cell of Part 3 — the session isn't created until the next code cell ("Build a Noisy Session"). They restart from the top thinking they missed something, then realize the cells are inverted. By the time they sort it out, they've lost the thread on what Part 3 is teaching.
**Smallest fix:** Move the `get_items()` inspection cell to *after* the "Build a Noisy Session" cell — or merge it with the existing `items_before = await noisy_session.get_items()` line that already does this work three cells later. The standalone inspection cell is redundant once the noisy session has been built.
**Blocks transfer:** No
**Outline-backed:** No (this is a structural execution issue)

---

**Severity:** `[lost why]`
**Location:** Part 3 intro, just before the "Build a Noisy Session" demo
**Quote:** `Treat memory as a curated record rather than an exhaustive log — storing everything indiscriminately leads to contradictions, stale facts, and context bloat.`
**What the student experiences:** The Part is titled "Avoiding Noisy Memory" but the demo shows 5 short, coherent, on-topic turns about a Python web scraper — there's no visible noise. The student watches a fine conversation get summarized and pruned, and may think "but the original was clean — why am I throwing it away?" The motivation lands abstractly but not in the demo itself.
**Smallest fix:** Add one sentence between the intro and the demo: *"Even a clean session like the one below grows unbounded over time — every turn adds to context, most aren't worth storing verbatim, and quality degrades silently as the history bloats."* This connects the demo (clean conversation) to the principle (compress proactively, not reactively).
**Blocks transfer:** Yes — without this, the student won't trigger the pattern in their own app because they'll only reach for it when they see "noise," which the lesson never actually shows.
**Outline-backed:** Yes (outline calls for "Avoiding noisy memory — when to summarize vs drop")

---

**Severity:** `[confidence gap]`
**Location:** Part 4, "Conflicting Memory and Forgetting" subsection
**Quote:** `For repeated conflicts, apply the summarize → clear → store pattern from Part 3 rather than letting contradictions stack.`
**What the student experiences:** The Topics list promises "Conflicting memories and user-controlled forgetting" and the outline calls for both. The student sees correction (latest statement wins) and a pointer to summarize → clear → store, but never sees what happens when the user says *"forget my old job title"* or *"please don't remember what I just told you about my health."* They walk away knowing how to *correct* a fact but not how to handle a *forget* request — and `clear_session()` is the only forgetting tool they've been shown, which is all-or-nothing.
**Smallest fix:** Either (a) add one sentence acknowledging the limitation: *"The SDK doesn't support per-fact deletion — for true forgetting, the practical pattern is summarize the session, drop the unwanted fact during summarization, then clear and restore."* Or (b) drop "user-controlled forgetting" from the Topics list so the lesson doesn't promise something it doesn't deliver.
**Blocks transfer:** Yes — a student building a real assistant will hit this the first time a user asks them to forget something.
**Outline-backed:** Yes (outline calls for "Conflicting memories and user-controlled forgetting")

---

## OpenAI Review

**Severity:** [pacing break]  
**Location:** `## ⚠️ Part 3: Avoiding Noisy Memory`  
**Quote:** `## ⚠️ Part 3: Avoiding Noisy Memory`  
**What the student experiences:** This section asks the student to track three new ideas at once: inspecting session contents, building a noisy session, and learning the summarize → clear → store pattern. Because the very first code cell tries to inspect `noisy_session` before the session has even been introduced in the flow, the student loses the sequence of what they’re supposed to be learning.  
**Smallest fix:** Add one short markdown sentence right under the Part header: “We’ll do this in three steps: inspect a growing session, summarize what matters, then replace the full history with that summary.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** first code cell in Part 3  
**Quote:** `# First: inspect what's in a session\nitems = await noisy_session.get_items()`  
**What the student experiences:** As a first-time learner, I can’t tell whether I’m supposed to already have `noisy_session` from somewhere else, or whether inspecting memory is the actual lesson. Even if the code issue is fixed elsewhere, the teaching point is unclear: should I inspect first in my own projects, or summarize first?  
**Smallest fix:** Add a one-line markdown note before this cell: “In a real app, inspect the stored items first so you can decide whether the session is worth compressing.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** Part 2 intro  
**Quote:** `Persistent memory is especially useful for preferences — things the user tells the agent once that should apply to every future interaction.`  
**What the student experiences:** I understand the demo, but I still don’t quite know when this is worth implementing in a real project versus just asking the user again each time. The notebook shows the behavior but doesn’t fully land why this matters operationally.  
**Smallest fix:** Add one sentence to the intro: “This matters in real products because users expect style and preference settings to ‘stick’ without repeating them every time they return.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [unexplained term]  
**Location:** Part 2 later-session demo  
**Quote:** `result = await Runner.run(prefs_agent, input="Explain what a REST API is.", session=later_session)`  
**What the student experiences:** “REST API” is used as if I already know what that means, but at this point in the course I’m learning agents, not web architecture. The example distracts me because I’m now parsing an unfamiliar term inside a memory demo.  
**Smallest fix:** Replace the prompt with a plainer request, such as “Explain what an API is.” or “Explain what a Python function is.”  
**Blocks transfer:** No  
**Outline-backed:** No

---

**Severity:** [confidence gap]  
**Location:** Part 4, correction demo  
**Quote:** `If a user corrects an earlier fact, treat the most recent statement as the current truth.`  
**What the student experiences:** I can follow the demo, but I don’t know what I should actually do in my own project when memories conflict repeatedly. Is the lesson that a prompt instruction is enough, or that I should periodically rewrite stored memory? The transition from single correction to real-world cleanup is too implicit.  
**Smallest fix:** Add one sentence after the demo: “For occasional corrections, an instruction like this is often enough; for repeated conflicts, rewrite the stored memory using the summarize → clear → store pattern.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [unexplained term]  
**Location:** Key Takeaways  
**Quote:** `Store and retrieve memories by meaning rather than exact keywords, using embeddings and semantic search.`  
**What the student experiences:** By the end of the notebook, I’m told the next step uses “embeddings” and “semantic search,” but those terms haven’t been explained yet in language I can act on. Even in a forward-looking sentence, this creates a small confidence dip right before the handoff to the next lesson.  
**Smallest fix:** Soften the line to: “Store and retrieve memories by meaning rather than exact keywords — the next lesson introduces the embeddings-based approach for that.”  
**Blocks transfer:** No  
**Outline-backed:** No

---

## Gemini Review

This notebook passed third-pass review with three findings. The core concepts are well-explained and the demos are effective, but a code ordering issue will block students, and a couple of "why this works" explanations could be sharpened to improve transfer.

---

**Severity:** `[lost thread]`
**Location:** Part 3: Avoiding Noisy Memory
**Quote:**
```python
# First: inspect what's in a session
items = await noisy_session.get_items()
print(f"Session has {len(items)} stored items")
for item in items:
    content = str(item)[:100]
    print(f"  {content}")
```
**What the student experiences:** "This code cell fails with a `NameError` because `noisy_session` hasn't been defined yet. I must have missed a step or the notebook is broken."
**Smallest fix:** Move the markdown cell `#### Build a Noisy Session` and its corresponding code cell to be *before* this inspection code cell. The logical flow should be: 1. create the noisy session, 2. then inspect it.
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

**Severity:** `[lost why]`
**Location:** Part 3: Avoiding Noisy Memory, "Why This Works" cell
**Quote:** `The agent retains everything that matters in a fraction of the tokens.`
**What the student experiences:** "I understand this makes the memory more compact, but why is using fewer tokens a big deal? The notebook says it improves 'quality,' but that feels a bit vague."
**Smallest fix:** Add the concrete engineering reasons. Change the sentence to: "The agent retains everything that matters in a fraction of the tokens, which saves API costs and prevents the conversation from exceeding the model's context window."
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`
**Location:** Part 4: What NOT to Store — Privacy and Correction, "Why This Works" cell
**Quote:** `SQLite gives you durability — curation gives you useful memory. Storing everything indiscriminately leads to contradictions, stale facts, and privacy risk. The right mental model is a curated record: keep stable facts and preferences, drop noise and sensitive data, and correct wrong facts promptly rather than letting them accumulate.`
**What the student experiences:** "The demo for correcting a memory worked, but I'm not sure *why*. I might think the agent is just smart enough to figure it out, and miss that the special instruction in the prompt is what's making it work."
**Smallest fix:** Add a sentence to the "Why This Works" cell that explicitly calls out the instruction's role. For example: "The correction works because of a specific instruction: `If a user corrects an earlier fact, treat the most recent statement as the current truth.` Without this guidance, the agent would see two conflicting facts and might get confused."
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Grok Review

**Severity:** `[lost thread]`

**Location:** Part 3 (first code cell after the Part 3 header)

**Quote:** `# First: inspect what's in a session
items = await noisy_session.get_items()`

**What the student experiences:** I'm following along, I just finished Part 2, I run the next cell and immediately get a `NameError: name 'noisy_session' is not defined`. I have to scroll down to the next markdown cell ("Build a Noisy Session") to understand what was supposed to happen first. The thread of the lesson disappears while I mentally reorder the notebook.

**Smallest fix:** Move the "Build a Noisy Session" cell (the one that creates `noisy_session` and runs the five messages) before the inspection cell. Add a one-sentence markdown cell before the inspection: "First we'll create a deliberately noisy session, then inspect it."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[pacing break]`

**Location:** Part 3 (the three cells titled "Build a Noisy Session", "Summarize the History", and "Clear and Store the Summary")

**Quote:** `## 🛡️ Part 3: Avoiding Noisy Memory`

**What the student experiences:** I'm hit with a new summarizer agent, string truncation with `[:200]`, feeding history into a prompt, `clear_session()`, re-injecting the summary as a user message, and advice about turn-count thresholds—all in rapid succession. I can't tell which piece is the core pattern I should remember versus scaffolding.

**Smallest fix:** After the summarizer cell, add one short markdown cell:  
"**The pattern is always the same:** summarize → clear → store the summary. The rest of this cell is just the mechanical steps to do it once."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`

**Location:** End of Part 3 ("Why This Works" cell)

**Quote:** "Apply this pattern proactively — set a turn count threshold (e.g. every 20 turns) rather than waiting for quality to degrade."

**What the student experiences:** I see the pattern work in the demo, but I have no idea where I would actually put the "every 20 turns" logic in my own project. Is it inside a wrapper around `Runner.run()`? A decorator? A background task? The notebook never makes the leap from demo to "what I'd write."

**Smallest fix:** Append one sentence to the Why This Works cell:  
"In your own code you would maintain a simple counter on the session (or check `len(await session.get_items())`) and trigger the summarize-clear-store sequence when it exceeds your threshold."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[lost why]`

**Location:** Part 4 (the correction demo and following "Why This Works" cell)

**Quote:** "If a user corrects an earlier fact, treat the most recent statement as the current truth."

**What the student experiences:** I watch the agent update its memory when the user says "Actually I just changed jobs...", but I don't get a clear picture of why this matters on a real project or how I should design my agent's instructions so it reliably does this without me having to demonstrate it every time.

**Smallest fix:** Add one sentence after the final `print` in the correction cell:  
"In a real assistant you would include this correction behavior in the agent's instructions once; the persistent session then keeps the latest version automatically."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Overall assessment:** This notebook is close to passing third-pass review. The core concepts are sound and the examples are well-chosen, but the out-of-order cells in Part 3 and the missing "how do I actually use this in my code?" bridges create genuine comprehension drops for a student coming straight from Notebook 19. With the four minimal fixes above the notebook would be very clean.

---

## DeepSeek Review

## Third-Pass Review: Notebook 20 — Persistent Memory

**Overall assessment:** This notebook is well-structured and the core concepts are clearly conveyed. The persistent session demos, preference storage, and the summarize–clear–store pattern all teach effectively. One significant cell-ordering bug breaks the flow in Part 3 and would confuse a student. Two additional minor confidence gaps exist but do not undermine the lesson. With the ordering fix, the notebook is in strong shape.

**Summary of findings:** 3 findings (1 lost-thread, 2 confidence-gap). The notebook passes third-pass review with minor fixes.

---

### Finding 1

**Severity:** `[lost thread]`

**Location:** Part 3 — first code cell in "Avoiding Noisy Memory"

**Quote:**
```python
# First: inspect what's in a session
items = await noisy_session.get_items()
print(f"Session has {len(items)} stored items")
for item in items:
    content = str(item)[:100]
    print(f"  {content}")
```

**What the student experiences:** The student reads the markdown intro about noisy memory, then runs this cell and gets a `NameError` because `noisy_session` doesn't exist yet. The *next* cell builds and populates `noisy_session`, but by then the student is already confused — they think they missed a previous step or the notebook is broken. The teaching flow is interrupted because the demo now runs backwards: inspect → build, instead of build → inspect.

**Smallest fix:** Move the problematic cell to *after* the "Build a Noisy Session" code cell (which defines `noisy_session`). The cell headers can stay; just reorder the two code cells. Alternatively, merge the build and inspect into one cell with a short comment. The minimal change is a pure cell swap.

**Blocks transfer:** No — the concept of inspecting a session is already known from Lesson 19. The bug only obstructs this specific demo.

**Outline-backed:** Yes — the outline calls for "Avoiding noisy memory" teaching; the student cannot follow the demo as written.

---

### Finding 2

**Severity:** `[confidence gap]`

**Location:** Part 3 — "Summarize the History" code cell and its `### 💡 Why This Works` cell

**Quote:**
```python
summarizer = Agent(
    name="Summarizer",
    instructions="Extract only the key facts worth remembering. Return a concise bullet list.",
    model=MODEL
)
```
Then in Why This Works: *"Apply this pattern proactively — set a turn count threshold (e.g. every 20 turns) rather than waiting for quality to degrade."*

**What the student experiences:** The student watches a separate `Summarizer` agent being created just to summarize conversation history. They understand *what* the code does, but they are left silently wondering: "Do I always need a separate agent just for this? Why couldn't I ask the main assistant to summarize its own history? And how do I actually set up that turn-count threshold in a real project?" The demo shows the pattern but doesn't explain the design decision, so the student can't transfer the pattern to their own code with confidence.

**Smallest fix:** Add one sentence to the Why This Works cell: *"A separate summarizer agent keeps concerns clean — your assistant focuses on the user, and the summarizer compresses history. In a real app, you would call the summarizer from a background task after a configurable number of turns."* This addresses both the "why separate" and the "how to integrate" questions.

**Blocks transfer:** Partially — the student can copy the demo code but doesn't know *when* and *why* to reach for a separate summarizer.

**Outline-backed:** Yes — the outline calls for "avoiding noisy memory" and "when to summarize vs drop"; the design decision behind using a separate agent is part of the judgment students need.

---

### Finding 3

**Severity:** `[confidence gap]`

**Location:** Part 4 — "What NOT to Store" section, security note cell

**Quote:**
> ⚠️ **Security note:** Never store passwords, API keys, financial account details, or health information in agent memory. Only persist data the user would expect an assistant to remember. Prompt injection and input validation are covered in Lesson 23.

**What the student experiences:** The student reads "never store sensitive data" and immediately thinks: "But the conversation session stores *everything* the user says — if the user types their password, it's automatically saved. How do I prevent that? The instructions don't cover how to filter or redact sensitive input before it hits the session." The warning is correct but leaves the student with an unaddressed "how" question that undermines confidence in using persistent memory in real projects.

**Smallest fix:** Add one sentence after the security note: *"In practice, you can filter sensitive input before passing it to the agent, or use output guardrails (covered in Lesson 22) to detect and block sensitive data from being persisted."* This gives the student an immediate actionable next step (pre-filtering) and a forward reference for the formal solution.

**Blocks transfer:** Yes — the student would likely avoid using persistent memory for fear of accidentally storing sensitive data without knowing how to prevent it.

**Outline-backed:** Yes — the outline lists "What NOT to store — privacy and sensitive data boundaries"; the notebook currently warns but doesn't equip the student with a strategy.
