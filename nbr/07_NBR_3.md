# Review Report: Structured Outputs
Generated: 2026-05-10 | Notebook: /Users/scott/Dropbox/Notebooks/openai-agents/Week_1_Foundations/07_Structured_Outputs.ipynb
Models: Anthropic (claude-opus-4-7 (CLI/Max)) · OpenAI (gpt-5.4) · Gemini (gemini-2.5-pro) · Grok (grok-4.20-0309-reasoning) · DeepSeek (deepseek-reasoner) · Dedup (claude-opus-4-7 CLI/Max)

---

# Grouped Findings: Structured Outputs

# Third-Pass Synthesis — Notebook 07: Structured Outputs

**Reviewer split:** 2 of 5 reviewers (Grok, DeepSeek) found no comprehension issues. 3 reviewers (Anthropic, OpenAI, Gemini) flagged a total of 9 findings, mostly clustered around Part 1's "Why This Works" cell and the "when to use" framing promised in the Topics list.

One Gemini finding (schema evolution / new Part 3) is excluded — the fix requires a new section plus new demo, which exceeds "smallest fix" scope, and Grok specifically called the scope-out intentional. Worth noting but not actionable as a third-pass fix.

---

## CONFIDENCE GAP findings

### 1. Validation failure behavior is left implicit
- **Severity:** [confidence gap]
- **Location:** Part 1, "Why This Works" cell
- **Reviewers:** 1 (Anthropic)
- **Quote:** "the SDK uses the Pydantic model to generate a JSON schema and instructs the model to conform to it. `result.final_output` is now an instance of `SentimentResult`, not a string."
- **What the student experiences:** They learned `Field(ge=0.0, le=1.0)` constraints in NB06 and naturally wonder: what if the model returns `confidence=2.5`? Does the SDK retry, raise, or coerce? They only see the happy path, so when their own structured agent fails validation in the wild, they won't know what to expect.
- **Smallest fix:** One sentence in the Why This Works cell: "If the model returns data that violates a `Field` constraint, the SDK raises a validation error instead of silently passing through." (Verify exact behavior before wording.)
- **Blocks transfer:** Borderline — student can use the pattern, but won't know how it fails.
- **Outline-backed:** Yes ("Validating and parsing agent output safely")

### 2. Field names function as part of the prompt
- **Severity:** [confidence gap]
- **Location:** Part 1, "Why This Works" cell
- **Reviewers:** 1 (Gemini)
- **Quote:** "The SDK uses the Pydantic model to generate a JSON schema and instructs the model to conform to it."
- **What the student experiences:** "Why did the author name the fields `key_phrase` and `reasoning`? I'd have used `phrase` and `reason`." They don't realize field names themselves act as instructions, so they may write models with terse names and get worse results without knowing why.
- **Smallest fix:** Append one sentence to the Why This Works cell: "The model also uses your field names as instructions, so descriptive names like `reasoning` produce better results than short names like `rsn`."
- **Blocks transfer:** Yes — bad field names silently degrade output quality.
- **Outline-backed:** No

### 3. No guidance on choosing fields for one's own model
- **Severity:** [confidence gap]
- **Location:** Part 1 intro
- **Reviewers:** 1 (OpenAI)
- **Quote:** "Define a Pydantic model, pass it to the agent via `output_type=`, and the SDK requests a defined schema and validates the returned shape…"
- **What the student experiences:** They can copy the demo, but cannot confidently answer "what should go in my schema for my project?" They may include everything the agent could say rather than what their downstream code needs.
- **Smallest fix:** Add one sentence to the Part 1 intro: "In your own projects, define fields based on what the next step in your code needs — not everything the agent could say."
- **Blocks transfer:** Yes
- **Outline-backed:** Yes

### 4. The reusable pattern is never named
- **Severity:** [confidence gap]
- **Location:** Whole notebook (after first structured-output demo)
- **Reviewers:** 1 (OpenAI)
- **Quote:** Topics list bullet: "Validating and parsing agent output safely"
- **What the student experiences:** They see structured output succeed but lack a one-line mental model for what to do in their own app. "Safely" stays implied rather than spelled out as a pattern.
- **Smallest fix:** One markdown sentence after the first structured-output demo: "The reusable pattern is: run the agent, trust `result.final_output` only if it matches your model, then pass those typed fields into regular Python code."
- **Blocks transfer:** Yes
- **Outline-backed:** Yes

---

## LOST WHY findings

### 5. Problem demo failure message doesn't match what was shown
- **Severity:** [lost why]
- **Location:** Problem cell — failing free-form demo
- **Reviewers:** 1 (Anthropic)
- **Quote:**
  ```
  except TypeError:
      print("❌ Can't access fields from a string — format varies every run")
  ```
- **What the student experiences:** They're told format variability is the problem but only see a contrived TypeError. They might think "I'd just parse the string with regex" because the actual failure mode (drift across runs) was never demonstrated.
- **Smallest fix:** Either re-run the unstructured agent 2–3 times on the same input to show wording drift, or change the printed message to match what was actually shown — e.g., "❌ `result.final_output` is a string — there's no field to access." Pick one and align demo + message.
- **Blocks transfer:** No
- **Outline-backed:** Yes ("Why free-form text is fragile in real workflows")

### 6. "When to use structured outputs" is promised but never landed
- **Severity:** [lost why]
- **Location:** Key Takeaways / end of Part 2
- **Reviewers:** 1 (Anthropic)
- **Quote:** Topics list: "When structured outputs help most"
- **What the student experiences:** They finish with a clear sense of *how* but no answer to "should I use this everywhere?" The natural impulse is to slap `output_type=` on every agent — including chat-style ones where free text is correct.
- **Smallest fix:** One bullet in Key Takeaways or one sentence at the end of Part 2: "Use structured outputs whenever the response feeds into code; free-form text is the right choice for human-facing chat replies."
- **Blocks transfer:** Yes — affects how students decide to apply the pattern.
- **Outline-backed:** Yes

### 7. Connection to downstream Python code is left abstract
- **Severity:** [lost why]
- **Location:** Part 1, "Why This Works" cell
- **Reviewers:** 1 (OpenAI)
- **Quote:** "`result.final_output` is now an instance of `SentimentResult`, not a string."
- **What the student experiences:** They understand the mechanism but not the project payoff beyond "nicer than a string." They don't yet connect this to actual downstream code.
- **Smallest fix:** Append one sentence: "This matters any time the next step is Python code — saving to a database, calling another function, filtering results, or passing the data to another agent/tool."
- **Blocks transfer:** No
- **Outline-backed:** Yes

### 8. Optional fields — when to use them isn't grounded
- **Severity:** [lost why]
- **Location:** Part 2, "Why This Works" cell
- **Reviewers:** 1 (OpenAI)
- **Quote:** "`Optional` fields make missing data explicit — downstream code can safely distinguish 'absent' from 'wrong' without string parsing."
- **What the student experiences:** They see fields can be `None` but not when this is the right design choice versus making everything required.
- **Smallest fix:** Add one sentence: "Use this pattern when real input is messy — resumes, emails, notes, and forms often contain some fields but not others."
- **Blocks transfer:** No
- **Outline-backed:** Yes

---

## UNEXPLAINED TERM findings

### 9. "Silently passing through" assumes pipeline-failure thinking
- **Severity:** [unexplained term]
- **Location:** Part 1 intro
- **Reviewers:** 1 (OpenAI)
- **Quote:** "…invalid outputs surface as errors instead of silently passing through."
- **What the student experiences:** A new agent builder may not yet think in pipeline failure modes and won't grasp what is being avoided.
- **Smallest fix:** Replace "instead of silently passing through" with "instead of reaching your Python code in the wrong shape and breaking later."
- **Blocks transfer:** No
- **Outline-backed:** Yes

---

## Excluded (noted for completeness)

- **Schema evolution as a new Part 3** (Gemini): fix requires a new section + new demo, which exceeds minimal-fix scope. Grok read the omission as intentional scoping (validation already covered in NB06). If Scott wants to address this beat, it's a structural decision rather than a third-pass fix.

---

## Notes on prioritization

Findings 1–4 (confidence gaps) plus finding 6 (the "when to use" steer) most affect whether the lesson achieves its goal. If Scott wants the absolute minimum set, those five fixes — three of which are one-sentence appends to the Part 1 "Why This Works" cell — would close most of the gap. Findings 5, 7, 8, 9 are smaller polish items.

---

# Individual Reviews

The grouped findings above are your primary working surface. The full individual reviews below are for verifying nuance.

---

## Anthropic Review

# Third-Pass Review — Notebook 07: Structured Outputs

Three findings. The notebook is in good shape overall — clean flow from problem → solution → optional fields, with the previous lesson's Pydantic groundwork doing real work.

---

**1. [lost why]**

**Location:** Problem cell — the failing free-form demo

**Quote:**
```
# Try to use the output in code — this will fail unpredictably
output = result.final_output
try:
    sentiment = output["sentiment"]  # TypeError — string indices must be integers
except TypeError:
    print("❌ Can't access fields from a string — format varies every run")
```

**What the student experiences:** The printed message says "format varies every run" — but the failure they just saw (TypeError from dict-indexing a string) is a different problem entirely. The student is told format variability is the issue but isn't shown it. They might silently think "I'd just parse the string with regex" because the contrived TypeError doesn't feel like a real failure mode.

**Smallest fix:** Either re-run the unstructured agent 2–3 times on the same input and print outputs (showing wording drift), or change the failure message to match what was actually shown — e.g., "❌ `result.final_output` is a string — there's no field to access." Pick one and align demo + message.

**Blocks transfer:** No  
**Outline-backed:** Yes (outline calls for "Why free-form text is fragile in real workflows")

---

**2. [lost why]**

**Location:** Whole notebook — promised topic not landed

**Quote:** From the Topics list: `- When structured outputs help most`

**What the student experiences:** They finish the notebook with a clear sense of *how* structured outputs work, but no explicit answer to "should I use this everywhere?" The natural impulse for a new student is to slap `output_type=` on every agent — including chat-style ones where free text is the right answer. Without a one-line steer, they may overuse the pattern.

**Smallest fix:** Add one bullet to Key Takeaways or one short sentence at the end of Part 2 — e.g., "Use structured outputs whenever the response feeds into code; free-form text is the right choice for human-facing chat replies."

**Blocks transfer:** Yes — affects how the student decides to apply the pattern in their own work.  
**Outline-backed:** Yes

---

**3. [confidence gap]**

**Location:** Part 1, "Why This Works" cell

**Quote:** "the SDK uses the Pydantic model to generate a JSON schema and instructs the model to conform to it. `result.final_output` is now an instance of `SentimentResult`, not a string."

**What the student experiences:** Earlier in the same Part, the lesson promised "invalid outputs surface as errors instead of silently passing through" — but the student only ever sees the happy path. They learned `Field(ge=0.0, le=1.0)` constraints in NB06, and naturally wonder: what if the model returns `confidence=2.5`? Does the SDK retry? Raise? Coerce? Without a one-liner, a student writing their own structured agent won't know what behavior to expect when validation fails.

**Smallest fix:** One sentence in the Why This Works cell describing what happens on invalid output — e.g., "If the model returns data that violates a `Field` constraint, the SDK raises a validation error instead of silently passing through." (Verify the exact behavior before wording.)

**Blocks transfer:** Borderline — student can use the pattern, but won't know how it fails.  
**Outline-backed:** Yes (outline lists "Validating and parsing agent output safely")

---

Other than these, the notebook is clean for a student at this point in the course. Optional fields, dot-notation access, and the model-as-schema concept all land cleanly off the NB06 foundation.

---

## OpenAI Review

**Severity:** [lost why]  
**Location:** Part 1: Defining Output Structure with Pydantic — “Why This Works”  
**Quote:** “The SDK uses the Pydantic model to generate a JSON schema and instructs the model to conform to it. `result.final_output` is now an instance of `SentimentResult`, not a string.”  
**What the student experiences:** The student understands the mechanism, but not the real project payoff beyond “it’s nicer than a string.” They still may not connect this to actual downstream code they would write after an agent run.  
**Smallest fix:** Add one sentence after this cell: “This matters any time the next step is Python code — saving to a database, calling another function, filtering results, or passing the data to another agent/tool.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Part 1: Defining Output Structure with Pydantic  
**Quote:** “Define a Pydantic model, pass it to the agent via `output_type=`, and the SDK requests a defined schema and validates the returned shape — invalid outputs surface as errors instead of silently passing through.”  
**What the student experiences:** The student sees the pattern once, but may not know how to choose fields for their own model. They can copy the demo, but not yet confidently answer “what should go in my schema for my project?”  
**Smallest fix:** Add one sentence to the Part 1 intro: “In your own projects, define fields based on what the next step in your code needs — not everything the agent could say.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [unexplained term]  
**Location:** Part 1 intro  
**Quote:** “...invalid outputs surface as errors instead of silently passing through.”  
**What the student experiences:** “Surface as errors” is understandable, but “silently passing through” assumes the student already thinks in pipeline failure modes. A new agent builder may not yet grasp what failure mode is being avoided.  
**Smallest fix:** Replace “instead of silently passing through” with “instead of reaching your Python code in the wrong shape and breaking later.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** Part 2: Optional Fields — “Why This Works”  
**Quote:** “`Optional` fields make missing data explicit — downstream code can safely distinguish "absent" from "wrong" without string parsing.”  
**What the student experiences:** The student sees that some fields can be `None`, but may not understand when this is the right design choice versus just making every field required. The practical reason for optional fields in real extraction tasks is still a little abstract.  
**Smallest fix:** Add one sentence: “Use this pattern when real input is messy — resumes, emails, notes, and forms often contain some fields but not others.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Whole notebook / missing teaching beat promised in Topics  
**Quote:** “- Validating and parsing agent output safely”  
**What the student experiences:** The notebook shows successful structured output, but never gives the student a concrete mental model for what they should do in their own app after the run besides access fields. “Safely” remains mostly implied rather than named as a pattern they can reuse.  
**Smallest fix:** Add one short markdown sentence after the first structured-output demo: “The reusable pattern is: run the agent, trust `result.final_output` only if it matches your model, then pass those typed fields into regular Python code.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

## Gemini Review

This notebook passed third-pass review with two findings. The core teaching flow is excellent — the "Problem / Solution" structure is clear, and the demos effectively show the value of structured outputs. The findings below address two small gaps that, if fixed, would significantly increase a student's confidence in applying these patterns to their own projects.

---

**Severity:** `[confidence gap]`
**Location:** Part 1: Defining Output Structure with Pydantic
**Quote:** `## 💡 Part 1: Defining Output Structure with Pydantic` (The issue is a missing explanation in this section)
**What the student experiences:** "Okay, I see how this works. But why did the author name the fields `key_phrase` and `reasoning`? I probably would have used `phrase` and `reason`. I don't realize that the field names themselves are part of the instructions to the model and that better names lead to better results."
**Smallest fix:** Add a sentence to the `### 💡 Why This Works` cell in Part 1. Change it from:
"The SDK uses the Pydantic model to generate a JSON schema and instructs the model to conform to it. `result.final_output` is now an instance of `SentimentResult`, not a string."
...to:
"The SDK uses the Pydantic model to generate a JSON schema and instructs the model to conform to it. `result.final_output` is now an instance of `SentimentResult`, not a string. **The model also uses your field names as instructions, so descriptive names like `reasoning` produce better results than short names like `rsn`.**"
**Blocks transfer:** Yes. A student could write a model with non-descriptive field names and be confused about why their agent performs poorly, not realizing they've written a weak "prompt" via their schema.
**Outline-backed:** No.

---

**Severity:** `[confidence gap]`
**Location:** End of notebook, before Practice Exercises.
**Quote:** (From Course Outline) `- Schema evolution — what happens when you add fields or change types`
**What the student experiences:** "This is great for a fixed output type, but what if my project requirements change? If I add a new field to my Pydantic model, will the agent just start filling it in? Or will it break? I'm not confident about how to maintain this over time."
**Smallest fix:** Add a new, small Part 3 called "Evolving Your Schema". It would need one markdown cell explaining the concept and one code cell that adds a new field (e.g., `is_actionable: bool`) to the `SentimentResult` model, re-runs the agent, and shows that the new field is now populated. This directly addresses the missing teaching beat from the outline.
**Blocks transfer:** Yes. Any real project will involve changing requirements. A student who doesn't know how to evolve their output schemas will not feel confident using this pattern in production.
**Outline-backed:** Yes.

---

## Grok Review

**This notebook passed third-pass review with no significant comprehension issues.**

As a student who has just finished Notebook 06 (Pydantic models, validation errors, `Field`/`Annotated`, dot access, and why we prefer Pydantic over raw schemas), everything here felt like a natural next step. The core new idea (`output_type=`) is introduced clearly, the "why this matters for real code" is shown both in the opening failure case and in the exercises, and nothing buries the lesson under scaffolding. The optional-fields example and the two practice exercises build directly on what I already know without dropping any terms I wouldn't recognize.

The schema-evolution bullet that appears in the Course Outline is not present in this notebook's Topics list or demos; the author appears to have intentionally scoped the lesson to the happy path plus validation surface errors (already taught in 06). No comprehension blocker for a student at this exact point in the sequence.

All three findings I considered were borderline style or exercise-scaffolding notes rather than true blockers, so I discarded them. The notebook is ready.

---

## DeepSeek Review

This notebook passed third-pass review with no significant comprehension issues.

A student who completed Lesson 06 (Pydantic Basics) and worked through the course sequentially would be able to follow the material without confusion. The problem statement (free-form text breaks downstream code) is concretely demonstrated, the solution (`output_type=`) is introduced with a clear minimal example, and the nuance of optional fields is taught with a practical extraction demo. The exercises are well-scaffolded to the demonstrated patterns.

No findings to report.
