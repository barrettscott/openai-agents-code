# Review Report: Capstone #2 — Multi-Agent Research Team
Generated: 2026-05-10 | Notebook: /Users/scott/Dropbox/Notebooks/openai-agents/Week_3_Multi_Agent_Systems/18_Capstone_2_Research_Team.ipynb
Models: Anthropic (claude-opus-4-7 (CLI/Max)) · OpenAI (gpt-5.4) · Gemini (gemini-2.5-pro) · Grok (grok-4.20-0309-reasoning) · DeepSeek (deepseek-reasoner) · Dedup (claude-opus-4-7 CLI/Max)

---

# Grouped Findings: Capstone #2 — Multi-Agent Research Team

# Third-Pass Synthesis — Lesson 18: Capstone #2 (Multi-Agent Research Team)

Five reviewers, nine deduplicated findings. One finding is unanimous (missing demo run cell). The rest are concentrated around unexplained design choices the student is asked to copy without understanding.

I filtered out re-flags of first/second-pass issues: code-in-markdown-cells (Gemini, DeepSeek), duplicate Key Takeaways cells (DeepSeek). Those are structural fixes, not comprehension findings.

---

## Finding 1 — Pipeline never actually runs

**Severity:** `[confidence gap]` / `[lost thread]`
**Location:** Phase 3: Demo (between `research_pipeline()` definition and Practice Exercises)
**Reviewers:** 5/5 — Anthropic, OpenAI, Gemini, Grok, DeepSeek (unanimous)
**Quote:**
> "⚠️ **Cost note:** This pipeline uses web search, code interpreter, and multiple model calls. Cost and runtime will vary depending on tool behavior and container reuse — budget a few minutes and a small number of API credits per run."

(Followed immediately by Practice Exercises. No `await research_pipeline(...)` call exists.)

**What the student experiences:** Reads ~80 lines of pipeline code with parallel `gather`, exception handling, critique, and revision — then arrives at "Phase 3: Demo" expecting to see it run. Sees only a cost note. Goes to Exercise 2, which says "run the demo cell above first to populate `result['final_report']`" — referring to a cell that doesn't exist. The student loses trust in the notebook and doesn't know what a "research report" actually looks like in this system.

**Smallest fix:** Add one code cell after the cost note:
```python
result = await research_pipeline(
    "What is the current state of electric vehicle adoption globally?"
)
print("\n" + "="*60)
print("FINAL REPORT")
print("="*60)
print(result["final_report"])
```

**Blocks transfer:** Yes
**Outline-backed:** Yes — outline calls for a working pipeline demo, and Exercise 2 depends on it.

---

## Finding 2 — Why procedural orchestration (and not handoffs / agents-as-tools)?

**Severity:** `[lost why]`
**Location:** System Architecture section / Phase 2 intro
**Reviewers:** 2/5 — Anthropic, OpenAI
**Quote:**
> "**Procedural orchestration** — Python coordinates the phases directly with `asyncio` and sequential `Runner.run()` calls"
> "We'll orchestrate the pipeline directly in code — parallel research, sequential writing and critique. This gives us full visibility into each step."

**What the student experiences:** Lessons 14–15 just taught two SDK-native coordination patterns (handoffs, agents-as-tools), and now the big multi-agent capstone uses neither. "Full visibility" is vague to someone who's never built one of these. The student carries the unanswered question through the whole lesson, which dilutes confidence in the choice when they go to apply it.

**Smallest fix:** One sentence under the procedural-orchestration bullet: "We coordinate phases in code here because the pipeline shape is fixed and we want explicit control over parallel execution and partial-failure handling — handoffs and agents-as-tools are better when the agent itself decides what to do next."

**Blocks transfer:** Yes — student can't choose between procedural and agent-led orchestration on their own work.
**Outline-backed:** Yes — outline specifies procedural pipeline as a deliberate design choice.

---

## Finding 3 — `tool_config` / `container: auto` appears with no explanation

**Severity:** `[unexplained term]`
**Location:** Phase 1, Analyst agent definition
**Reviewers:** 3/5 — OpenAI, Gemini, Grok
**Quote:**
> ```python
> tools=[CodeInterpreterTool(tool_config={
>     "type": "code_interpreter",
>     "container": {"type": "auto"}
> })]
> ```

**What the student experiences:** `WebSearchTool()` was used two cells earlier with no arguments. Lesson 12 introduced `CodeInterpreterTool` more simply. Now this dict appears with `tool_config`, `container`, and `auto` — none explained. The student doesn't know if it's required, what it does, or what they'd change for their own project. The cost note later mentions "container reuse" as if the student already knows what that means.

**Smallest fix:** One inline comment above the analyst agent: `# container: "auto" lets the SDK reuse a Code Interpreter sandbox across calls — faster and cheaper than spinning a fresh one each time.`

**Blocks transfer:** Yes (3 reviewers); No (1 reviewer — OpenAI marked it No)
**Outline-backed:** No

---

## Finding 4 — Model selection (REASONING_MODEL) not justified at point of use

**Severity:** `[confidence gap]`
**Location:** Phase 1, Critic agent definition
**Reviewers:** 1/5 — Anthropic
**Quote:**
> ```python
> critic_agent = Agent(
>     name="Critic",
>     ...
>     model=REASONING_MODEL
> )
> ```

**What the student experiences:** Three agents use `MODEL`, one uses `REASONING_MODEL`, with no explanation at the point of choice. The rationale appears in Key Takeaways at the very end — too late. On their own pipeline, the student wouldn't know which agent gets which model.

**Smallest fix:** One sentence in the markdown introducing Phase 1 (or as a comment above the Critic): "We give the Critic `REASONING_MODEL` because critique is judgment-heavy — the other specialists do well-scoped work and run on `MODEL`."

**Blocks transfer:** Yes — model-selection is one of the things this capstone is supposed to make concrete.
**Outline-backed:** Yes — outline lists model selection as core; Lesson 31's framework builds on this kind of judgment.

---

## Finding 5 — The reusable pattern is never named

**Severity:** `[confidence gap]`
**Location:** Just before the `research_pipeline()` function definition
**Reviewers:** 1/5 — OpenAI
**Quote:**
> `async def research_pipeline(question):`

**What the student experiences:** The function is the lesson's core takeaway, but no sentence frames it as a reusable template. A student can read it and still leave without "this is the shape I'd use for my own multi-agent workflow."

**Smallest fix:** One markdown sentence immediately before the function cell: "This function is the reusable pattern: run independent specialists in parallel, convert failures into fallback text, then feed their outputs into downstream agents one phase at a time."

**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Finding 6 — Failure-handling pattern shown but never demonstrated

**Severity:** `[lost why]`
**Location:** Reliability note + the `isinstance(..., Exception)` block in `research_pipeline()`
**Reviewers:** 1/5 — Grok
**Quote:**
> "`asyncio.gather(return_exceptions=True)` for parallel steps. If one specialist fails, the pipeline detects it, logs the failure, and continues with partial results rather than crashing."

**What the student experiences:** Sees the fallback code but never sees a failure actually occur. Knows *what* the pattern is but not *why* it matters in practice (no silent degradation, no 2 a.m. crash).

**Smallest fix:** One sentence after the demo run cell (Finding 1's fix): "Try temporarily breaking one specialist (e.g. an invalid tool) to watch the fallback text appear — the pipeline keeps running instead of crashing."

**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Finding 7 — Phase 1 doesn't reconnect agents to the pipeline shape

**Severity:** `[lost thread]`
**Location:** Phase 1 header
**Reviewers:** 1/5 — OpenAI
**Quote:**
> `## 🤖 Phase 1: Build the Specialist Agents`

**What the student experiences:** Once Phase 1 starts, the student sees a lot of agent setup without a reminder of how each role fits into the larger flow. They remember the names but not why those are the right roles.

**Smallest fix:** One sentence under the Phase 1 header: "Each agent owns one phase of the pipeline, so the code can combine specialized outputs instead of asking one agent to do everything."

**Blocks transfer:** No
**Outline-backed:** Yes

---

## Finding 8 — Exercise 1 doesn't name the transfer principle

**Severity:** `[confidence gap]`
**Location:** Exercise 1 intro
**Reviewers:** 1/5 — OpenAI
**Quote:**
> "Extend the pipeline with a `FactChecker` agent that verifies the key claims in the final report using web search before it's delivered."

**What the student experiences:** The exercise asks for a new phase, but no sentence tells the student that "adding/swapping a phase" is *the* extension mechanism for procedural pipelines. They may not generalize this to their own projects.

**Smallest fix:** One sentence in the exercise intro: "This is the main way you'll customize a pipeline in your own project — keep the orchestration shape, then insert or swap specialist phases where your workflow needs them."

**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Finding 9 — Pipeline function is one wall of code with four new ideas

**Severity:** `[pacing break]`
**Location:** The `research_pipeline()` function (60+ lines)
**Reviewers:** 1/5 — Grok
**Quote:** The full `async def research_pipeline(question):` cell.

**What the student experiences:** Four new ideas in one cell — parallel `gather` with exception handling, writer input construction, critique step, revision step — with no internal markers. Loses track of which part to focus on.

**Smallest fix:** Three short markdown sub-headers between the function's existing comment dividers: `#### Sub-phase A: Parallel research`, `#### Sub-phase B: Write`, `#### Sub-phase C: Critique & revise`. Each can be one sentence — or, if the function shouldn't be split, add sentence-long comments at each boundary inside the cell.

**Blocks transfer:** No (student can still copy the pattern, but it feels overwhelming)
**Outline-backed:** Yes

---

## Summary

The unanimous finding (missing demo run cell) cascades into Findings 6 and 8 — once the pipeline runs, the failure-pattern demonstration and Exercise 2's `result` reference both work. Findings 2 and 4 are the two design choices the lesson asks the student to copy without explaining (procedural orchestration, REASONING_MODEL on the Critic). Finding 3 is the most-flagged unexplained term. The remaining findings are single-reviewer one-line additions.

Total fix budget if all are addressed: one code cell + roughly seven added sentences.

---

# Individual Reviews

The grouped findings above are your primary working surface. The full individual reviews below are for verifying nuance.

---

## Anthropic Review

# Third-Pass Review — Lesson 18: Capstone #2 — Multi-Agent Research Team

Three findings. The notebook's structure and code patterns are familiar to the student from Weeks 2–3, so most of it lands cleanly. The comprehension gaps are concentrated around (a) what the pipeline actually does end-to-end, and (b) the unexplained design choices the student is asked to copy.

---

## Finding 1

**Severity:** `[confidence gap]`

**Location:** `## 🎬 Phase 3: Demo`

**Quote:**
> "⚠️ **Cost note:** This pipeline uses web search, code interpreter, and multiple model calls. Cost and runtime will vary depending on tool behavior and container reuse — budget a few minutes and a small number of API credits per run."

**What the student experiences:** The student arrives at Phase 3 expecting to see the pipeline they just built actually run. They get a heading, a cost note — and then nothing. No `await research_pipeline(...)` call, no output, no concrete sense of what a "research report" looks like in this system. The next thing they see is Practice Exercises. Exercise 2 then explicitly says "run the demo cell above first to populate `result['final_report']`" — referring to a cell that doesn't exist. The student now distrusts the notebook.

**Smallest fix:** Add a single code cell after the cost note that calls `result = await research_pipeline("What is the current state of electric vehicle adoption globally?")` and prints `result["final_report"]`. This is the demo the rest of the notebook assumes exists.

**Blocks transfer:** Yes

**Outline-backed:** Yes — the outline calls for a working pipeline demo, and Exercise 2's golden-test pattern depends on it.

---

## Finding 2

**Severity:** `[lost why]`

**Location:** `## 🏗️ System Architecture`

**Quote:**
> "**Procedural orchestration** — Python coordinates the phases directly with `asyncio` and sequential `Runner.run()` calls"

**What the student experiences:** The student just spent Lessons 14–15 learning two SDK-native ways to coordinate agents — handoffs and agents-as-tools. Now the first big multi-agent capstone uses neither. They wonder, "Wait, why are we writing the orchestration in Python instead of using a triage agent or agents-as-tools?" The notebook never answers this, and the student carries the question through the whole lesson — which dilutes their confidence in the choice when they go to apply it.

**Smallest fix:** Add one sentence under the bullet: "We coordinate the phases in code here because the pipeline is fixed and we want explicit control over parallel execution and partial-failure handling — handoffs and agents-as-tools are better when the agent itself decides what to do next."

**Blocks transfer:** Yes — without this, the student doesn't know when to choose procedural orchestration vs. an orchestrator agent in their own work.

**Outline-backed:** Yes — the outline specifies "Procedural multi-agent pipeline — explicit phase-by-phase coordination in code" as a deliberate design choice for this capstone.

---

## Finding 3

**Severity:** `[confidence gap]`

**Location:** Phase 1 — agent definitions, in the line `model=REASONING_MODEL` on the Critic agent

**Quote:**
> ```
> critic_agent = Agent(
>     name="Critic",
>     instructions=critic_instructions,
>     model=REASONING_MODEL
> )
> ```

**What the student experiences:** Three agents use `MODEL`, one uses `REASONING_MODEL`, and there's no explanation at the point of choice. The student can see the code but doesn't know the principle. They learn the rationale in Key Takeaways at the end ("Stronger models are useful for higher-judgment tasks like evaluation, critique, and scoring") — but by then they've already passed the design decision they were supposed to learn from. On their own pipeline, they wouldn't know which agent gets which model.

**Smallest fix:** Add one sentence to the markdown cell that introduces Phase 1 (or as a brief comment above the Critic agent): "We give the Critic `REASONING_MODEL` because critique is a judgment-heavy task — the other specialists do well-scoped work and run on `MODEL`."

**Blocks transfer:** Yes — the model-selection decision is one of the things this capstone is supposed to make concrete.

**Outline-backed:** Yes — the outline lists model selection as core to this capstone and Lesson 31's framework explicitly builds on judgments like this.

---

**Summary:** The notebook's bones are good — the architecture diagram is clear, the pipeline function is well-commented, and the patterns are familiar from prior lessons. The three issues above are concentrated, fixable with small additions, and together cover the gap between "the student saw it work" and "the student knows when and why to use this pattern."

---

## OpenAI Review

**Severity:** [lost thread]  
**Location:** `## 🤖 Phase 1: Build the Specialist Agents`  
**Quote:** `## 🤖 Phase 1: Build the Specialist Agents`  
**What the student experiences:** The capstone says it will show a full pipeline, but once Phase 1 starts, the student sees a lot of agent setup without any reminder of how each role fits into the larger flow. By the time they reach the pipeline function, they may remember the names Researcher / Analyst / Writer / Critic, but not why those are the right roles for this project.  
**Smallest fix:** Add one sentence directly under the Phase 1 header: “Each agent owns one phase of the pipeline, so the code can combine specialized outputs instead of asking one agent to do everything.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** `## 🚀 Phase 2: Run the Full Pipeline`  
**Quote:** `We'll orchestrate the pipeline directly in code — parallel research, sequential writing and critique. This gives us full visibility into each step.`  
**What the student experiences:** The student can tell that orchestration is happening in Python, but not why that matters enough to prefer it over a single big agent prompt. “Full visibility” is vague if you’ve never built an agent system before.  
**Smallest fix:** Add one sentence after the quoted text: “In real projects, this makes failures and intermediate outputs inspectable — you can see which specialist failed, retry one phase, or swap one agent without rewriting the whole system.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** `async def research_pipeline(question):`  
**Quote:** `async def research_pipeline(question):`  
**What the student experiences:** This is the core pattern the lesson wants them to learn, but there’s no brief framing that names the reusable principle. A student can read the function, but may not leave knowing, “this is the template I’d use for my own multi-agent workflow.”  
**Smallest fix:** Add one markdown sentence immediately before the function cell: “This function is the reusable pattern: run independent specialists in parallel, convert failures into fallback text, then feed their outputs into downstream agents one phase at a time.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [unexplained term]  
**Location:** `## 🎬 Phase 3: Demo`  
**Quote:** `⚠️ **Cost note:** This pipeline uses web search, code interpreter, and multiple model calls. Cost and runtime will vary depending on tool behavior and container reuse — budget a few minutes and a small number of API credits per run.`  
**What the student experiences:** “Container reuse” arrives like prior knowledge. A Python-comfortable student who has only taken the earlier notebooks may remember Code Interpreter exists, but may not know what container reuse means or why it affects cost/runtime here.  
**Smallest fix:** Add a short parenthetical: “(for Code Interpreter, reusing the same execution container can make later steps faster/cheaper than starting a fresh one).”  
**Blocks transfer:** No  
**Outline-backed:** No

---

**Severity:** [pacing break]  
**Location:** `## 🎬 Phase 3: Demo`  
**Quote:** `## 🎬 Phase 3: Demo`  
**What the student experiences:** The notebook announces a demo, but the student never actually sees the pipeline run or sees any output walkthrough before being sent to exercises. That creates a sharp break: the lesson has introduced specialist setup, orchestration, parallel failure handling, and critique, but doesn’t land them in one visible end-to-end example.  
**Smallest fix:** Add one short markdown cell under the Demo header: “Run `result = await research_pipeline("your question here")`, then inspect `result["final_report"]` and `result["critique"]` to see how each phase contributed.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Exercise 1 intro  
**Quote:** `Extend the pipeline with a \`FactChecker\` agent that verifies the key claims in the final report using web search before it's delivered.`  
**What the student experiences:** The exercise asks for a new phase, which is good, but the student never got an explicit sentence saying what changes when they adapt this capstone to a different project. They may not realize that “adding a phase” is the main extension mechanism for procedural multi-agent systems.  
**Smallest fix:** Add one sentence to the exercise intro: “This is the main way you’ll customize a pipeline in your own project — keep the orchestration shape, then insert or swap specialist phases where your workflow needs them.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

## Gemini Review

This notebook passed third-pass review with three significant comprehension issues that should be addressed before it can be used by students.

---
**Severity:** `[unexplained term]`
**Location:** Phase 1: Build the Specialist Agents
**Quote:**
```python
analyst_agent = Agent(
    name="Analyst",
    instructions=analyst_instructions,
    model=MODEL,
    tools=[CodeInterpreterTool(tool_config={
        "type": "code_interpreter",
        "container": {"type": "auto"}
    })]
)
```
**What the student experiences:** "I've seen `WebSearchTool()` before, which was simple. What is this `tool_config` dictionary? The previous lessons on built-in tools didn't show this. Is it required? What do `container` and `auto` mean?"
**Smallest fix:** Add an inline comment explaining that this is an optional configuration for performance. Example: `# tool_config with container: "auto" enables container reuse for faster subsequent runs.`
**Blocks transfer:** Yes
**Outline-backed:** No

---
**Severity:** `[confidence gap]`
**Location:** Phase 2: Run the Full Pipeline / Phase 3: Demo
**Quote:** (Structural issue) The notebook defines the entire `research_pipeline` function but never contains a code cell that calls it and shows the output.
**What the student experiences:** "I just read all the code for a complex pipeline, but I never get to see it run. I have no idea what the final report looks like or if the process even works. I can't learn from this if I can't see the result."
**Smallest fix:** Add a code cell after the `research_pipeline` definition that calls the function with an example question and prints the final report.
```python
pipeline_result = await research_pipeline(
    "What is the current state of electric vehicle adoption globally?"
)

print("\\n" + "="*60)
print("📋 FINAL REPORT")
print("="*60)
print(pipeline_result["final_report"])
```
**Blocks transfer:** Yes
**Outline-backed:** Yes

---
**Severity:** `[pacing break]`
**Location:** Multiple sections, including the definition of the `research_pipeline` function.
**Quote:**
```markdown
async def research_pipeline(question):
    """Full multi-agent research pipeline."""

    print(f"🔬 Research Question: {question}")
    # ... entire function body ...
```
**What the student experiences:** "Why is the main pipeline function defined inside a markdown cell instead of a code cell? And in other places, why are markdown headers duplicated inside code cells? The notebook feels broken and I can't tell what is code versus what is commentary."
**Smallest fix:** Remove the duplicated and misplaced cells. The `research_pipeline` function definition should exist only once, in a code cell. The markdown headers (`## 🚀 Phase 2...`, `## 🎬 Phase 3...`) should exist only in markdown cells, not in code cells.
**Blocks transfer:** Yes
**Outline-backed:** No

---

## Grok Review

**Severity:** `[lost thread]`

**Location:** After the `research_pipeline()` definition (end of "Phase 2"), before the "Phase 3: Demo" markdown

**Quote:** (There is no code cell that actually calls the pipeline. The notebook jumps from the function definition + "research_pipeline() ready" print directly to the cost-note markdown and then Practice Exercises.)

**What the student experiences:** I've just read 80 lines of pipeline code that uses parallel gather, exception handling, critique loops, and result chaining, but I never saw it run. I don't know what the final report looks like, whether the fallback text actually appears in practice, or how good the critic's revisions are. The lesson's core idea (a working multi-agent research team) is now buried under scaffolding I can't see in action.

**Smallest fix:** Insert one code cell after the function definition:

```python
result = await research_pipeline(
    "What is the current state of electric vehicle adoption globally?"
)
print("\n" + "="*60)
print("FINAL REPORT")
print("="*60)
print(result["final_report"])
```

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[unexplained term]`

**Location:** Specialist Agents cell (Phase 1)

**Quote:** `tools=[CodeInterpreterTool(tool_config={"type": "code_interpreter", "container": {"type": "auto"}})]`

**What the student experiences:** I know `CodeInterpreterTool` from Lesson 12 and I know we used `WebSearchTool()` two cells ago with no extra arguments. This `tool_config` dict with "container" and "auto" appears with zero explanation. I don't know if it's required, what it does, or what I'd change for my own project.

**Smallest fix:** Add one sentence immediately before the analyst_agent definition:

"The CodeInterpreterTool requires an explicit container configuration in the current SDK to run in an isolated environment."

**Blocks transfer:** Yes

**Outline-backed:** No (outline does not call for teaching this specific config)

---

**Severity:** `[confidence gap]`

**Location:** Exercise 2 cell

**Quote:** `# Note: run the demo cell above first to populate result["final_report"].`

**What the student experiences:** There is no demo cell above that creates a `result` variable (the pipeline function returns a dict but the notebook never assigns it). I can't do the exercise without first fixing the earlier missing demo. Even if I run the pipeline myself, I don't know how to connect the Lesson 09 judge pattern to a real multi-agent output because the scaffolding hides the exact input shape the judge expects.

**Smallest fix:** Change the note to:

"Run the pipeline cell above first, then replace `result[\"final_report\"]` below with `pipeline_result[\"final_report\"]` (or assign the return value to `result`)."

**Blocks transfer:** Yes

**Outline-backed:** Yes (outline explicitly calls for applying the Lesson 09 judge pattern to the multi-agent output)

---

**Severity:** `[lost why]`

**Location:** "Reliability note" markdown + the exception-handling block inside `research_pipeline()`

**Quote:** "`asyncio.gather(return_exceptions=True)` for parallel steps. If one specialist fails, the pipeline detects it, logs the failure, and continues with partial results rather than crashing."

**What the student experiences:** I see the code that checks `isinstance(..., Exception)` and supplies fallback text, but the notebook never actually triggers a failure. I can see *what* the pattern is, but I don't feel why it matters on a real project (no silent degradation, no crashed dashboards at 2 a.m., etc.).

**Smallest fix:** After the pipeline run cell (from finding 1), add one sentence:

"Try temporarily breaking the researcher (e.g. invalid tool) to see the fallback text appear — the pipeline keeps running instead of crashing."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[pacing break]`

**Location:** "Phase 2: Run the Full Pipeline" section (the 60-line `research_pipeline()` function)

**Quote:** The entire `async def research_pipeline(question):` cell

**What the student experiences:** Four distinct new concepts (parallel gather + exception handling, writer input construction, critique step, revision step with full prompt templates) are introduced in a single giant cell with no markdown separators between phases. I lose track of which part I'm supposed to be focusing on.

**Smallest fix:** Insert three short markdown cells inside the function (after each `# -------------------------------------------------------` comment block) titled `#### Phase 1: Parallel research`, `#### Phase 2: Write`, etc. Each can be one sentence long.

**Blocks transfer:** No (student can still copy the pattern, but it feels overwhelming)

**Outline-backed:** Yes

---

This notebook is very close. The four issues above are the moments where a student coming straight from Notebook 17 would quietly get lost or unsure how to reuse the pattern. The rest of the capstone reads clearly and matches the course outline's intent.

---

## DeepSeek Review

## Third-Pass Review: Notebook 18 — Capstone #2

This notebook has significant comprehension issues. While the structural problems (code in markdown cells, duplicate cells, missing code cell separators) are covered by earlier passes, their cumulative effect on a student reading this for the first time is severe. I found 4 strong findings and 1 moderate finding.

---

**Severity:** `[lost why]` + `[confidence gap]`

**Location:** Part "🚀 Phase 2: Run the Full Pipeline" and the subsequent "🎬 Phase 3: Demo"

**Quote:** The entire `research_pipeline()` function is defined in a markdown cell (not runnable). The demo section contains only a code cell with the string:

```python
"## 🎬 Demo\n\n⚠️ **Cost note:** ..."
```

No cell actually calls `await research_pipeline(...)`. The student never sees the pipeline produce output.

**What the student experiences:** I just read the definition of a complex multi-phase function that handles parallel tasks, partial failures, and critique loops. Now I reach the "Demo" section expecting to see it run — and there's no run cell. I can see the code, but I have no idea what the output looks like, whether the failure handling actually works, or how long it takes. I'm being asked to extend this function in the exercise, but I've never seen it work.

**Smallest fix:** Replace the Phase 3 "Demo" code cell (the one with the cost-note string) with an actual demo cell that calls `research_pipeline()` and prints the final report and timing. A one-sentence cost-note markdown cell above it would be sufficient.

```python
result = await research_pipeline(
    "What is the current state of electric vehicle adoption globally?"
)

print("\n📋 FINAL REPORT")
print("=" * 60)
print(result["final_report"])
print(f"\nTotal time: {result['total_time']:.1f}s")
```

**Blocks transfer:** Yes — student cannot see what the pipeline produces, so they cannot confidently extend or evaluate it.

**Outline-backed:** Yes — the outline calls for a working demo of the full pipeline.

---

**Severity:** `[confidence gap]`

**Location:** Phase 1 code cell (the one that is actually a markdown cell containing code)

**Quote:** The agent definitions for Researcher, Analyst, Writer, and Critic appear in a markdown cell, not a code cell. The notebook shows:

```
# Researcher — web search for current information
researcher_instructions = ( ... )
researcher_agent = Agent( ... )
```

This is rendered as text, not executable.

**What the student experiences:** I scroll down from "Phase 1: Build the Specialist Agents" and see what looks like code. I try to run the cell — nothing happens because it's a markdown cell. I'm confused. I see the code, but I can't execute it to verify my setup or test each agent individually before the pipeline runs. I don't know if my API key works with these tools.

**Smallest fix:** Convert the markdown cell containing agent definitions into a proper code cell. The current code cell above it (containing only the string `"## 🧶 Build the Specialist Agents"`) should be replaced with a proper markdown header cell.

**Blocks transfer:** Yes — student cannot run the specialist agents in isolation, making debugging opaque.

**Outline-backed:** Yes — the capstone requires buildable, runnable specialist agents.

---

**Severity:** `[confidence gap]` + `[lost why]`

**Location:** Exercise 1 — Fact Checker

**Quote:** The TODO instructs: "Create a fact_checker_agent with WebSearchTool" and "Run the fact checker with revised_report as input". The exercise provides the full `research_pipeline_with_fact_check` function scaffold, but the student has never seen the `research_pipeline` function actually run (no demo cell exists).

**What the student experiences:** I'm asked to extend a pipeline I've never seen execute. I don't know what format the `revised_report` will be in, what the output of `Runner.run(fact_checker_agent, ...)` should look like, or how to fit the result into the return dict. The fallback-text pattern used in Phase 1 is shown earlier, but the actual mechanics of how `research_findings` and `analysis_findings` look as strings is invisible without seeing a run.

**Smallest fix:** Run the `research_pipeline` once in the demo cell. Then the student can inspect the structure of `result` and see the output format before being asked to extend it.

**Blocks transfer:** Yes — without seeing the pipeline's output, the exercise is guesswork.

**Outline-backed:** Yes — the exercise is listed in the outline.

---

**Severity:** `[pacing break]`

**Location:** The Phase 2 pipeline function definition is in a markdown cell that starts with "async def research_pipeline(question):" — this is a single, very long code block inside a markdown cell.

**Quote:** The entire 60+ line function definition is rendered as markdown text, with inline comments and code formatting.

**What the student experiences:** I'm looking at a wall of code inside a markdown cell. I can't copy it, can't run it, can't scroll through it in an editor. The code is the entire pipeline — the most important piece of the notebook — and it's not executable. I have to read it like a book, which is mentally exhausting for a 60-line async function with multiple phases and error handling.

**Smallest fix:** Move this code into a proper code cell (replace the current code cell that only has a string header). Add execution confirmation at the bottom.

**Blocks transfer:** Yes — student can't run, debug, or inspect the pipeline function.

**Outline-backed:** Yes — the pipeline definition is the core teaching moment.

---

**Severity:** `[lost thread]`

**Location:** End of notebook — two consecutive Key Takeaways markdown cells.

**Quote:** The first cell uses `<br>` tags for spacing; the second cell uses blank lines and no `<br>` tags. Same content, back-to-back.

**What the student experiences:** I read the Key Takeaways, then scroll down and see them again. I wonder if I accidentally scrolled up, or if there's new content I missed. The duplicate disrupts the clean ending of the notebook.

**Smallest fix:** Delete the second Key Takeaways cell (the one without `<br>` tags). Keep the first one that follows the Design Guidelines formatting.

**Blocks transfer:** No — content is fine, duplication is just confusing.

**Outline-backed:** No (this is a formatting issue, not a missing teaching beat).

---

## Summary

This notebook has a clean structure and strong teaching intent, but **crucial code is trapped in markdown cells** and **the demo never runs**. A student who just completed Notebook 17 (Debate & Critique) would be eager to see a full multi-agent pipeline in action — and would be left looking at a page of non-executable code and exercises that assume they already saw it work.

The fix is straightforward: convert the three markdown code blocks (agents, pipeline function, demo) into proper code cells, and add a demo run cell. No changes to the teaching narrative needed — just making the code actually executable.

**Blocking transfer overall:** Yes — the student cannot confidently extend, evaluate, or reuse this pipeline without seeing it run first.
