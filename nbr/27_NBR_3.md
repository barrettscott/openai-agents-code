# Review Report: MCP Fundamentals
Generated: 2026-05-10 | Notebook: /Users/scott/Dropbox/Notebooks/openai-agents/Week_5_MCP_And_Production/27_MCP_Fundamentals.ipynb
Models: Anthropic (claude-opus-4-7 (CLI/Max)) · OpenAI (gpt-5.4) · Gemini (gemini-2.5-pro) · Grok (grok-4.20-0309-reasoning) · DeepSeek (deepseek-reasoner) · Dedup (claude-opus-4-7 CLI/Max)

---

# Grouped Findings: MCP Fundamentals

# Third-Pass Synthesis — Notebook 27 (MCP Fundamentals)

Five reviewers, eight distinct findings after deduplication. The headline issues are a confidence gap around transferring the pattern to other MCP servers (3 reviewers) and the `MCPServerSse` bullet appearing in Key Takeaways without setup (4 reviewers).

---

## Finding 1 — Pattern doesn't transfer to other MCP servers

**Severity:** `[confidence gap]`
**Location:** Part 3, agent demo cell (intro and/or trailing markdown)
**Reviewers:** 3 — OpenAI, Grok, Gemini (Gemini frames it slightly differently, around the `command`/`args` magic string)
**Quote:** `"args": ["-y", "@modelcontextprotocol/server-filesystem", str(workspace.resolve())]` and the surrounding `mcp_servers=[server]` wiring
**What the student experiences:** "The demo worked, but if I wanted to use a GitHub or database MCP server tomorrow, what would I actually change? The `npx` command and args feel like magic specific to this one demo."
**Smallest fix:** Add one sentence near the Part 3 intro or right after the demo output: "To use any other MCP server you only change the `command`/`args` passed to `MCPServerStdio` — the `mcp_servers=[server]` line and the rest of the agent code stay the same. Server commands come from each server's documentation."
**Blocks transfer:** Yes
**Outline-backed:** Yes

---

## Finding 2 — Missing approval gate after Lesson 26's safety lesson

**Severity:** `[lost why]`
**Location:** Part 3, write demo (`create a new file called summary.txt`)
**Reviewers:** 1 — Anthropic
**Quote:** `"Read tasks.txt and create a new file called summary.txt with a one-sentence summary of the task list."`
**What the student experiences:** Lesson 26 made `needs_approval=True` the rule for write actions. Now an MCP agent freely creates files with no approval, and the student silently wonders whether they're forgetting something or whether the rule changed.
**Smallest fix:** Add one sentence to the existing security note: "MCP tools don't carry a `needs_approval` flag the way `@function_tool` does — Lesson 29 adds server-level approval for write and delete actions."
**Blocks transfer:** Yes — a student building their first MCP project will wire up filesystem write access with no approval and not realize the safety layer is missing.
**Outline-backed:** Yes (Lesson 29 outline calls out "Server-level approval before writing or deleting")

*Single-reviewer finding, but high-impact: it directly contradicts the Why-This-Works moment from the previous lesson and is the only finding that meaningfully blocks transfer to a real project.*

---

## Finding 3 — Why MCP over a simple `@function_tool`?

**Severity:** `[lost why]`
**Location:** Part 1, "Three ways to give an agent capabilities" / Part 2 intro
**Reviewers:** 2 — Grok, DeepSeek
**Quote:** "MCP — an external server hosts the tool; your agent connects and uses it automatically" / "The filesystem MCP server gives the agent tools to read, write, and navigate files."
**What the student experiences:** "A filesystem helper is trivial to write as a `@function_tool` — why bother with MCP for this?" The "no custom integration code" benefit is stated but not tied to a payoff the student can picture.
**Smallest fix:** Append to the third bullet in Part 1: "MCP is the third path: instant access to a growing ecosystem of community-maintained servers (GitHub, databases, Slack, calendars) with zero integration code."
**Blocks transfer:** Yes — without this the student doesn't know when to reach for MCP in their own projects.
**Outline-backed:** Yes ("What MCP is and why it matters")

---

## Finding 4 — `MCPServerSse` appears in Key Takeaways with no prior context

**Severity:** `[unexplained term]`
**Location:** Key Takeaways, "Two transport types"
**Reviewers:** 4 — Anthropic, OpenAI, Gemini, DeepSeek
**Quote:** `MCPServerSse — remote server over HTTP/SSE`
**What the student experiences:** SSE appears for the first time in the summary. Students register a vague "remote thing exists" with no mental model, and Python-comfortable students who aren't web devs don't know what HTTP/SSE means.
**Reviewer split on fix:** Gemini and DeepSeek say **delete the bullet** (not in the Lesson 27 outline; introducing it in the summary is confusing). Anthropic and OpenAI say **expand it** with a one-clause anchor ("for connecting to remote MCP servers — covered when we connect to a hosted server" or "use stdio for local, SSE when the server is already running remotely").
**Recommended:** Delete is the cleaner call — the lesson only teaches Stdio, and the outline doesn't call for SSE. If kept, anchor it with the OpenAI phrasing.
**Blocks transfer:** No
**Outline-backed:** No

---

## Finding 5 — "Resources" mentioned but undefined

**Severity:** `[unexplained term]`
**Location:** Part 1, immediately after "The Three Pieces"
**Reviewers:** 3 — OpenAI, Gemini, Grok
**Quote:** "MCP servers can also expose **resources** — readable data like files or database records — though this notebook focuses on tools."
**What the student experiences:** A new MCP concept lands without enough definition to place it. Students don't know how a resource differs from a read-only tool.
**Reviewer split on fix:** OpenAI and Gemini say **add one contrasting clause** ("unlike tools which perform actions, resources are read-only data the agent accesses directly"). Grok says **delete the sentence** entirely (the notebook explicitly skips resources, the outline doesn't list them).
**Recommended:** The clause-add is small enough and preserves the foreshadowing if resources come up later. Delete if they don't appear in any later notebook.
**Blocks transfer:** No
**Outline-backed:** Mixed — OpenAI/Gemini say yes; Grok says no

---

## Finding 6 — Part 3 cell teaches read + write at once

**Severity:** `[pacing break]`
**Location:** Part 3, the long `async with MCPServerStdio(...)` cell with two `Runner.run()` calls
**Reviewers:** 2 — OpenAI, Grok
**Quote:** The 40-line cell containing both the read/list demo and the write demo with verification
**What the student experiences:** Too many new pieces (the `mcp_servers=` parameter, automatic discovery, read vs write side effects) land in one cell. The student can't focus on a single takeaway.
**Smallest fix:** Split the write demo into its own code cell with a one-sentence markdown bridge: "Now let's ask the agent to create a new file using the write tools the server exposed." Optionally a `### 💡 Why the second prompt matters` cell flagging the side-effect risk.
**Blocks transfer:** No (Grok says yes, OpenAI says no — the read demo on its own would still teach the core API; splitting mostly helps focus, not transfer)
**Outline-backed:** Yes

*Note: this finding pairs naturally with Finding 2 — the split point is exactly where the approval-gate caveat would land.*

---

## Finding 7 — "External server" misleads vs the local subprocess demo

**Severity:** `[lost why]`
**Location:** Part 1, "Three ways to give an agent capabilities"
**Reviewers:** 1 — Anthropic
**Quote:** "MCP — an external server hosts the tool; your agent connects and uses it automatically"
**What the student experiences:** "External" sets up an expectation of remote, but two cells later they're spawning a local `npx` subprocess. The conceptual model goes muddy.
**Smallest fix:** Change "an external server hosts the tool" to "a separate MCP server hosts the tool (running locally or remotely)."
**Blocks transfer:** No
**Outline-backed:** Yes

---

## Finding 8 — `MCPServerStdio` and `list_tools()` arrive without orientation

**Severity:** `[unexplained term]` / `[lost why]`
**Location:** Part 2, "Connect and Discover Tools"
**Reviewers:** 2 — Gemini (Stdio naming), OpenAI (`list_tools()` purpose)
**Quote:** `async with MCPServerStdio(` and the `# List available tools — the server tells us what it can do` line
**What the student experiences:** Gemini: "What does Stdio stand for and why is the class named this way?" OpenAI: "If the agent discovers tools automatically, why would I ever call `list_tools()` myself?"
**Smallest fix:** One sentence in the Part 2 intro: "We'll use `MCPServerStdio` — the SDK's client for local servers that run as a subprocess and communicate over standard input/output. Calling `list_tools()` yourself is how you inspect a new MCP server's surface before trusting an agent with it."
**Blocks transfer:** No
**Outline-backed:** Partial — outline calls for tool-discovery beat, doesn't call for the Stdio-naming explanation

---

## Single-reviewer finding not promoted

DeepSeek's verification-via-traces finding (no visible evidence the agent called the MCP tools) is a real moment of doubt, but the proposed fix points students at the OpenAI dashboard, which sits outside the notebook's flow and assumes dashboard access. Worth considering as a `### 💡` note if traces are already taught earlier; otherwise lower priority than the others.

---

## Recommended fix order

1. **Finding 2** (approval gate) — only finding that meaningfully breaks safety transfer from Lesson 26
2. **Finding 1** (transfer to other servers) — 3 reviewers, blocks real-project use
3. **Finding 3** (why MCP at all) — 2 reviewers, blocks the "when do I reach for this" judgment
4. **Finding 4** (delete `MCPServerSse` bullet) — one-line removal, 4 reviewers
5. **Finding 6** (split Part 3 cell) — pairs with Finding 2's fix location
6. **Findings 5, 7, 8** — all small, can be batched

---

# Individual Reviews

The grouped findings above are your primary working surface. The full individual reviews below are for verifying nuance.

---

## Anthropic Review

# Third-Pass Review — Notebook 27 (MCP Fundamentals)

Three findings.

---

**Finding 1**

**Severity:** `[lost why]`

**Location:** Part 3 — Agent with MCP Tools, write demo

**Quote:** `"Read tasks.txt and create a new file called summary.txt with a one-sentence summary of the task list."`

**What the student experiences:** The student just finished Lesson 26, where every write action (`process_refund`) was gated behind `@function_tool(needs_approval=True)` and the read-vs-write safety principle was the entire Why-This-Works moment. Now they watch an MCP filesystem agent freely create a file with no approval anywhere in the loop. They silently wonder: *did we forget to add approval? Should I be doing that here? Why is the rule different now?* The security note addresses scoping but doesn't address the missing approval gate.

**Smallest fix:** Add one sentence to the security note (or as a separate `### 💡` note near the write demo): "MCP tools don't carry a `needs_approval` flag the way `@function_tool` does — Lesson 29 adds server-level approval for write and delete actions."

**Blocks transfer:** Yes — without this, a student building their first MCP-backed project will wire up filesystem write access with no approval and won't know they're missing the safety layer they just learned.

**Outline-backed:** Yes — Lesson 29 outline explicitly calls out "Server-level approval before writing or deleting."

---

**Finding 2**

**Severity:** `[unexplained term]`

**Location:** Key Takeaways

**Quote:** `"MCPServerSse — remote server over HTTP/SSE"`

**What the student experiences:** They've only seen `MCPServerStdio` in the lesson. SSE is introduced for the first time at the end with no setup — and "HTTP/SSE" is opaque to a Python-comfortable developer who isn't a web dev. They read it, register a vague "remote thing exists," and move on without a real mental model.

**Smallest fix:** Expand the bullet to: `"MCPServerSse — remote server over HTTP using Server-Sent Events (covered when we connect to a hosted MCP server)"` — or drop SSE specifics and just say `"MCPServerSse — for connecting to remote MCP servers over HTTP"`.

**Blocks transfer:** No — they didn't use it in this lesson.

**Outline-backed:** No — outline doesn't call out the SSE transport specifically.

---

**Finding 3**

**Severity:** `[lost why]`

**Location:** Part 1 — What Is MCP?, "Three ways to give an agent capabilities"

**Quote:** `"MCP — an external server hosts the tool; your agent connects and uses it automatically"`

**What the student experiences:** "External server" sets the expectation of something running on a remote machine. Two cells later, they're running `npx` to spawn a local subprocess and the docstring calls it a "local subprocess." The student isn't sure if the filesystem server is actually external, or if they were misled, or if MCP is fundamentally remote and this demo is a special case.

**Smallest fix:** Change "an external server hosts the tool" to "a separate MCP server hosts the tool (running locally or remotely)."

**Blocks transfer:** No — they can still follow the pattern. But the conceptual model stays muddy until Lesson 28 clarifies it.

**Outline-backed:** Yes — outline calls for this teaching beat ("How MCP servers relate to `@function_tool` and built-in tools — three ways to give an agent capabilities").

---

Otherwise this notebook is in good shape for a student at this point in the course. Setup, tool discovery, and the two-demo flow (read, then write) read cleanly, and the USB-C analogy lands. Finding 1 is the only one that meaningfully blocks transfer.

---

## OpenAI Review

**Severity:** [unexplained term]  
**Location:** Part 1, “MCP servers can also expose resources”  
**Quote:** “MCP servers can also expose **resources** — readable data like files or database records — though this notebook focuses on tools.”  
**What the student experiences:** “I just learned server/client/tools, and now ‘resources’ appears as another MCP concept without enough explanation to place it. I can tell it matters later, but I don’t yet know how it differs from a tool.”  
**Smallest fix:** Add one clause that contrasts it with tools, e.g. “Resources are read-only data the agent can access, unlike tools, which are actions it can call.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** Part 2, “Connect and Discover Tools”  
**Quote:** “    # List available tools — the server tells us what it can do”  
**What the student experiences:** The tool listing works, but it can feel like a diagnostic step rather than a meaningful MCP concept. A first-time learner may not see why they should ever call `list_tools()` themselves if the agent can already discover tools automatically.  
**Smallest fix:** Add one sentence before or after the cell: “This matters when you’re evaluating a new MCP server — inspect its tool surface first so you know what capabilities and risks you’re giving the agent.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [confidence gap]  
**Location:** Part 3, “Agent with MCP Tools”  
**Quote:** “Pass the MCP server to an agent via `mcp_servers=[server]`. The agent automatically sees and uses all the tools the server exposes — including write tools.”  
**What the student experiences:** The student sees the pattern work, but not what they would actually change in their own project. They can copy the exact filesystem demo, but may not yet realize that the reusable part is just the server connection plus `mcp_servers=[server]`, while the `npx` package and workspace path are the only server-specific pieces.  
**Smallest fix:** Add one sentence right under this intro: “In your own project, the pattern stays the same — you mainly swap the server command/args for a different MCP server and keep the agent wiring unchanged.”  
**Blocks transfer:** Yes  
**Outline-backed:** Yes

---

**Severity:** [pacing break]  
**Location:** Part 3: Agent with MCP Tools  
**Quote:** “## 🤖 Part 3: Agent with MCP Tools”  
**What the student experiences:** This section teaches two new ideas at once: agent wiring with `mcp_servers=[fs_server]` and the much more important safety implication that the same agent can perform writes. The write demo is valuable, but it arrives in the same code cell as the first successful read/list demo, so the student has to absorb both the core API pattern and the side-effect risk together.  
**Smallest fix:** Insert a brief markdown separator between the two actions, e.g. a `### 💡 Why the second prompt matters` cell that says the first prompt shows automatic tool use, while the second shows that MCP tools can have side effects just like local write tools.  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

**Severity:** [lost why]  
**Location:** Key Takeaways, “Two transport types”  
**Quote:** “**Two transport types:**  
- `MCPServerStdio` — local subprocess via `npx` or similar command  

- `MCPServerSse` — remote server over HTTP/SSE”  
**What the student experiences:** `MCPServerSse` appears for the first time in the summary, but the student hasn’t seen it in the notebook and doesn’t yet know why they should care about the distinction. It lands as a term to memorize rather than a practical idea.  
**Smallest fix:** Add a short clause to anchor the distinction, e.g. “Use stdio for local servers you launch yourself; use SSE when the server is already running remotely.”  
**Blocks transfer:** No  
**Outline-backed:** Yes

---

## Gemini Review

This notebook passed third-pass review with a few small opportunities for clarification. The core concepts of MCP, the USB-C analogy, and the demo flow are clear and effective for a student at this level. The following findings are minor but would improve student confidence and reduce potential confusion.

---

**Severity:** `[unexplained term]`
**Location:** Part 1, "The Three Pieces" section
**Quote:** `MCP servers can also expose **resources** — readable data like files or database records — though this notebook focuses on tools.`
**What the student experiences:** "What's a 'resource' and how is it different from a tool that reads a file? Am I supposed to know this or is it just a random new term?"
**Smallest fix:** Add a single sentence to clarify the distinction. For example: "Unlike tools which perform actions, resources are data objects the agent can access directly."
**Blocks transfer:** No
**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`
**Location:** Part 2, "Connect and Discover Tools" code cell
**Quote:** `"args\": [\"-y\", \"@modelcontextprotocol/server-filesystem\", str(workspace.resolve())]`
**What the student experiences:** "I see this magic string works for the filesystem server, but where did it come from? If I wanted to use a different MCP server, I wouldn't know what to put here. I can copy this example, but I don't understand the general pattern."
**Smallest fix:** Add a short markdown cell explaining the pattern. For example: "The `command` and `args` for an MCP server are found in its documentation. The values used here are the same as you would type on the command line to run the server manually."
**Blocks transfer:** Yes
**Outline-backed:** No

---

**Severity:** `[unexplained term]`
**Location:** Part 2, "Connect and Discover Tools" code cell
**Quote:** `async with MCPServerStdio(`
**What the student experiences:** "What does `Stdio` stand for? I can guess it's 'standard I/O', but the notebook doesn't explain why this class is named this way or what that implies."
**Smallest fix:** Add a sentence of explanation when the class is first introduced. For example, in the markdown for Part 2: "We'll use `MCPServerStdio`, the SDK's client for connecting to local servers that run as a subprocess and communicate over standard input/output (stdio)."
**Blocks transfer:** No
**Outline-backed:** No

---

**Severity:** `[unexplained term]`
**Location:** Key Takeaways
**Quote:** `- MCPServerSse — remote server over HTTP/SSE`
**What the student experiences:** "What is `MCPServerSse`? The notebook never mentioned this. Did I miss something? What is HTTP/SSE?"
**Smallest fix:** Remove this bullet point. This concept is not taught in this notebook and introducing it for the first time in the summary is confusing. Let the notebook that teaches remote servers introduce this class.
**Blocks transfer:** No
**Outline-backed:** No

---

## Grok Review

**Severity:** `[pacing break]`

**Location:** Part 3 markdown header + the long code cell that follows it ("## 🤖 Part 3: Agent with MCP Tools")

**Quote:** "Pass the MCP server to an agent via `mcp_servers=[server]`. The agent automatically sees and uses all the tools the server exposes — including write tools." (followed immediately by the 40-line `async with MCPServerStdio(...)` cell containing two separate `Runner.run()` calls, a write demo, and post-verification code)

**What the student experiences:** I've just finished the filesystem connection and tool-listing demo, and now this one cell throws the full agent, two different user queries, automatic tool discovery, a write operation, and disk verification at me all at once. I can see it worked, but I'm holding too many new pieces (the `mcp_servers=` parameter, how discovery actually happens, the difference between read and write behavior) and can't focus on any single takeaway.

**Smallest fix:** Split the write demo into its own short code cell. Insert a one-sentence markdown cell between them: "Now let's ask the agent to create a new file using the write tools the server exposed."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[confidence gap]`

**Location:** End of the Part 3 demo cell (after the final `print("=" * 60)`)

**Quote:** (no explicit sentence after the demo; the next cell is the Cleanup header)

**What the student experiences:** The agent successfully read files, summarized them, and wrote `summary.txt` using tools I never defined. It feels magical, but I have no idea what I'd change to point the exact same agent at a different MCP server (e.g. a GitHub or database server). The `params` dict with the `npx` command feels like magic numbers specific to this one demo.

**Smallest fix:** Add one sentence after the demo output: "To use any other MCP server you only change the `command`/`args` passed to `MCPServerStdio`; the `mcp_servers=[server]` line and the rest of the agent code stay exactly the same."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[lost why]`

**Location:** Part 1 (the cell that begins "**Three ways to give an agent capabilities:**")

**Quote:** "**Three ways to give an agent capabilities:**  
- `@function_tool` — you write the code; the tool runs locally in Python  
- Built-in tools (e.g. web search) — OpenAI hosts and runs them; zero code needed  
- MCP — an external server hosts the tool; your agent connects and uses it automatically"

**What the student experiences:** I can see the three options and the USB-C analogy, but after the demo I'm still not sure when I'd actually choose MCP over just writing a normal `@function_tool` in my own project. The "no custom integration code" benefit is mentioned but never tied to a concrete real-world payoff (e.g. instantly using community-maintained servers for Slack, GitHub, databases, etc.).

**Smallest fix:** Change the final sentence of that cell to: "MCP is the third path: you get instant access to a growing ecosystem of community servers (GitHub, databases, calendars, etc.) with literally zero integration code."

**Blocks transfer:** Yes

**Outline-backed:** Yes

---

**Severity:** `[unexplained term]`

**Location:** The markdown cell immediately after the "Three Pieces" diagram

**Quote:** "MCP servers can also expose **resources** — readable data like files or database records — though this notebook focuses on tools."

**What the student experiences:** The notebook has been careful to contrast tools with the three capability methods I already know, but now "resources" appears with no definition or example. I don't know if a resource is different from a tool, whether the agent sees them automatically, or why the notebook bothers to mention them if we're not using them.

**Smallest fix:** Delete the entire sentence (the notebook explicitly says it focuses on tools, and the Course Outline does not list resources for this lesson).

**Blocks transfer:** No

**Outline-backed:** No

---

This notebook is otherwise excellent. The USB-C analogy is memorable, the progression from "what is MCP" → tool discovery → agent usage is logical, and a student coming from Notebook 26 can follow the code comfortably. The four items above are the only moments where I (as a first-time reader) would have paused or felt uncertain. With those tiny additions/deletions the notebook would be flawless at this stage of the course.

---

## DeepSeek Review

## Third-Pass Review: Notebook 27 – MCP Fundamentals

This notebook is generally clear and well-structured for a student who has completed Notebook 26. It introduces MCP with a clean analogy, a simple demo, and appropriate context. However, three specific moments would likely cause a student to pause or lose confidence.

---

### Finding 1 — [unexplained term]

**Location:** Key Takeaways cell – bullet list under "Two transport types:"

**Quote:** `- MCPServerSse — remote server over HTTP/SSE`

**What the student experiences:** The term "SSE" (Server-Sent Events) appears for the first time in the entire notebook here, with no prior introduction, no definition, and no code example. A student who just learned about MCPServerStdio will wonder "What's SSE? Why is there a second transport type I've never seen?" This is a minor but real comprehension break — the student now has a dangling term they don't know how to evaluate.

**Smallest fix:** Remove the MCPServerSse bullet from Key Takeaways. The lesson only teaches MCPServerStdio, and the outline for Lesson 27 does not call for SSE. Keep Key Takeaways focused on what was actually demonstrated.

**Blocks transfer:** No — the student can still use Stdio successfully.

**Outline-backed:** No (SSE is not in the Lesson 27 outline).

---

### Finding 2 — [confidence gap]

**Location:** The agent demo cell (the `async with MCPServerStdio` block that runs two queries and prints results)

**Quote:** The code and its printed output (the agent's final text response)

**What the student experiences:** The agent returns text about the workspace and creates a summary file. The student saw the server’s tool list earlier but never sees evidence that the agent actually called `read_file` or `write_file`. There is no explicit "this ran the MCP tool" indicator. A student comfortable with Python will think "Did the agent really use those MCP tools, or did it just answer from its training data? How would I check?" Without a way to verify, confidence in the MCP mechanism is low.

**Smallest fix:** After the agent demo (before the Cleanup cell), add one sentence:

> Each tool call is captured in the trace — open the OpenAI dashboard after the run to see exactly which MCP tools the agent called and in what order.

**Blocks transfer:** Yes — a student who cannot verify tool usage cannot independently validate MCP connections in their own projects.

**Outline-backed:** Yes — the outline includes "Agent discovers tools automatically" which requires the student to trust that discovery and usage actually happen; a verification path closes that gap.

---

### Finding 3 — [lost why]

**Location:** Part 2 intro paragraph: "The filesystem MCP server gives the agent tools to read, write, and navigate files."

**Quote:** (the opening sentence of Part 2)

**What the student experiences:** The student can see *what* the filesystem MCP server does, but may not understand *why* this is better than writing a `@function_tool` with `pathlib`. They have written `@function_tool` helpers in several earlier notebooks (Lessons 04, 05, 08, 26). A filesystem tool is trivial to write — so why introduce MCP for this? The notebook’s USB-C analogy and the three-ways comparison are in Part 1, but by the time the student reaches the concrete filesystem demo, the "why MCP" motivation is no longer fresh.

**Smallest fix:** In the Part 2 intro, after the first sentence, add:

> The filesystem server is a simple starting point — the real power of MCP is that it decouples tool implementation from agent code. Any server (database, web service, cloud API) that already has an MCP implementation can be plugged in without writing a single Python wrapper.

**Blocks transfer:** No — the student can still follow the demo, but may not see why MCP matters beyond this course.

**Outline-backed:** Yes — the Topics list for Lesson 27 includes "What MCP is and why it matters."

---

**Summary:** The notebook is fundamentally sound. The three issues above are small, targeted fixes — two sentences and one bullet removal — that would close significant comprehension and confidence gaps for a student encountering MCP for the first time.
