# {{NOTEBOOK_LABEL}} Codex Editor-First Camera Candidate

This is the internal editing contract completed before either external audit.

1. Verify and pin the live notebook, frozen baseline, preflight, guidelines,
   outline, and complete 1440px baseline render.
2. Read every cell in source order and inspect the complete render.
3. Map every adjacent-markdown run as one visible surface.
4. Apply the from-scratch test to the presentation spine and triggered surfaces:
   title/problem, Part intros, important pre-code bridges, post-demo explanations
   and takeaways, safety/notes, recap, adjacent runs, surfaces over six beats,
   and nested structures.
5. Record `KEEP`, `UPGRADE`, `FIX`, or `MERGE/DROP`. Rank and cap optional
   upgrades at six; do not cap real fixes. Do not manufacture edits to make the
   pass look productive. Recorded notebooks are `FIX`-only unless Scott has
   explicitly authorized structural or discretionary work.
6. For every change, provide human-readable full-run before/after source,
   camera-shape rationale, spoken path, measurements, and native-scale crops.
7. Build a deleted-claim ownership report. A claim cannot survive only in code,
   output, an exercise, or the final recap.
8. Preserve existing double `<br>` pairs and all unapproved code source.
9. Run the complete mechanical gate on the exact candidate.
10. Freeze the candidate, report, ownership report, renders, crop manifest, and
    both coordinated audit prompts together. Record every SHA-256.

Do not edit the live notebook. The next action is external audit of this exact
candidate.
